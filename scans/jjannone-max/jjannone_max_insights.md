# jjannone Max Corpus — Insights

Patching insights extracted from jjannone's personal `.maxpat` corpus
under `_Projects__________________/`. Insights live in the patches, not
in folder names — every entry below was extracted by reading the `.maxpat`
in full, not by pattern-matching on filenames or directory structure.

This file supplements `scans/c74-forum/forum_insights.md` and
`scans/cookbook/cookbook_insights.md`, and serves as the source-of-record before
the closing-sweep promotes selected entries into `scans/c74-forum/forum_insights.md`,
`patching/MAX_PATCHING.md`, `SPEC_REFERENCE.md`, or `packages/package_objects.json`.

Each entry follows the same shape as `scans/c74-forum/forum_insights.md`:

> **Insight title**
> The insight in 1–3 sentences.
> *Source*: relative path under Projects root (and the patch filename it was extracted from).
> *Why it matters*: one sentence on when this changes a patching decision.

If an insight is specific to an object, also propagate it to the relevant
section of `SPEC_REFERENCE.md`. If a package-object insight accumulates,
fold it into `packages/package_objects.json`.

Entries flagged **[PROMOTION-CANDIDATE]** are queued for evaluation as
candidate rules — see `JJANNONE_MAX_CRAWL_LOG.md` § "Promotion pass".
Promotion is user-confirmed only — see "Rules from Corrected Errors" in
CLAUDE.md.

---

## Audio (MSP)

(empty — populate as you crawl.)

## Video / Jitter

(empty.)

## Gen / gen~ / jit.gen

(empty.)

## JS / v8

(empty.)

## MIDI

(empty.)

## UI / Presentation

(empty.)

## Patching Patterns

(empty.)

## Performance

(empty.)

## Max for Live

(empty.)

## RNBO

(empty.)

## Workflow / Naming / Repo Discipline

(empty — observations specific to how jjannone organizes patches across
the corpus, e.g. naming conventions, abstraction-folder layout, version
suffix discipline. Worth capturing because they reveal pragmatic answers
to questions the official docs don't cover.)
