# Work History

- 2026-03-30: Set up GitHub connection, added Work History tracking to CLAUDE.md and moved log to this file. (Note: a stop hook for auto-updating work history was attempted but never reliably worked — replaced by proactive in-session updates per CLAUDE.md.)
- 2026-03-30: Replaced `11clicks` with `shell` in mouse-control patch, reformatting all commands as cliclick CLI calls. Clarified in CLAUDE.md that spec embedding is required whenever the Claude2Max workflow is used, not just when running the converter binary.
- 2026-03-31: Reviewed prior session work, verified WORK_HISTORY.md structure, and configured automatic work history updates for all Claude Code instances via stop hook.
- 2026-03-31: Created one-sound-a-day repo (local only — git server proxy restricts to Claude2Max; push manually). Original patch on main; JS-refactored branch `claude/js-refactor` replaces date/time/match/blink/newday logic with `v8 onesound.js`. Documented v8 object usage, external JS files, message routing, dialog/playlist~/umenu patterns in CLAUDE.md and SPEC_REFERENCE.md.
- 2026-04-03: Added `add_tutorial.py` — analyzes a .maxpat's data-flow graph (wave-depth grouping), generates tutorial steps with object descriptions, and adds a umenu + prev/next nav + v8 JS controller + thispatcher to the patch. Highlights objects per step via bgcolor scripting; annotation comments show/hide per step. Tested on jit-grab-scale.maxpat (9 steps). Also deleted redundant external .json spec files (spec now embedded in .maxpat only). Added Tutorial System section to CLAUDE.md.
- 2026-04-03: Fixed tutorial annotation z-order (annotations now last in boxes array = on top), positioned annotations adjacent to their panels with correct bubbleside arrows. Added ALL CAPS naming convention to CLAUDE.md. Configured bypass permissions globally (`~/.claude/settings.json` + UI mode selector). Added audience note: repo is for students, CLAUDE.md must be beginner-comprehensive.
- 2026-04-03: Rewrote tutorial step grouping in `add_tutorial.py` — spatial clustering (X+Y distance), any-pair merging of connected objects, bounding box limits. Switched annotations to bubble-arrow comments (`bubble: 1` + `bubble_bgcolor`), panels to locked background (`locked_bgcolor: 1`). Fixed strip-before-generate ordering bug. Added `TUTORIAL_GUIDELINES.md` with process lessons. Configured global `~/.claude/settings.json` with `dontAsk` permission mode and broad Bash/Read/Write/Edit allow rules.
- 2026-04-03: Added AI-enhanced tutorial descriptions to `add_tutorial.py` — `--ai` flag (Claude API/Haiku), `--analyze`/`--steps-json` two-pass workflow (no API key, works from Claude Code). Fixed Max z-order (first in boxes array = on top, not last), corrected @bubbleside mapping (0=top, 1=left, 2=bottom, 3=right), restored flexible annotation placement. Added z-order and bubbleside docs to SPEC_REFERENCE.md, TUTORIAL_GUIDELINES.md, and CLAUDE.md.
- 2026-04-03: Added PreToolUse hook (hooks/sync_maxpat.py + .claude/settings.json) — any .maxpat file Claude reads is automatically synced: generates an embedded spec if none exists, or reconciles the existing spec with manual edits. Also: analyzed taylorbrook/MAX-MSP_CC_Framework; imported 1,694-object database (max_objects.json via build_objects_db.py), applied expert overrides, added 6 missing variable_io rules (spray, funnel, join, unjoin, selector~, matrix~), updated appversion to Max 9, fixed playlist~ outlet count bug (5→3), added Object Correctness Notes and Object Relationships sections to SPEC_REFERENCE.md, added upstream maintenance note to CLAUDE.md. Added `sync` command to spec2maxpat.py: reverse-engineers a spec from any .maxpat (no embedded spec) or reconciles an existing embedded spec with manual edits made in Max. Updated CLAUDE.md workflow to cover both cases.
- 2026-04-17: Created LCARS Panel Generator jsui (lcars.js + lcars.maxpat) — draws LCARS-style panels with 4 color schemes, generates random layouts, persists last panel to file, and supports JSON import/export.
- 2026-04-17: Fixed overlapping labels/controls in lcars.maxpat; added UI Layout spacing rules section to CLAUDE.md covering label-width estimation, horizontal spacing formula, routing-object widths, and the loadmess init chain pattern.
- 2026-04-17: Synced user edits to lcars.maxpat (jsui filename attr, resize to 560px, removed title comment); documented three rules in Claude2Max CLAUDE.md: use jsui filename attr, right-size UI elements, no redundant title comments.
- 2026-04-25: Built Improvised Movement+Music Ensemble Sequencer v3 and v4 (ensemble-sequencer-v4.maxpat + ensemble-v4.js + display.js). v4 features: grey names until piece starts (setactive), large GL timer, randomized performer order, max permutations with coverage-first selection, max consecutive same-role parameter, permutation list in jit.cellblock with index column and dynamic rows/cols, perm count message box, 7-outlet brain. Fixed jit.gl.text centering (align 1, int type — not anchor_x or string "center"), fixed jit.cellblock set COL ROW order (column-first). Moved all object-specific behavioral notes from CLAUDE.md to SPEC_REFERENCE.md. Added rules: never regress functionality when changing modality; always verify Max attributes against documentation before use (type, values, default, settability); object-specific knowledge goes in SPEC_REFERENCE.md not CLAUDE.md; update WORK_HISTORY proactively, not just at session end; read WORK_HISTORY at session start.
- 2026-04-26: Fixed perm-summary.js and ensemble-v5.js: (1) pass performer constraints in summary data so noSoloDance/noSoloMusic suppress expected observations; (2) track music and dance streaks separately, report all outliers not just one; (3) remove "N sections have a solo performer" (not notable); (4) add soft streak penalty in optimizeOrder (always active, grows with streak length beyond 3, weight 0.6) to reduce long same-role runs without hard-blocking.
- 2026-04-26: Added scrollbar + autoscroll:0 to perm-summary textedit in ensemble-sequencer-v5.maxpat. Mirror avoidance: non-sequential mirrors are fine and not notable; only adjacent mirrors are avoided. ensemble-v5.js: added permKey()/mirrorKey() helpers; optimizeOrder() adds +3.0 penalty when next candidate is the mirror of the current section. perm-summary.js: removed mirror-section and bookend-mirror observations (non-sequential mirrors are not notable). Added MOVING column (4th) to jit.cellblock permlist showing how many performers move in the next transition (-- for last row); widened cellblock presentation_rect from 346→432px.  MOVING column shifted to represent next transition, not previous.
- 2026-04-30: Applied Soviet Constructivist theme (c2m-themes/themes/soviet.css) to drift-sequencer.maxpat presentation view. Cream paper bg, red accent multisliders/bars, ink text, large bold title. Key fixes: (1) removed unconditional `parameter_enable`/`saved_attribute_attributes` injection for `live.*` objects from `spec2maxpat.py` — was locking gain slider on load; (2) fixed `live.gain~` arrow invisibility on cream bg using `@trioncolor`/`@tricolor` (not `bglocked`); (3) removed stale `sov_gain_bg` dark panel and extended `sov_bg` to cover full presentation height; (4) compensated for Max comment internal padding (~4px) by offsetting comment x positions to achieve precise left-alignment; (5) fixed window rect to match presentation content size. Enshrined rules: (a) `live.*` on light/dark themes — set `@tricolor`/`@trioncolor` when bgcolor changes significantly; (b) Max comment text offset — ~4px internal padding, compensate and verify visually; (c) sync-first applies to programmatic .maxpat post-processing too, not just user GUI edits; (d) converter must match Max's defaults, never exceed them (CLAUDE.md).
- 2026-04-29: Discovered c2m-themes folder — a complete CSS design system for Claude2Max presentation panels and jsui canvases, with 10 themes (5 original sci-fi + 5 new design-history themes), a token contract, and an UPSTREAM-SKILL.md manifest for a `/claude2max-design` skill. Added task to Opus queue to test the skill before integrating into the presentation UI workflow.
- 2026-04-29: Explored applying LCARS-Bold theme to drift-sequencer.maxpat. Iterations: (1) attribute-only restyling of existing controls (amber multisliders, dark number boxes); (2) added `panel`-based chrome (rail/header/pills) — deemed not convincing as LCARS; (3) generated authentic Okuda PNG background via PIL (large sweeping inner elbow, desaturated peach/salmon/lavender/mustard palette, numeric codes, multi-cell statement bar) referenced from a real screen-used Okuda console image, integrated as `fpic` at z-order 0; (4) reverted all theme styling per user request. Final patch state: tutorial panels restored, `live.gain~` `_parameter_initial` attributes stripped (now uses default settings instead of forcing to 0 dB on load — note: 0 dB is unity gain, not silence). Added `MAX_PATCHING.md` rule "Themes carry graphic elements, not just colors" and `c2m-themes/README.md` per-theme graphic-elements checklist (10 themes, plus a "how to read CSS frame tokens" guide). Enshrined rules: (a) audio parameter values are meaningless without their unit — verify `@units`/`@range` before describing behavior (`SPEC_REFERENCE.md`); (b) when a multi-part clarifying question is partly answered, re-ask the unanswered parts rather than acting on a best guess (`CLAUDE.md`).
- 2026-04-29: Repaired tutorial system across drift-sequencer, face-capture, and jit-grab-scale. Root cause: `spec2maxpat.py` `_PRESERVE_ATTRS` and `reconcile_spec()` only preserved styling attrs (bgcolor/textcolor/font*), so anything affecting **identity / visibility / z-order / runtime behavior** was silently dropped on each sync→convert cycle, breaking tutorials over time. Fixes: (a) extended `_PRESERVE_ATTRS` to include `varname`, `hidden`, `background`, `bordercolor`, `border`, `rounded`, `locked_bgcolor`, `bubble`, `bubbleside`, `bubblepoint`, `bubbletextmargin`, `bubble_bgcolor`, `items`, `prefix`; (b) updated `reconcile_spec()` to re-extract `_PRESERVE_ATTRS` from live boxes for matched objects, not just `pos`/`text` (without this, varnames added in Max's Inspector were invisible to sync); (c) restored proper `tut-panel-N`/`tut-ann-N` varnames, `hidden:1`, `background:1`, `locked_bgcolor:1`, panel chrome (border/rounded/bgcolor/bordercolor), bubble formatting (`bubble:1`, `bubble_bgcolor`, `bubbleside`), 220px standard width, and umenu `items` arrays on all 3 patches. Refactored: tutorial annotations now park at a single shared "stow" position per patch (out of the way of editing), and the JS controller's `gotoStep()` moves the current step's annotation to its active position via a baked-in `ACTIVE_RECTS` array (Max box-rect format `[x1,y1,x2,y2]`) plus `STOW_RECT`. Round-trip verified: sync→convert preserves all tutorial state across all three patches. Fixed face-capture's `t b l` → `t l b` (right-to-left fire order means the operation that runs FIRST must be the rightmost arg; existing wiring was topologically correct but trigger args had bang and list swapped, causing iter to receive a no-op bang and clear to wipe the menu after population). Enshrined rules: (a) `TUTORIAL_GUIDELINES.md` — full structural contract (panel attrs, annotation attrs, umenu items, patchlines, JS structure) plus comment-pile pattern explanation and a sync/convert breakage diagnostic script; (b) `CLAUDE.md` — "Reference Instances Illustrate Principles" (enshrine the structural contract, not specific files; files are illustrative, may be renamed); (c) `MAX_PATCHING.md` — "trigger for sequencing — verify both order and outlet types" (with `t l b` vs `t b l` worked example).
- 2026-04-28: Restructured docs: extracted MAX_PATCHING.md from CLAUDE.md (patching principles, presentation guidelines, doc verification rules, common pitfalls); generalized patching principles to lead with intent; added model-switching rules (6 Opus trigger cases, exact switch commands, Claude Design workflow for presentation UI); added rules for verifying external state before asserting and always generalizing before enshrining; reorganized TASK_QUEUE.md into Opus/Sonnet sections; added TouchOSC mk2 Integration task.
- 2026-04-27: Fixed drift-sequencer and face-capture tutorial/patch bugs: bubbleside=1 on tutorial annotations 1–9 (were pointing up instead of left); face-capture vdevlist→umenu chain (inserted route vdevlist → t b l → clear + iter → prepend append). Reviewed drift-sequencer presentation view via screenshot; fixed layout against CLAUDE.md guidelines: content-driven multislider width (400px = 8 steps × 50px), monospace Courier font on all labels, ignoreclick+triangle=0 on display-only note number box. Enshrined rules: read presentation guidelines before visual review; multislider width = step_count × 40–50px (never window-derived); Courier/Monaco uses ~8.5px/char (not 7.5). Cleaned up youthful-austin worktree (branch was fully behind main, nothing to merge).
- 2026-04-26: Extended RefpageCache to cache digest, attributes, messages, object arguments, output descriptions, and see-also in a single XML pass (spec2maxpat.py). Added describe() convenience method. Created perm-summary.js (10 analysis dimensions, ranked plain-English output). Created ensemble-v5.js (outlet 7 → perm-summary) and ensemble-sequencer-v5.maxpat (adds perm-summary v8 + textedit in presentation view). Implemented layout engine Phase 1 (presentation_layout() — grid-based placement from {"col","row"} hints) and Phase 2 (auto_layout improvements: side-by-side grouping of inputs sharing a downstream destination). Phase 3 (screenshot verification) queued. Also fixed label-test and clamp-test patches: inlet/outlet 30×30, @comment via attrs, clip not max/min, * 1.0 for float multiply, no double title. Added rules: verify object existence + digest before use; rules from errors require user confirmation before enshrining; inlet/outlet @comment on box AND adjacent comment box.
- 2026-04-25: Replaced max_objects.json + build_objects_db.py with RefpageCache in spec2maxpat.py — on-demand XML parsing of C74 maxref.xml per object, session-cached, zero build step, always matches installed Max. Removed taylorbrook dependency entirely. Added TASK_QUEUE.md with two queued tasks (extended RefpageCache metadata; layout engine + screenshot verification). Added session-start check script (bash, /tmp timestamp) to both Claude2Max CLAUDE.md and global ~/.claude/CLAUDE.md. Established subpatcher inlet/outlet labeling rule: @comment attr on p object (outside) + comment box adjacent to inlet/outlet (inside). Created label-test.maxpat demonstrating convention. Fixed label-test.maxpat errors: inlet/outlet size now 15×15 (added to UI_SIZES), @comment attr via spec attrs field, * not *. for int×float multiply, removed redundant title comment, inputs arranged side by side.
- 2026-04-29: Fixed face-capture face-count detection. Two bugs: (a) `jit.matrix 4 char 640 360 @usedstdim 1` had `@usedstdim 1` set without `@dstdimstart`/`@dstdimend` companions — a no-op toggle; removed the attribute entirely. (b) `cv.jit.faces` outlet 1 wired directly into `> 0`; that outlet emits info messages like `nfaces N`, not raw integers, so the comparison never fired. Inserted `t b l` to sequence matrix-in then `getnfaces` query, and `route nfaces` between outlet 1 and `> 0`. Enshrined two rules in `SPEC_REFERENCE.md`: (i) attribute "enable" toggles must be set as a group with their companion bounds (e.g. `@usedstdim 1` requires `@dstdimstart`/`@dstdimend`) — set the whole group or omit the toggle; (ii) info outlets follow a query/route pattern — send `get<param>` to inlet, place `route <selector>` on the outlet, never wire info outlets directly into arithmetic. Process correction: violated the existing CLAUDE.md "Partial Answers Are Not Consent" rule by acting on the nfaces fix when only the @usedstdim question had been answered — flagged for stricter adherence going forward.
- 2026-04-30: Fixed face-capture video-never-reaches-matrixset bug. Later session: threw out face-capture.maxpat and rebuilt from scratch after full doc verification. New patch: jit.grab → jit.matrix 4 char 320 240 → (jit.rgb2luma → cv.jit.faces → gate control) → gate → jit.matrixset 300 4 char 320 240 → jit.world @floating 1 for playback. All depth-first ordering correct (resize→luma fires before resize→gate inlet 1; gate→trig_store/write_counter fires before gate→matrixset). face-capture-tutorial.js is now orphaned (tutorial not rebuilt). Root causes: (a) gate was receiving a bang instead of the video matrix — matrix from resize was never routed to gate or matrixset; (b) trig_store was `t b b`, so matrixset only received bangs; (c) counter max connected to inlet 3 (reset) instead of inlet 4 (set maximum); (d) jit.matrixset missing required positional args — needs `<matrixcount> <planecount> <type> <width> <height>` (e.g. `300 4 char 640 360`), not just `@matrixcount`; (e) jit.gl.render + jit.window replaced with jit.world @floating 1 (semi-deprecated). Fixes: resize routes to gate inlet 1; gate fires depth-first to trig_store (t b → counter → index) then directly to matrixset; counter max on inlet 4; matrixset with full format args; jit.world for display. Enshrined jit.world preference rule in MAX_PATCHING.md and SPEC_REFERENCE.md.
- 2026-04-30: Started Package Objects Library task. Built `build_package_objects.py` — extracts refpage XML metadata for installed Max packages by reusing `RefpageCache._parse` from `spec2maxpat.py`. Supports `--package <name>` (single-package extract) and `--merge package_objects.json` (preserves existing `use_when` values across re-extractions). Curated HISSTools Impulse Response Toolbox (HIRT) as v1 pilot — 24 objects, every entry has a written `use_when` describing the patching scenarios where it beats native chains. Added "Consult Installed Packages Before Long Native Chains" section to CLAUDE.md. Survey results: 28 user packages have refpages (~1700 objects total); ~10 well-known packages (cv.jit, Vsynth, ejies, modulo, Syphon, RNBO Synth Building Blocks, Max for the Visual Arts, catart-mubu) ship help files only and are deferred to a Phase 2 extractor.

- 2026-04-29: Refactored advisory hooks to a single source of truth. Tagged headings inline in CLAUDE.md with `{!pre-edit}` and `{!pre-commit}` markers (Rules from Corrected Errors, Verify External State, Reference Instances, Partial Answers, When Building a New Version, Never Regress, Working on existing patch — sync first, Spec files temporary, Keeping Docs in Sync, Work History). Replaced `pre_commit_review.py` and `pre_edit_review.py` with `hooks/inject_admonitions.py <event>` — one generic injector that greps tagged headings across `*.md`, extracts each tagged section (heading through next ≤-level heading), and surfaces them as `additionalContext`. Same gates as before: pre-edit skips `/tmp/`+`/var/folders/`; pre-commit only fires when Bash command contains `git commit`. Adding a new admonition is now a one-tag edit, not a Python checklist edit; rules and at-action-point reminders are guaranteed-identical text. Updated `.claude/settings.json` to call the new injector. Tested all paths (pre-edit fires with full tagged content, /tmp skips, pre-commit non-commit skips, pre-commit on `git commit` fires).
