# Work History

- 2026-09-07: **Retired the cord-hiding rules on John's instruction, and rebuilt `four-voice-random-sounds.maxpat` against six new ones.** John's notes on the patch overturned a binding rule and set five conventions; he chose option A on the open question, so **both** hiding rules are gone, not just the cord one. **The reversal:** *Always Hide Plumbing Patchcords* and *Always Hide Redundant Message Boxes* are replaced by **Never Hide Patchcords or Boxes** — the presentation view already decides what the operator sees, `hidden` only acts on the patching view, and that view belongs to whoever is editing or learning the patch, so hiding there strips information from its only readership. Plumbing is kept off the operator's screen by omitting `presentation: 1`, full stop. Sole exception: the spec-embed codebox, which is storage, not graph. Written into `CLAUDE.md`, `SPEC_REFERENCE.md`, the MCP server's `load()` knowledge text, and the `c2m-design` skill. **The verifier inverted with it**: `rule_hidden_box_cords_hidden` and `rule_redundant_message_box` are replaced by one `rule_no_hidden_elements` emitting `hidden-cord` / `hidden-box`, with `obj-spec-embed` exempt; golden tests rewritten, 38/38 pass. **Five new conventions**, all generalized past their instances and confirmed before writing: prefer an object's own attribute over an adapter chain; use the UI object where a plain object does the same job (`button`, not `[t b]` — it blinks and can be clicked); route signal with `s~`/`r~` where a cord would cross the patch, keeping short local connections as cords; write `s`/`r`/`s~`/`r~`; and lay fan-out destinations out right-to-left so the first-firing destination sits furthest right. **Two facts verified against C74 sources, both load-bearing.** (1) `random`'s `@range` takes TWO values and its high value is **exclusive** — `random.maxhelp` says "for an int range it is one less than the high value" and labels its own `random @range 25 50` demo "between 25 and 49". So the substitution needed `+ 1` on both broadcasts, and the `scale 0 999 4000 8000` it replaced was itself off by one at the top. Recorded in `patching/MAX_PATCHING.md` with the general lesson: replacing an adapter chain with an attribute means re-verifying the endpoints, not assuming they carry over. (2) Multiple `send~` sharing a name **sum** into the matching `receive~` (send~ refpage, verbatim), which is what makes the mix bus one `s~` pair per voice with no summing objects. **The rebuild**: 104 boxes / 78 lines, from 100/83 — four `scale` and four `+ 1` gone, twelve per-voice receives down to eight, ten audio cords replaced by eight `s~` and two `r~`, and `[t i i]`'s destinations swapped so `s RUN` sits right of the `select 0` stop-cleanup (which also happens to be the order that matters: clocks stop before the playlists are silenced). Zero hidden cords, one hidden box (the spec embed). `convert` and `gate_maxpat_file` both clean at 0/0/0; patching view audited at 0 overlaps within 1320×1000, presentation at 0 overlaps within 1176×649 with the operator's 42-object surface unchanged. **Visual verification in Max still NOT done.** **Then John added a seventh rule — `join`/`unjoin` over `pack`/`pak`/`unpack`, with `join @triggers -1` in place of `pak`** — generalized to *Prefer the Object That States Its Behavior in an Attribute*: where two objects do the same job and one carries its behavior in an attribute while the other encodes it in its name, take the attribute, because an attribute is readable in the box, greppable, runtime-settable, and checkable against the refpage. Verified: the `join` refpage says `triggers` `-1` "will cause the object to trigger output for any inlet (all inlets will be 'hot')", and C74's own `join.maxhelp` ships `join 3 @triggers -1`. **Two substitution traps, both documented**: `unjoin <n>` has **n+1** outlets (the refpage arg "specifies the number of outlets in addition to the rightmost outlet, which is always present"), and `join <n>`'s arg is the inlet count, so unlike `pak 4000 8001` its slots start at 0 and any meaningful default has to move elsewhere. **This exposed a converter bug**: `guess_newobj_io` did `int(args[0])` for eight object families, so `join @triggers -1` — a valid object — crashed the convert with `ValueError`. Fixed generally rather than per-object: a `_positional_args()` helper strips `@name value…` runs before anything positional is counted, and `_int_arg()` falls back to the documented default instead of raising; `unjoin`'s outlet count corrected to n+1 at the same time. Logged as a second instance under the existing *Parsers Must Tolerate the Schema's Full Value Space* rule — only `join` had ever been exercised, so seven other families carried the same latent crash. **Two further notes from John, recorded but deliberately NOT applied** (he said not to rebuild the patch again): (1) leave enough vertical space under a box for its cords to read as cords — `s~ MIX_L`/`s~ MIX_R` at 14px under a 30px-tall `live.gain~` makes the stereo cords barely visible; ~30px minimum, matching the converter's own 55px auto-layout row pitch, with more for tall sources and converging cords. This one bites harder now that nothing may be hidden: spacing is the only tool left for a legible graph. (2) `join @triggers -1` can be a plain `join` when one upstream multi-outlet object feeds every inlet, because right-to-left output already loads the cold inlets before the hot one fires — the attribute is for inlets fed from genuinely separate sources. (3) a send/receive name is a **channel, not a wire** — repeat the sender under each source rather than stretching a cord to an existing one; the refpage states all same-named senders reach all same-named receivers, and C74's own `jit.anim.path.maxhelp` ships nine `send topath` boxes in one patch. The patch's `[select 0] → [0(` runs a ~590px cord to a far-off `[s SOUNDS]` that a local duplicate would replace. Caveat recorded with it: the refpage says reception order across two receivers is explicitly not deterministic, so duplicate freely but never lean on firing order. All three written into `patching/MAX_PATCHING.md` (with the `@triggers` scope and the channel-not-wire point also folded into the `CLAUDE.md` rules), and queued against the patch for its next build. Worth noting where the ordering guarantee comes from: right-to-left is a Max language rule that `trigger`'s refpage states outright, but `unjoin`'s and `unpack`'s own refpages never restate. **Bonus fix**: `rule_message_resolves` now treats a documented `anything` method as an authoritative wildcard — `button`, `send`, and `trigger` all document it, and both warnings on this patch were false positives of exactly that kind. Noted on queue item 11, whose ERROR promotion depends on how large that false-positive family is.

- 2026-08-21: **Built `patches/four-voice-random-sounds.maxpat`, and corrected three repo statements the build proved wrong.** The patch: four independent voices, each re-picking a random clip from its own `playlist~` every MIN..MAX ms (4000..8000 default). Per voice the clock is `receive RUN → [t i i] → [metro 4000] → [t b b]`, with `t b b`'s right outlet setting the *next* interval (`random 4001 → + 4000 → metro right inlet`) before the left outlet fires this tick's clip (`random 1 → + 1 → playlist~`); `metro`'s right inlet applies on the following bang, which is what makes a random-interval clock work. PLAY→0 runs `[t i i]` outlet 0 into `[select 0] → [0(` so a stop also silences whatever is mid-play rather than letting four sounds ring on. Shared parameters broadcast by `send`/`receive` (RUN, MINMS, RANDSPAN, NCLIPS, SOUNDS); the MIN/MAX window is turned into `random`'s span at the top level by `pak 8000 4000 → unpack 0 0 → - 4000 → + 1`, with `pak` carrying `[max, min]` so `unpack`'s right-to-left firing lands min in the cold inlet before max hits the hot one. Sound loading is one `opendialog → prepend append → send SOUNDS` so a single file chooser appends to all four playlists — a bare `append` broadcast would open four dialogs. 116 objects / 100 connections; `verify_spec`, `convert`, and `gate_maxpat_file` all report 0 errors and the single known `message-unverified` false positive (`clear` through a `send`, which the rule can't see past). Presentation audited programmatically: 43 presented boxes, no overlaps, all inside their panels and the 1240×900 window, every interactive control labelled. **Visual verification was NOT done — screen access to Max was declined.** **Three doc corrections (user-confirmed):** (1) `playlist~` is a **UI maxclass with 1 inlet and 5 outlets** (`signal, signal, signal, "", dictionary`), stereo by default, accepts dragged audio files, indexes clips from 1 with `0` = stop — `patching/MAX_PATCHING.md` had said `newobj`-style `outlets=3 (sig audio, sig position, int state)` and `SPEC_REFERENCE.md`'s commonly-wrong-outlet-count table listed 3 as correct with "assumed 5" as the mistake, i.e. exactly backwards. Verified against `msp-ref/playlist~.maxref.xml` plus 34 unanimous `playlist~` boxes in Max's shipped help patches. It is also absent from `MAXCLASS_DEFAULTS`/`UI_SIZES`, so specs must supply `inlets`/`outlets`/`outlettype`/`size`. (2) `MAX_PATCHING.md`'s comment-sizing rule said "set only `size[0]`" — `build_box` does `w, h = obj_spec["size"]`, so a one-element list raises `ValueError` and the convert fails. Now states the two-element requirement and treats height as nominal. (3) The same section's "Sync caveat" claimed `sync_spec` does not capture `size`; `reconcile_spec` has a "Patching size" block that does. Replaced with the residual gap that is actually live: for an object with no `UI_SIZES` default, sync only records `size` when live height ≠ 22, so a narrowed *one-line* comment loses its width. **Method note worth keeping:** the corpus cross-check (count real boxes of a maxclass across Max's shipped `.maxhelp` files and read their `numinlets`/`numoutlets`/`outlettype`) settled every I/O question here faster than reading prose, and is what caught the backwards table. **CORRECTION (2026-09-07):** the patch this entry describes was never saved to `patches/four-voice-random-sounds.maxpat`. The file on disk at the time of writing was a different, earlier build — 100 boxes / 83 connections, with `random 1000 → scale 0 999 4000 8000` for the interval and separate `send MINMS` / `send MAXMS` broadcasts, not the `random 4001 → + 4000` clock and `pak`/`unpack` window described above. Its embedded spec matched its boxes exactly, so nothing was lost to a Max-side edit; the described version simply never reached disk. The three doc corrections in this entry are unaffected — they were verified against refpages and the help-patch corpus, not against the patch. Counts and wiring above should be read as describing an artifact that does not exist.

- 2026-08-21: **Group A step 1 done — `extract_spec` now discriminates, so `convert` can no longer be fed a decoy spec.** The scan in `spec2maxpat.py` returned the first box containing `--- CLAUDE2MAX SPEC ---` with no id or maxclass check, so in `patches/4step-sequencer.maxpat` a stray `newobj` (`obj-22`) holding a 27-character stub outranked the real `obj-spec-embed` and the patch reported zero objects; converting from that stub would have erased all 24 boxes with nothing printed. Split into `_spec_embed_candidates()` (all marker-bearing boxes, file order), `_choose_spec_embed()` (canonical id → `text.codebox` maxclass → file order), and a rewritten `extract_spec(maxpat, stream=sys.stderr)` that names every losing candidate when more than one matches. **Two design calls**: (i) a corrupt *chosen* embed raises a new `SpecEmbedError` rather than falling through to a candidate that parses — falling through is precisely how the decoy wins, and the caller cannot tell a partial spec from a small one; (ii) `verify_patch_file` catches it, prints it, and re-verifies from the patch's own boxes (`mode="native-scopes"`), so the gate degrades loudly instead of crashing — 4step now checks clean at 0 errors from its boxes instead of vacuously clean from a 27-char stub. `extract` and `sync` CLI branches exit 1 and write nothing; confirmed byte-identical file after a refused sync. New `tests/test_spec_embed_discriminator.py` (8 cases: decoy-shadowing, maxclass fallback, multi-candidate report, silence on the ordinary case, no-embed → None, corrupt-raises, missing END marker, verify degradation). Regression: 38/38 verify goldens pass, 4/4 presentation-roundtrip tests pass, the four healthy patches still extract and verify in `embedded-spec` mode with unchanged counts. **Also found**: `add_tutorial.py` holds a second copy of `extract_spec` that discriminates correctly but swallows `JSONDecodeError` and returns `None`, so it reads a corrupt patch as "no spec" — queued onto item (c)/(d), with the note that two implementations of one scan is the real smell. Pitfall bullet in `patching/MAX_PATCHING.md` updated to past tense with the fix recorded. **Still pending in Group A**: (b) trace the `_SKIP_BOX_IDS` bypass that wrote the self-referential spec object, (c) repair `4step-sequencer.maxpat`, (d) verify rules for "more than one marker-bearing box" and "embed present but unparseable" — all three unchanged in scope.

- 2026-08-18: **MCP server installed + mcp<2.0.0 pin added.** Created `mcp_server/.venv`, installed dependencies, and registered the server with Claude Code at user scope. Discovered that pip resolved `mcp>=1.27.0` to 2.0.0 which dropped FastMCP (`mcp.server.fastmcp` no longer exists in 2.x), breaking server startup. Fixed by pinning `mcp>=1.27.0,<2.0.0` in `requirements.txt` and reinstalling to land on 1.29.0. Server now starts cleanly; registered without API key (assess() falls back to keyword routing).

- 2026-08-18: **Queue: removed the Repo-as-Total-Scope Enforcement System task; replaced it with "Rule enforcement moves into the MCP server — one library, thin callers."** John's call: don't build a second enforcement system alongside the MCP layer — have the MCP server own it. Verified first that nothing had been built toward the old task (no `ENFORCEMENT.md` / `VIOLATIONS.md` / `verify_repo_invariants.py` / `RULES.yaml` / `rule_index.py` on disk); the only artifacts were 5 textual cross-references, all removed (4 in `TASK_QUEUE.md` — the MCP task's "Pairs with…" sentences, the pairings bullet, the "Not folded" bullet + its emptied heading, the position note — and 1 in `mcp_server/DESIGN_DECISIONS.md` § (j)). `WORK_HISTORY.md`'s 2026-05-28 mention left intact as history. New entry generalizes the pattern `claude2max_verify` already proves (one library; convert + `verify_spec` + content-gate hook as callers) to the rest of the rule corpus: classify the 73 indexed rule sections by enforcement class, add post-hoc structural families (presentation-required, comment-label-per-control, hidden plumbing cords/redundant message boxes, inlet/outlet double-labeling, embedded-spec presence, spec-vs-patch count drift, debug-addition marking), add `verify_patch(path)` + a repo-wide CLI, replace the flat `PRE_EDIT_CHECKLIST.md` injection with a trigger-filtered `checklist(context)` tool, and reduce `hooks/*.py` to payload-parsing shims. Three constraints recorded in the entry: hooks import the shared library rather than calling MCP over the protocol (hooks are short-lived subprocesses; the server is stdio-bound and may not exist) — `claude2max_maxpat_content_gate.py` is the reference shim; no `RULES.yaml` (prose stays source of truth, the classification is an index over it); and no new check blocks convert until proven zero-false-positive on the existing `patches/` corpus, per the jsui/v8ui `declareattribute` regression. **Two corrections after measuring rather than assuming**: (a) 8 of the structural families the entry first listed as "to add" are already implemented in `rules.py` at `WARNING`/`STYLE` (convert blocks only on `ERROR`), so item 2 became promote-warn-to-block plus the two genuinely-absent families (embedded-spec presence, spec-vs-patch count drift); (b) work items reordered into dependency order after finding the original sequence would promote rules to blocking on the evidence of 5 in-repo patches — running the full rule set over all 5 `patches/*.maxpat` embedded specs yields exactly **1 warning** (`presentation-required` on `jit-grab-scale`), far too thin to calibrate against. New order: sweep CLI + `verify_patch` (the instrument) → validate against the 11,873-file corpus in `maxhelp/maxhelp_crawl_state.json` reusing `maxhelp/extract_observed_attrs.py`'s box-walk, producing a per-rule false-positive table (the measurement) → promote (the change it authorizes) → classify the 73 rule sections → `checklist(context)` tool → hooks-to-shims → conditional violations log. C74's shipped patches are the adversarial set: authored without knowledge of these conventions, so a rule flagging thousands of them measures our preference, not a defect. Soft prerequisite noted: the separate attribute-conflicts task (44 resolver errors across `patches/`, all the known no-op class) should clear first so the sweep has a clean baseline. **Rule enshrined (user-confirmed)**: folded a repo-tooling clause into `CLAUDE.md` § "Verify External State — Never Assert from Memory" — the repo's own converter/verifier/MCP-server/hooks count as state to verify, because they drift from the prose describing them; determine current behavior by importing and running them rather than inferring from docs. Includes a three-tier table distinguishing what is checkable how: repo Python tooling (run it), Max static facts (lookup_object / list_attributes / refpages / package library / observed-attrs corpus), Max runtime behavior (c2m.inspect with the user driving Max). The tier split exists because Max itself cannot be executed from a Claude session — an earlier draft of the rule said "measure by running it" without that scope and would have sent a future instance looking for a way to run Max. Immediately demonstrated: the task entry's "73 rule sections" figure was stale (75 at HEAD, recorded as 73 on 2026-06-21), corrected to ~75 with an instruction to count it live.

- 2026-08-18: **Completed "Review the attribute conflicts the anti-guessing gate flags" — all three parts answered by measurement; 44 occurrences cleaned across 4 patches.** *Part 1 (why only 4)*: measured the envelope instead of assuming it narrow — **1,196 of 1,798 attr occurrences (66.5%) are actually inspected** across `patches/*.maxpat`; the rest is 507 universal-box attrs (valid by construction), 80 attr-less boxes, 15 on `text.codebox` (exempt). **Zero** skipped for no-refpage, so the hypothesised blind spot doesn't bite on this corpus (no external-heavy patches). *Part 2*: the named key enabler is already installed — `_GateResolver.attrs_for()` already returns `refpage ∪ jbox ∪ help-corpus observed` (wired 2026-06-21), so the 4 survive the widened envelope, three independent negatives each. *Part 3*: all 4 confirmed invalid and all 4 are family-resemblance traps — each attr is real on a *different* class: `locked_bgcolor` only on `p` (826 corpus boxes; 0 of 12,920 `panel`), `tribordercolor` on `live.slider`/`live.gain~`/`live.dial` (0 of 13,097 `number`), `bgfillcolor` on `panel`/`umenu`/`message` (0 of 2,535 `multislider`), `contrast` only on `gridmeter~`. All 44 removals provably zero-visual-change: `locked_bgcolor` held integer `1` (never a color) on panels already setting bgcolor+bordercolor; `tribordercolor`/`bgfillcolor` duplicated `tricolor`/`bgcolor` at identical rgba. Cleaned boxes **and** embedded specs (44+44) surgically — no `convert`, since two patches make it unsafe — then verified every file semantically identical modulo the 4 attrs; gate now 0 errors on all 5 patches; 38/38 verify tests pass. **Root cause**: `_PRESERVE_ATTRS` in `spec2maxpat.py` documented `locked_bgcolor` under "panel chrome", reading as a validity claim; the list is class-agnostic and the attr is real on `p`, so the entry stays and the comment was corrected. Repeatable resolution procedure captured in the Done entry (incl. two schema traps hit this session: the embed lives in `box["code"]` not `box["text"]`, and the observed-attrs corpus nests classes under a top-level `objects` key). **New task queued** — `extract_spec` returns the first marker-bearing box with no discriminator, so a stray `newobj` stub (`obj-22`) shadows the real `obj-spec-embed` in `4step-sequencer.maxpat`; that patch's real embed is also corrupt (truncated at 4,403 chars by a self-referential `obj-spec-embed` spec object that `_SKIP_BOX_IDS` should have excluded). `convert` on it today would emit a near-empty patch and destroy 24 boxes — latent, silent, triggered by the next convert. **Two pitfalls enshrined (user-confirmed)** in `patching/MAX_PATCHING.md` § Common Pitfalls (23→25 bullets), both generalized from mistakes made during this cleanup: (1) *a field's location in a data structure is part of its API* — confirm where a value lives rather than inferring from a similar-looking sibling; names the two concrete `.maxpat` cases (the embedded spec is `box["code"]` on `text.codebox`, not `box["text"]`; the observed-attrs corpus nests classes under a top-level `objects` key) and gives the recognition signal — a structural edit reporting **zero** changes where you expected some is an unverified probe, not a clean result. Framed as the same silent-acceptance trap as guessing an attribute name, one level up. (2) *a scan returning the first match needs a discriminator whenever the collection can hold more than one* — grounded in `extract_spec()` taking the first marker-bearing box with no id/maxclass check, which lets `obj-22`'s 27-char stub shadow the real embed in `4step-sequencer.maxpat` and would make `convert` destroy all 24 boxes.

- 2026-06-27: **maxhelp prose-insight extraction pass 3 — core `help/jitter/` Jitter objects.** Read help patches for 14 Jitter objects: jit.matrix (creation/fill/type-conversion/planemap), jit.pwindow (mouse outlet, texture display, topmost note), jit.world (preferred over jit.gl.render, @fps, headless mode), jit.grab (open/close idiom, getvdevlist), jit.playlist (setclip, selection, done notification), jit.gl.videoplane + jit.gl.layer alias (blend/depth/aspect defaults), jit.gl.render (legacy pipeline for reference), jit.gl.gridshape (@shape values, OB3D attrs), jit.gl.texture (GPU hold, two-texture swap, jit.gl.slab output), jit.gl.sketch (command list, reset, drawobject), jit.op (operator table, pass operators, first reach), jit.expr (variables/constants/caching/comma escaping vs jit.op/jit.gen), jit.cellblock (cell/insert/refer/formatting msgs, note: help lives in help/max/), jit.noise + jit.3m, jit.rota (boundmode), jit.alphablend (planemap mask idiom), jit.chromakey. Two PROMOTION-CANDIDATEs identified — pending user confirmation.

- 2026-06-27: **maxhelp prose-insight extraction pass 2 — core `help/msp/` audio objects.** Read help patches for 16 MSP objects: buffer~ (fill/apply/normalize/sizeinsamps/peek~), groove~ (@loop/@timestretch/@followglobaltempo/replace), record~ (start/stop toggle idiom, sync outlet), wave~ (phasor~-driven wavetable), line~ (ramp idiom, multi-segment, @activeout, @maxpoints), adsr~ (poly~ usage, @retrigger, legato, target 0 broadcast), phasor~ (@syncupdate, @jitter/@limit, phase-reset inlet), saw~/tri~/rect~/noise~/rand~ (bandlimited trio idioms, duty cycle, rand~ as LFO), tapin~/tapout~ (min-delay = vector size, click-free change idiom, feedback), biquad~/filtergraph~ (canonical pair, cascade msg), lores~/svf~ (svf~ freq modes, 4 outlets, max cutoff = sr/4), gain~ (logarithmic scale, `scale`/`inc` messages), meter~/scope~/peakamp~/slide~. Wrote structured per-object notes to `maxhelp_insights.md` § "MSP audio objects". Two PROMOTION-CANDIDATEs identified — pending user confirmation.

- 2026-06-27: **maxhelp prose-insight extraction pass 1 — core `help/max/` foundational objects.** Read 20+ help patches; wrote structured per-object notes to `maxhelp_insights.md`. Promoted 4 PROMOTION-CANDIDATEs to reference docs (user-confirmed): gate-vs-switch clarification (MAX_PATCHING.md + CLAUDE.md table), `select @fuzzy` pitfall (MAX_PATCHING.md), `sprintf symout` pitfall (MAX_PATCHING.md), `qmetro = metro @defer 1` note (CLAUDE.md table). MAXHELP_CRAWL_LOG.md updated (pass 1 logged as done, MSP recommended as next chunk).

- 2026-06-21: **Made the anti-guessing gate authoritative on three new fronts — messages, hand-edits, fail-loud.** (1) **Message/method validation** — `rule_message_resolves` (WARNING) in `claude2max_verify`; `_GateResolver.messages_for(name)` exposes the refpage `<methodlist>`. Flags a `[message foo …]` selector absent from every checkable inlet-0 target (resolves, non-empty methodlist, not a passthrough/router/custom-script object). WARNING not ERROR by decision: methodlists are materially incomplete and there's no observed-messages backstop yet, so it's a suspicion not a block (promotion path: mine observed messages from help patches). Conservative firing → near-zero false positives (`chord`→metro flags, `chord`→kslider doesn't). Runs in all 3 entry points. (2) **Hand-edit hole closed** — `spec2maxpat.gate_maxpat_file(path)` reads a native `.maxpat`/`.maxhelp`/`.amxd`, transforms each patcher scope into the spec shape, and runs the SAME resolver rules via new `claude2max_verify.run_resolver_rules`/`verify_resolver_only` (one library → hand-edit gate flags exactly what convert flags). New `hooks/claude2max_maxpat_content_gate.py` (PostToolUse, Edit|Write) feeds invented names back as `additionalContext` (exit 0, non-blocking, bulletproof). Registered in repo `.claude/settings.json` AND `install_global.py`/`uninstall_global.py` (cross-project). Native specifics: skip spec-embed by id+content-marker, exempt codebox-family from attr checks (`code` is real-but-unlisted), carry message-box text. **Verified live**: writing a bad `.maxpat` via the Write tool triggered the hook, which fed `oscparse`+`bogusattr` back as a system reminder. (3) **Fail-loud** — `_gate_spec` now prints a `[verify] WARNING … DISABLED/DEGRADED` line when the verify lib is missing, the resolver is None, or it can't resolve a core object (`_GateResolver.healthy()` probes `metro`), instead of silently proceeding un-gated. Real-patch validation: `4step-sequencer` clean; drift/jit patches flag ONLY the genuine known no-ops (`locked_bgcolor` etc.) — no new false positives. 38/38 verify tests pass; convert still blocks `oscparse` and still builds on warnings. Docs: DESIGN_DECISIONS § (i) (the three fixes) + § (j) (suggestions for promoting principle checks — block presentation-required, unmissable summary, creation-arg arity, repo-wide invariant check); CLAUDE.md "Never Write API Names From Memory" updated (hand-edits now post-hoc gated). **Honest residual scope**: principle checks still warn-not-block; creation-arg semantics unchecked; message rule is a suspicion until an observed-messages corpus exists.

- 2026-06-21: **MCP server — mtime-based cache invalidation (README contract made real).** The README promised "read once + watch mtimes" but the code read-once with no invalidation, so the long-lived MCP server served the startup snapshot until restart — editing a binding-rule doc / insight file / `package_objects.json` mid-session had no effect. Added `_FileCache` (mtime-signature-gated lazy value) + `_mtime_sig()` in `mcp_server/server.py` and converted all six disk-backed caches: package library (`_pkg_raw`/`_pkg_idx`), object-name index (`_all_object_names`), gate resolver (`_resolver` — watches `package_objects.json` + `maxhelp_observed_attrs.json`), rule sections (`_rule_sections` — watches the 3 rule docs), pitfall corpus (`_pitfall_chunks` — watches MAX_PATCHING.md + forum/cookbook insights). One `stat` per source per call; rebuild only when an mtime changes; all `_pkg_raw()`/`_resolver()`/etc. call sites unchanged (wrapped). Max-install refpage XML deliberately not watched (doesn't change during patch work). Verified live: a watched-file mtime bump triggers an in-process rebuild (fresh list identity), rebuild-only-on-change proven with a call-counter, original mtimes restored; 38/38 verify tests pass; functional numbers unchanged (73 rules / 170 pitfalls / 2795 pkg objs / 3969 names). Docs: README caching paragraph now accurate; TASK_QUEUE marked done. With this, the MCP layer is feature-complete: 10 tools, gate-consistent attribute validity, live-refreshing caches.

- 2026-06-21: **MCP attribute tools unified with the convert gate — fixed a tool-vs-gate inconsistency + gained no-refpage-external verification.** Found and fixed a real bug: `lookup_attribute`/`list_attributes` were refpage-only, but the convert gate (which `verify_spec` invokes) validates against `refpage ∪ jbox ∪ observed-in-help`. So the same server reported universal jbox attrs (`hidden`, `presentation`, `textcolor`, …) **INVALID** via the query tools while convert accepted them — the query tools were *stricter than the gate*, the exact silent inconsistency the gate exists to remove. Fix: both tools now decide validity through the SAME cached `_GateResolver` as the gate. Added two clean public accessors to `_GateResolver` in `spec2maxpat.py` (`base_attrs()`, `observed_attrs(name)`) so the server labels provenance without reaching into internals; added `_resolver()` (cached) + `_jbox_attrs()` + `_classify_attr()` in `mcp_server/server.py`; `verify_spec` switched to the cached resolver too. `lookup_attribute.source` now ∈ `{c74-refpage, jbox-base, observed-in-help, no-refpage}`; `list_attributes` returns the full gate-accepted union partitioned into `refpage_attributes`/`base_attributes`/`observed_only` + `writable`. **New capability:** no-refpage externals (`bach.roll` 95 attrs, `imubu` 127) that were "CANNOT VERIFY" are now positively confirmable from the help corpus (positive-allowlist only — never used to flag-as-invalid on no-refpage objects). Verified: **0 tool-vs-gate disagreements** across live.gain~/toggle/metro/number/panel/multislider/jit.matrix incl. the 4 known silent-no-op attrs; `bgcolor`-on-`live.gain~` trap preserved; `hidden`/`presentation` now correctly VALID(jbox-base); 38/38 verify tests pass; convert still blocks `oscparse`; `attrs_for` unaffected. Docs: README tool surface, SMOKE_TEST_RESULTS § "Attribute Tools Unified", DESIGN_DECISIONS § (h) two new extension notes (help-corpus union + tool/gate sharing).

- 2026-06-21: **MCP server — original tool surface COMPLETED (2 final tools) + Haiku verification closed + stale-bookkeeping reconcile.** Three things. (1) **Closed the one verification still owed** — the live Haiku routing test for `assess()`. `ANTHROPIC_API_KEY` is present in the registered MCP env (`~/.claude.json`), so re-confirmed live through the running server: three representative tasks all returned `method:"llm"` and routed correctly (6-domain case → `core,jitter,gen,networking,m4l,msp`; "granulate a recorded sound" → `core,msp` with no `~` named; "fix the comment label alignment" → `core`-only). Found the test had *already* passed 2026-06-20 (SMOKE_TEST_RESULTS.md line 287) but the bookkeeping was never reconciled — line 323 of the same file plus three TASK_QUEUE markers still said "verification owed." Fixed all four. (2) **Built the last two "planned" tools** from the original task surface: `search_pitfalls(term)` and `lookup_rule(name_fragment)` in `mcp_server/server.py` — server now exposes **10 tools and the full original surface is complete**. Both deterministic (no LLM / no API-key dependency): exact/token search over a small curated corpus where fuzzy matching adds cost without recall. `search_pitfalls` searches `## Common Pitfalls` bullets + forum + cookbook insights (170 entries; whole-phrase +50 > token +5; honest curated-not-exhaustive no-match message pointing back at `lookup_object`/`list_attributes`). `lookup_rule` searches the 73 `## ` rule sections across CLAUDE.md/SPEC_REFERENCE.md/MAX_PATCHING.md (header match +100/+20-per-token outranks body +8/+1; returns full rule text, 1800-char cap with source pointer). New helpers: `_split_sections`, `_rule_sections`, `_split_blockquote_entries`, `_pitfall_chunks`, `_query_tokens`, `_truncate` (lazy-cached, matching the `_pkg_raw` idiom). Smoke-tested all cases pass (textedit trap, trigger-order, presentation/hide-plumbing/api-names rule lookups, no-match honesty). (3) **Docs**: README tool-surface ("planned"→live, "10 tools" note), server module docstring, SMOKE_TEST_RESULTS.md (new "Knowledge-Search Tools" section + Haiku-closure note), TASK_QUEUE.md (all stale "owed" markers reconciled; "Resume at — nothing owed on the original surface" with non-committed next-direction ideas). Loose end left for John's call: untracked `.claude/skills/comment/` (a non-Max global skill copied into the repo — per the C2M-distribution rule, John decides whether cloners get it) and 3 unpushed commits.

- 2026-06-21: **Rule: prefer a labeled message box over button+comment for one-shot actions.** `patching/MAX_PATCHING.md` § Presentation Design Principles — for operator actions (save/load/clear/recall) use a `message` box whose text is the label rather than a `button` + separate `comment`: one object does both jobs and self-documents. Route through `[t b]` if a bare bang is needed downstream; reserve `button` for blink-indicator monitoring. (From the Zendrum bank UI: save/load messages replaced button+comment pairs.)
- 2026-06-21: **jsui/v8ui correctness + verifier false-positive fix + top-to-bottom flow rule (from Zendrum zkeyboard work).** (1) **Verifier fix** — `mcp_server/claude2max_verify/rules.py`: `rule_attribute_resolves` now skips objects that load custom scripts (`_CUSTOM_ATTR_OBJECTS`: jsui, v8ui, js, v8, jspainter, mxj, mxj~, gen~, gen, jit.gen, jit.expr, jit.gl.pix, jit.gl.slab, rnbo~) — they `declareattribute`/`param` arbitrary names the refpage can't list, so attr-validation false-positived on every custom attr (e.g. `@dotsize` on v8ui) and BLOCKED convert. 38/38 verify tests still pass. (2) **SPEC_REFERENCE.md** jsui/v8ui section: custom scalar attrs declared in-script (object-form `declareattribute` + setter); verifier skips attr-checks for script objects; **colors set in JS (jsarguments + messages), NOT declareattribute** (no rgba attr form — ref `jsui_3ddial.js`); verified mgraphics/mouse API (`paint`, `set_source_rgba/rectangle/ellipse/move_to/line_to`, `mgraphics.size`, `onclick(x,y)`/`ondrag`, one-inlet+selector). (3) **patching/MAX_PATCHING.md** § Patching Layout: "Signal flow runs top-to-bottom — inputs above, outputs below" (feeder message box below its object → upward cord that reads as broken; from a help patch where messages under the widgets read as non-functional).
- 2026-06-21: **`.maxhelp` Corpus Crawl — mechanical attr-tally pass complete.** Built `maxhelp/` (mirrors the `c74-forum/`/`cookbook/`/`c74-projects/` pattern): `extract_observed_attrs.py` (stdlib box-walk over all 3 corpus roots, recurses nested subpatchers, harvests top-level box keys + `saved_object_attributes`/`saved_attribute_attributes` nested keys minus a structural denylist), `MAXHELP_CRAWL_LOG.md`, `maxhelp_insights.md`. Ran over the **whole corpus in ~2.5s: 5,264 files scanned, 2 malformed-skipped, 306,478 boxes, 3,496 distinct objects** → `maxhelp_observed_attrs.json` (`object → {boxes, attrs:{name:count}}`, keyed exactly as the resolver looks objects up) + `maxhelp_crawl_state.json` (per-file status). Cross-checked against `spec2maxpat.build_resolver` (`refpage ∪ jbox`): **323 refpage objects carry 829 real gap-attrs** the union should fill (`comment/linecount`, `message`+`umenu` `bgfillcolor_*`, `flonum/format`, `inlet`+`outlet` `index`, `bpatcher/viewvisibility`, `panel/proportion`, `js/filename`, …); **2,527 objects have no enumerable refpage** (observed-attrs is their sole attr ground truth — incl. high-vocab externals `imubu` 127 attrs, `bach.roll` 239 attrs). Independently **confirmed the 4 gate-flagged production attrs are silent no-ops** — `panel/locked_bgcolor`, `number/tribordercolor`, `multislider/contrast`, `multislider/bgfillcolor` appear on **zero** of 5,175/6,316/1,537 boxes corpus-wide. Logged 4 resolver-integration cautions (alias-resolve gap in `attrs_for()`; filter `^rnbo`/`^frozen` export artifacts; ≥3-box frequency floor; positive-allowlist-only for no-refpage objects). Prose-insight extraction queued for next session (start: core `help/max/`).
- 2026-06-21: **`.maxhelp` crawl — extended past help patches to all patcher kinds + a Max JS API census.** Generalized `extract_observed_attrs.py` to scan `.maxpat` (abstractions/bpatchers/examples) and `.amxd` (M4L devices, via `ampf`-header strip + `raw_decode`) alongside `.maxhelp`; `strict=False` JSON parsing recovers files carrying raw control chars in strings (50→7 failures). Patcher corpus 5,264→**11,866 files / 797,407 boxes / 4,201 objects**; refpage-gap objects 323→**498** (1,155 attrs), no-refpage 2,527→**3,180**, and the 4 silent-no-op confirmations now rest on ~13K boxes/class. New `extract_js_api.py` + `maxhelp_js_api.json`: a **data-driven** Max JS API census over 8,811 `.js`/`.mjs` (477 in-process js/v8/jsui + 20 Node-for-Max classified; 8,314 RNBO/lib/web excluded) — ranks constructors (`JitterObject`/`Task`/`Dict`/`JitterMatrix`/`Buffer`/`Global`/`MGraphics`…), lifecycle handlers (`bang`/`paint`/`onclick`/`msg_int`/`setvalueof`/`getvalueof`/`jit_matrix`/`onidle`…), globals (`outlet` 384, `inlet` 141, `post`, `declareattribute`), declarations (`outlets`/`inlets`/`autowatch`). Names extracted structurally, nothing from memory — promotion candidate for `JITTER_JS_PATCHING.md` + `SPEC_REFERENCE.md` v8 section. Crawl log + insights updated.
- 2026-06-21: **Three enshrined rules + two queued tasks (from Zendrum_Player review).** (1) `patching/MAX_PATCHING.md` § Common Pitfalls + `SPEC_REFERENCE.md` `size` row + `PRE_EDIT_CHECKLIST.md`: **preserve each object's default box size in the patching view** — only set a patching-view `size` for content that needs it (wide message/comment, clipped routing text); resizing for layout belongs in `presentation_rect`. Arbitrary patching-view resizing buries controls (kslider instance in Zendrum_Player). (2) `CLAUDE.md` § "Building Reusable Objects — Generalize, and Mirror the Vocabulary You Inherit" + `SPEC_REFERENCE.md` v8 pointer: when writing a `v8`/`js`/`jsui` (or any non-patch-specific) object — generalize it; if based on an existing object keep it backwards-compatible with all its messages/attrs; if inheriting functionality reuse that object's message/attr names (flag conflicts for the user); if hybridizing two objects import the base's full vocabulary + as much of the second's as possible, flagging conflicts. Grounded in `zkeyboard` (kslider+multislider `[v8ui]` hybrid). (3) Queued (Opus): **`.maxhelp` Corpus Crawl** (~5,266 help patches → canonical-usage corpus + a `maxclass → observed-attrs` map to union into the anti-guessing resolver) and **Review the attribute conflicts the gate flags** (why only 4, how to widen the detection envelope via the .maxhelp observed-attrs map, how to resolve). The 4 flagged silent-no-op attrs in the drift-sequencer patches remain to be cleaned (tracked in the new task).

- 2026-06-21: **Anti-guessing convert gate — make Claude unable to ship invented Max names.** Addressed the standing problem that even with full knowledge loaded, Claude builds objects/attributes from memory and Max silently accepts the guesses (red box / ignored attr), failing out-of-session. Diagnosis: epistemic, not informational — the only robust fix is to reject guesses at the one chokepoint every patch passes through (`convert`). Extended `claude2max_verify` with resolver-gated anti-guessing rules: `rule_object_resolves` (newobj name must resolve to a C74 refpage / installed package / abstraction on disk — ERROR), `rule_attribute_resolves` (attribute must be in the object's complete valid set — ERROR), `rule_maxclass_resolves` (WARNING). `run_all(spec, resolver=...)` threads an injected resolver (keeps the lib dependency-free). `spec2maxpat.build_resolver()` wraps the existing `RefpageCache` + `PackageObjectsCache` + on-disk abstraction check + the jbox base-attr set; `_gate_spec()` makes `convert` **block** (exit 1, nothing written) on any error unless `--allow-unverified`. Per-object `"unverified": true` downgrades to a style note (explicit, auditable); abstraction-on-disk auto-OKs. The MCP `verify_spec` tool builds the same resolver so it gives the authoritative answer pre-convert. **Key finding (load-bearing): attribute validity = the object's own refpage attrs ∪ the jbox base-class attrs.** Object refpages list only object-specific attrs; universal box attrs (textcolor, background, hidden, varname, presentation, …) are inherited from `jbox` and live once in `jbox.maxref.xml`, never re-listed — they ARE knowable (corrected an earlier wrong assumption that they weren't, which had led to a needless `live.*`-only scoping). Unioning jbox cut false positives from ~90/patch to 0 while still catching the canonical `bgcolor`-on-`live.gain~` trap, and made attribute checking general (all objects). The 4 attrs still flagged on production patches (`panel/locked_bgcolor`, `number/tribordercolor`, `multislider/contrast`, `multislider/bgfillcolor`) are absent from refpage AND jbox AND every shipped C74 patch on that class — real silent-no-op attrs a prior session introduced, i.e. the gate working. Object resolution handles operator aliases (via `add_tutorial.REFPAGE_ALIAS`) + verified word-aliases (`t→trigger, sel→select, s→send, r→receive, b→bangbang, del→delay, j→join` in `_VERIFIED_WORD_ALIASES`) so real patches don't false-positive on `t i i` / `+ 1`. Validation: `oscparse` and `bgcolor`-on-`live.gain~` blocked; aliases resolve; 11 new resolver tests (deterministic FakeResolver with jbox base union) → 38/38 verify tests pass. Added an "Operating stance — you do not know Max from memory" preamble to the `core` knowledge module naming the gate consequence. Docs: `DESIGN_DECISIONS.md` § (h), README verify_spec entry + gate note, `PROJECT_CLAUDE_SNIPPET.md` "Before converting". **Follow-up owed:** the 4 flagged silent-no-op attrs in the production patches should be cleaned (they currently make `convert` block those patches).

- 2026-06-21: **kslider correctness — converter fix + reference notes (from the Zendrum Player JS port).** Empirically confirmed (refpages + shipped Max help + a v8 sanity patch) and enshrined toolkit-wide so no project re-derives them. (1) `spec2maxpat.py`: added `kslider` to `MAXCLASS_DEFAULTS` as 2 in / 2 out (`outlettype ["",""]`) — it was absent, so `type:"kslider"` specs silently fell to the 1/1 fallback (the refpage `<misc name="Output">` lists a single entry, so a refpage-derived count under-resolves it; velocity fires out the right outlet first, note out the left). Verified a no-override kslider spec now converts to 2/2. (2) `SPEC_REFERENCE.md`: kslider row in "Outlet counts commonly wrong" + new "kslider — display messages, key range, mode" subsection (`chord <n> <v>…` = the only multi-note display, replaces held + no echo; `set <n>` = single value, no output; `clear`/`flush`; bare `int` displays AND outputs; `mode` 0/1/2; `offset` = low MIDI key default 36 — flagged "octaves vs low-MIDI-key" contradiction for in-Max confirmation; `range` default 48; number/kslider `set` = display-without-output). (3) `patching/MAX_PATCHING.md` Common Pitfalls: the spec `name` field auto-emits a title comment at `[50,15]` that overlaps a top-of-patch comment — omit `name` or place the first comment below the title band.

- 2026-06-21: **MCP Phase (iii) — verify_spec() + shared rule library.** Built `mcp_server/claude2max_verify/` — a dependency-free Python package (no `mcp`, no `spec2maxpat` imports) that statically checks a Claude2Max spec against the binding rules. Hand-coded (not markdown-parsed) per open-decision #1: 12 rules across three severities — `error` (connection refs to missing objects, malformed connection shape, out-of-range outlet/inlet indices), `warning` (no presentation view despite UI controls, presented controls with no labels, visible cords touching hidden boxes, unhidden formatter message boxes, unlabelled subpatcher inlets/outlets, untracked/in-presentation debug scaffolding, empty newobj), `style` (non-ALL-CAPS user names, `[js]` over `[v8]`). Each `Violation` carries a `source` pointing back at the doc section it enforces. Package layout: `rules.py` (Violation dataclass + SpecContext + rule fns + REGISTRY), `verify.py` (`verify_spec`/`verify_spec_json`/`format_report` + stable result-dict shape `{ok, counts, violations, summary}`), `__init__.py` (re-exports). **Shared by two consumers**: (a) new MCP tool `verify_spec(spec_json)` in `server.py` (server now exposes 8 tools — adds `report` to the result for pasteable text); (b) `spec2maxpat.py convert` via a guarded optional import — surfaces `[verify]` findings to **stderr** (stdout stays pure .maxpat JSON), non-blocking (still builds; hard errors still raised by `convert_patcher`), `--no-verify` opts out. Tuned for zero false positives: I/O-index check fires only on explicitly-declared `inlets`/`outlets` (never guesses); redundant-message-box fires only when *every* inbound source is an interactive control; presentation check exempts pure-DSP patches. 27 golden tests (`mcp_server/tests/test_verify.py`, stdlib-only runner) all pass. Real-patch validation: `drift-sequencer-{lcars,soviet}` + `drift-sequencer` (109–118 objects) report **0 violations**; `jit-grab-scale` correctly flags its genuinely-missing presentation view (4 interactive controls, no presentation). Existing `tests/test_presentation_roundtrip.py` still passes. Docs updated: server docstring + README tool-surface (regrouped by phase: knowledge / verification / safety-net) + `DESIGN_DECISIONS.md` § (g) (decisions: hand-coded rules, shared library, severity contract, non-blocking convert integration, conservatism over coverage) + phasing-renumber note + `PROJECT_CLAUDE_SNIPPET.md` (new "Before converting" step). TASK_QUEUE Phase (iii) marked done; resume at Phase (iv+): global skill + PreToolUse enforcement hook + installer.

- 2026-06-20: **MCP Phase (ii) — assess() + load() module system.** Rethought architecture after smoke test review: reactive tool-call pattern is insufficient — Claude needs Max knowledge front-loaded before patching, not queried mid-build. Implemented `assess(task_description)` (keyword-based domain detection → returns domain list) and `load(domains)` (assembles and returns full knowledge for those domains from existing Claude2Max files). Seven modules: `core` (binding rules + Common Pitfalls + re-evaluation signal, always loaded), `gen` (GEN_PATCHING.md), `jitter` (JIT_GEN + JITTER_JS), `m4l` (M4L_PATCHING.md), `networking` (inline multi-user-template summary), `msp` (inline audio guidance), `spec` (SPEC_REFERENCE.md). Re-evaluation pathway built into core module: explicit recognition signals tell Claude when to call `load()` again if task scope grows. `essentials()` kept as backward-compat alias for `load(["core"])`. Smoke test: `assess("step sequencer + audio + jitter")` → `["core","jitter","msp"]`; `load(["core"])` = 20K chars; `load(["core","msp","jitter"])` = 40K chars. `PROJECT_CLAUDE_SNIPPET.md` updated to two-call pattern. TASK_QUEUE.md updated: Phase (ii) complete, Phase (iii) is `verify_spec()`.

- 2026-06-20: **Doc corrections from smoke test review.** Four rules added/corrected after John reviewed the Phase (i) test patch: (1) `MAX_PATCHING.md` — buttons used as pass-through indicators are inline on the cord, not parallel side-taps; (2) `MAX_PATCHING.md` — some objects configure range via creation args not `@`-attributes (`counter 0 3`, `makenote 100 250`, `metro 500`); (3) `M4L_PATCHING.md` — new section clarifying that `live.*` UI objects (`live.step`, `live.dial`, etc.) and `itable` are NOT M4L-exclusive — they work in any Max patch; only the LOM access objects and `.amxd` packaging are genuinely M4L-only; (4) `CLAUDE.md` preferred-objects table — added "Step sequencer UI" row covering `live.step` (via `next` message) and `itable` (graphical table editor), verified via refpages before writing. Also: all `live.step` behavior claims verified against `live.step.maxref.xml` before writing (caught an error — original draft said "wire inline", corrected to "send `next` message").

- 2026-06-20: **MCP server Phase (i) Step 5 — end-to-end real-world test. Phase (i) complete.** Called all five tools in a live session while building a 4-step MIDI sequencer from scratch using only MCP-verified names: 9 objects verified via `lookup_object` (metro, counter, makenote, noteout, toggle, multislider, button, select, number), 2 attribute sets verified via `list_attributes` (multislider → confirmed `@size`/`@setminmax`/`@slidercolor`; counter → confirmed range is a creation arg, NOT `@min`/`@max`), 1 package search (no match → native objects correct). Patch spec written, converted via spec2maxpat.py, verified: 24 boxes, 18 patchlines, 16 presented objects, `openinpresentation: 1`, all binding rules followed. Two issues caught and fixed same session: (1) `search_packages` returned silent empty list on no-match — return type changed from `list` to `dict` with explicit `count` and `message` fields. (2) `essentials()` did not warn that `counter`/`makenote`/`metro` use creation args for range/defaults, not `@attr` — clarifying note added to the "before writing any attribute" section. New files written: `mcp_server/SMOKE_TEST_RESULTS.md` (full test log), `mcp_server/PROJECT_CLAUDE_SNIPPET.md` (template CLAUDE.md snippet for student projects). Module docstring updated to reflect Phase (i) complete status. TASK_QUEUE.md in-progress note updated. **Phase (i) foundation is done. Next: Phase (ii) — `verify_spec()` + shared rule library (Opus task).**

- 2026-06-13: **MCP server Phase (i) Step 4 — `lookup_attribute()` + `list_attributes()` tools.** Added two tools to `mcp_server/server.py`. `lookup_attribute(object_name, attr)` calls `_attrs_for()` (a helper that wraps `RefpageCache.lookup()` and returns the `attributes` dict already parsed by `_parse()`) and returns `{valid, value_type, size, default, readable, writable, inspector_label, valid_values, source, summary}`. `list_attributes(object_name)` returns `{found, source, count, attributes, writable, summary}`. Key discovery: Max refpage XML carries `enumvals` entries in inner `<attributelist>` elements but the `value` attribute is always `None` — valid enum strings are not stored in refpage XML and cannot be extracted. The `label` inner-attribute is the Inspector UI display name ("Cold Signal Color"), not a list of valid values. `valid_values` is therefore `[]` in v1 with a docstring note explaining the limitation; `inspector_label` exposes the display string separately. Also added `_attrs_for()` helper to deduplicate the "get attrs dict + source label" lookup used by both tools. Smoke tests: all 5 assertions pass (`bgcolor` invalid, `coldcolor` valid/float/size=4, `list_attributes` 32 attrs/coldcolor present/bgcolor absent, package-only object → CANNOT VERIFY, unknown object → OBJECT NOT FOUND). README tool-surface table updated.

- 2026-06-13: **MCP server Phase (i) Step 3 — `lookup_object()` + `search_packages()` tools.** Extended `mcp_server/server.py` with two new tools. `lookup_object(name)` imports `RefpageCache` from `spec2maxpat.py` (reusing the same XML-backed C74 refpage lookup the converter uses), falls through to a lazily-built flat index of `packages/package_objects.json` for installed package externals, and returns `{found, source, numinlets, numoutlets, outlettype, digest, use_when, deprecated_by, summary}`. `search_packages(term, limit=5)` scores the package library by use_when > digest > name substring match and returns ranked results. Updated `_ESSENTIALS_MD` to reference these tools instead of the old "run this shell command" fallbacks. All three smoke tests from the task queue passed: `lookup_object("metro")` → c74-refpage; `lookup_object("oscparse")` → unknown/not-found (the canonical silent-failure example); `lookup_object("cv.jit.faces")` → package. Server now exposes three tools: `essentials`, `lookup_object`, `search_packages`. TASK_QUEUE.md updated: Step 3 complete, resume at Step 4 (`lookup_attribute()` + `list_attributes()`).

- 2026-06-13: **MCP server Phase (i) Step 2 — server skeleton + `essentials()` tool.** Created `mcp_server/server.py` (FastMCP stdio server, one tool: `essentials()` returning ~1.4K-token structured markdown summary of all binding rules), `mcp_server/__init__.py`, `mcp_server/README.md` (venv install + `claude mcp add` instructions + tool surface table for all planned phases). Added `mcp_server/.venv/` to `.gitignore`. Platform discovery: Homebrew Python enforces PEP 668 and refuses system-wide `pip install`; created `mcp_server/.venv/` with `mcp[cli]>=1.27.0` (installs FastMCP, starlette, uvicorn, httpx, pydantic). Server verified: FastMCP instantiates cleanly, `essentials` tool registers, `essentials()` returns 6937 chars / 1065 words. Registered at user scope via `claude mcp add --scope user claude2max -- <venv>/python3 <repo>/mcp_server/server.py` → written to `~/.claude.json`. Smoke test (calling the tool from a fresh non-Claude2Max session) requires a separate Claude restart — deferred to next session startup. TASK_QUEUE.md updated: Step 2 complete, resume at Step 3 (`lookup_object()`).

- 2026-06-05: **Sonnet preparatory tasks — spec2maxpat.py presentation_rect round-trip fix + plugin/skills polish pass.** Closed two Sonnet `[pending]` tasks. (1) `spec2maxpat.py`: `reconcile_spec` now folds live `presentation_rect` (canonical 4-element form) and live `patching_rect` size back into the embedded spec, so manual resizes/moves in Max survive `sync → extract → convert`. `build_box` accepts `presentation_rect` as a sibling spec field (winning over `presentation` when both are present), making hand-edited specs forgiving when the author writes the obvious field name. `has_presentation` and `openinpresentation` triggered by either field. `_box_to_spec_obj` was already emitting the canonical 4-element list — bug 3 in the task body was stale. Added `tests/test_presentation_roundtrip.py` (4 assertions: sibling field, live presentation_rect captured on sync, live patching size captured on sync, end-to-end convert→resize→sync→extract→convert preserves rect). Smoke-tested against `patches/drift-sequencer-lcars.maxpat` — 38 presented boxes round-tripped clean, 0 orphan `presentation_rect` siblings. `SPEC_REFERENCE.md` Object Fields table now documents both forms. (2) Plugin/skills polish: trimmed `c2m-sync` (70→58), `c2m-design` (85→66), `c2m-tutorial` (93→91 — only one duplicated block to remove; rest is operational guidance) by replacing duplicated CLAUDE.md rule prose with section pointers. `c2m-explain` (99), `c2m-inspect` (93), `c2m-package-search` (67) left at their lengths — they're skill-specific operational content (script output shape, OSC kind/cap tables, search workflow), not rule duplication. Added in-repo-vs-UPSTREAM-SKILL distinguishing paragraph to CLAUDE.md § "Plugin / Slash Commands". Added `.claude/skills/README.md` discovery index. Subtask 5 (`/c2m-refpage` skill) stays struck — superseded by the pending MCP `lookup_object` Opus task.

- 2026-06-04: **Promotion-candidate review pass — all 35 forum flags cleared, 9 new entries written to target docs.** Swept `c74-forum/forum_insights.md` (35 `[PROMOTION-CANDIDATE]` flags) and `cookbook/cookbook_insights.md` (all already promoted in prior sessions). Confirmed 24 stale flags on entries already written to target docs; removed all via Python batch script. Promoted 9 genuinely-pending entries with user confirmation: `patching/MAX_PATCHING.md` gained phasor→delta→<~0 edge detector, smoothed preset interpolation via function→buffer→play~, mass-spring-damper jit.phys→audio, uzi→peek~ buffer scan, cpuclock interval timing, and PBR full transparency stack (gamma+skybox+environment). `patching/GEN_PATCHING.md` gained codebox intro definition, multi-parameter buffer-as-config-table, and Lorenz/ODE integrator template. `CLAUDE.md` gained "Always Use Absolute Paths in Bash Commands" rule. Confirmed M4L LOM chain and getpath/deferlow already in `patching/M4L_PATCHING.md`; cookbook items already in `patching/MAX_PATCHING.md`. Task marked complete and moved to Done in TASK_QUEUE.

- 2026-05-22: **Document local-folder-only workflow + add suggested student workflow.** Added four sections to `CLAUDE.md` — "Local-Folder Use Is Fully Supported — GitHub Is Optional" (no GH remote required for students), "Suggested Student Workflow" inside the existing Workflow section (9-step Description→Plan→Instructor-Review→First-Draft loop), gated "New User Setup" with `git rev-parse --is-inside-work-tree` so local-only students skip the branch flow and get a different greeting, and "State-File Location for External Projects" (WORK_HISTORY/TASK_QUEUE/insights live with the external project, not with the toolkit). Added "Two Operating Modes" section to `README.md` (Mode A: patches inside repo, Mode B: Claude2Max as a tool with patches in an external folder via absolute `-o` paths) plus an optional-only note on the `diff.claude2max.textconv` setup step. Then a trim-pass over `CLAUDE.md` to soften phrasing that implied GitHub was mandatory — the natural-commit-moment cadence now conditions push on a configured remote, the "Before every commit/push" rule reads "Before every commit (and before every push, when a remote is configured)", the New User Setup greeting splits on `git remote` presence, and the `insights.md` template renames `[PR candidate]` → `[shareable]` with the end-of-session PR-reminder gated on a remote existing. Queued a Sonnet TASK_QUEUE entry to refine the setup process once tested against a real first-time student — open questions: onboarding script, persistent external-project pointer, instructor-handoff helpers, step-skip conditions, local-only greeting tone, and enforcement of the state-file-location convention.

- 2026-05-08: **Sharpen "Flag Natural Commit Moments" — never tell user to `/clear`.** Per user: "don't prompt to commit and clear — prompt to commit and start a new session — old sessions are useful to keep for reference. Enshrine." Added a paragraph to the rule explicitly distinguishing "start a new session" from "clear the current one": old transcripts capture the path to a decision (what was tried, what was rejected, the back-and-forth) which is often more valuable than the diff alone; clearing destroys that history while a new session preserves it. Replaced the example phrasing ("you can clear and pick up the next chunk cold" → "this is a good place to start a new session for the next chunk") and updated the follow-up sub-bullets accordingly. Behavioral change: the new-session prompt is the only correct prompt; never suggest `/clear`.

- 2026-05-06: **Forum Knowledge Crawl — pass-5 (local) — 548 threads processed, 19 insights written, all 10 PROMOTION-CANDIDATEs promoted.** Per user "do another 500 forum posts". Ran `--diff --download` on Gen pp.16-25, Jitter pp.23-32, MaxMSP pp.25-34, Javascript pp.20-27, RNBO pp.13-19, Max For Live pp.16-22, Misc pp.13-18. 204 artifact-bearing + 344 zero-artifact = 548 NEW threads (above the 500 target). 180 artifact files saved to `/tmp/forum-pass5/`. WebFetch hit-rate 19/20 = 95%. Pass-5 was Jitter-heavy by sample composition (12/19 insights are Jitter), reflecting both the volume of Jitter threads in the candidate ranges and the surface area for non-obvious GL-pipeline gotchas: texture-as-attribute persistence after patchcord delete; normalised-coordinate ±2 visible window range (not ±1, not pixels); fixed-function vs shader-pipeline migration via `jit.gl.material`; `jit.glue` plane-count match requirement; Bayer-matrix stochastic transparency; equirect→cubemap via `jit.gl.pix` (not `jit.gen`+`fpic`). Same-pass closing sweep promoted all 10 candidates: 6 Jitter rules to `patching/MAX_PATCHING.md` Jitter notes, 1 audio rule (`slide~` vs `rampsmooth~` for `groove~`), 2 patching patterns (jit.cellblock-emits-on-selection-loop, bucket/zl.reg as event-state-shift), 1 gen codebox compile-bug + History workaround to `patching/GEN_PATCHING.md`, 1 M4L rule (live.menu enum changes via `_parameter_range` while keeping count constant) to `patching/M4L_PATCHING.md`. State JSON now totals 1708 entries (726 downloaded / 976 skipped-no-artifact / 6 scraped).

- 2026-05-05c: **Forum Knowledge Crawl — pass-4 (local) — 248 threads processed, 14 insights written, all 5 PROMOTION-CANDIDATEs promoted.** Per user "do another 250, promote all, push and sync with origin". Ran `--diff --download` on Gen pp.9-15, Jitter pp.19-22, MaxMSP pp.21-24, Javascript pp.16-19, RNBO pp.11-12, Max For Live pp.13-15, Misc pp.11-12. 110 artifact-bearing + 138 zero-artifact = 248 NEW threads at the 250 target. 72 artifact files saved to `/tmp/forum-pass4/`. WebFetch hit-rate 15/16 = 94%. 14 insights written across 6 sections of `forum_insights.md` — Gen alone yielded 6 (its dominant artifact density holds across passes). 5 PROMOTION-CANDIDATEs immediately swept into reference docs in the same session: leaky accumulator + circular-buffer click + envelope-data-as-coords + N-band crossover phase preservation → `patching/GEN_PATCHING.md`; `recallmulti` weighted preset interpolation → `patching/MAX_PATCHING.md`. State JSON now totals 1177 entries (522 downloaded / 649 skipped-no-artifact / 6 scraped). Closing-sweep cadence (promote in same pass) replaced the previous batched-sweep cadence; this is now the normal pattern.

- 2026-05-05b: **Forum Knowledge Crawl — pass-3 (local) — 617 threads processed, 22 insights written.** Per user "crawl 500 more threads now; periodic status updates" after the remote scheduled run was WAF-blocked. Ran the cheap `--diff` enumerator across 8 subforums (MaxMSP pp.6-20, Jitter pp.8-18, RNBO pp.6-10, Javascript pp.8-15, Gen pp.5-8, Misc pp.4-10, Max For Live pp.3-12, Java pp.1-5) — surfaced 617 NEW/UPDATED threads. Then `--download` pass saved 183 artifact files to `/tmp/forum-pass3/` and marked 265 threads `downloaded`; companion script marked the remaining 352 zero-artifact threads `skipped-no-artifact`. Selective WebFetch on the 24 highest-artifact-richness threads (mct + attach scoring) yielded 22 usable insights at 92% hit rate. Insights distributed across 10 sections of `c74-forum/forum_insights.md` (Audio MSP × 2, Jitter × 4, JS/v8 × 2, MIDI × 1, UI × 1, Performance × 1, Patching Patterns × 1, Gen / gen~ × 6, M4L × 1, RNBO × 2). 9 fresh PROMOTION-CANDIDATE flags applied. State JSON now totals 933 threads (was 321) — 412 downloaded / 515 skipped-no-artifact / 6 scraped. FORUM_CRAWL_LOG.md updated with pass-3 Coverage row + post-pass-3 Resume point (Gen pp.9+, Jitter pp.19+, MaxMSP pp.21+ etc.); Java deprioritised (43 threads → 2 artifact-bearing, very low density). Architectural finding: Gen subforum has the highest insight-per-artifact density of any subforum — 23 of 37 NEW threads were artifact-bearing (62%). Use as prioritisation guide for future passes.

- 2026-05-05: scheduled forum crawl pass-3 attempt (remote) — all candidate subforums returned HTTP 403 `x-deny-reason: host_not_allowed` from Cloudflare WAF on the Anthropic cloud env; both direct urllib and WebFetch blocked; 0 threads processed, 0 insights written. Routine disabled and the pass continued locally (see 2026-05-05b above).

- 2026-05-04c: **Folderized patching docs and c2m_explain.** Per user "folderize all 'insights' files" + "also put c2m_explain.py in the c2m-explain folder". (a) Created `patching/`; `git mv` moved MAX_PATCHING.md, GEN_PATCHING.md, JIT_GEN_PATCHING.md, M4L_PATCHING.md → there. Updated 13 external files (CLAUDE.md, README.md, PRE_EDIT_CHECKLIST.md, TASK_QUEUE.md, WORK_HISTORY.md, three insights files, three log/state files, .claude/skills/c2m-design/SKILL.md, hooks/inject_admonitions.py) to prefix references with `patching/`. Within patching/, sibling refs stay as bare names per the repo's `forum/`-style convention; root-doc refs (SPEC_REFERENCE.md, CLAUDE.md) also stay bare for prose readability. The admonition hook scans only repo-root *.md and only CLAUDE.md / PRE_EDIT_CHECKLIST.md carry tags, so the move doesn't break any pre-edit / pre-commit injections. (b) `git mv c2m_explain.py c2m-explain/c2m_explain.py`. Added a parent-directory sys.path injection at the top of the script (`sys.path.insert(0, str(Path(__file__).resolve().parent.parent))`) so `import add_tutorial` and `import spec2maxpat` continue to resolve regardless of cwd. Updated invocation paths in `.claude/skills/c2m-explain/SKILL.md`, `c2m-explain/UPSTREAM-SKILL.md`, README.md, TASK_QUEUE.md. Smoke-tested `python3 c2m-explain/c2m_explain.py --help` from the repo root — imports succeed, argparse output unchanged.

- 2026-05-04b: **Doc separation pass — split GEN_PATCHING into two flavors and broke M4L out into its own reference.** Per user "separate gen~ and jit.gen into two different insights files, separate max-for-live into its own insight file, then push." (a) `patching/GEN_PATCHING.md` narrowed to gen~ / gen (audio rate / control rate); cross-reference now points readers to the new sibling. (b) New `patching/JIT_GEN_PATCHING.md` covers jit.gen + jit.gl.pix (per-cell / per-pixel) — same gen language, but iteration is over matrix cells or pixels, not samples. Documents the position primitives (`norm`, `cell`, `dim`), texture sampling for shader-mode (`sample <input> <texcoord>`, `@rectangle 1`), the `history` divergence (per-frame in jit.gen, not per-cell — for per-cell persistence, use a feedback `jit.matrix` chain at patcher level), and three canonical idioms (norm→math→out 1, framewise history, distance-field rendering). (c) New `patching/M4L_PATCHING.md` consolidates Max for Live patterns: LOM access chain (`live.path → live.object → get → route`), `live.thisdevice` init-complete bang and self-id, `getpath` race needing `deferlow`, Push 3 polyphonic pressure via `Control_Surfaces` LOM path (not standard MIDI), `live.*` UI styling on light themes, `parameter_enable` non-injection rule (cross-references converter design rule), `.amxd` packaging notes. CLAUDE.md updated with three pre-work-read directives (one per new file) and three Key Files entries. Forum insights file's "Max for Live" section retained as source-of-record; patching/M4L_PATCHING.md is the canonical reference.

- 2026-05-04: **patching/GEN_PATCHING.md created + 20 promotion-candidates promoted across MAX_PATCHING / SPEC_REFERENCE + textbook-port refs scoped down + forum pass-2 (240 threads, 103 artifact-bearing, ~30 new insights, +5 PROMOTION-CANDIDATEs).** Per user request "make a separate GEN_PATCHING insights doc for gen; promote all candidates; fully analyze another 250 forum threads including MCT and attachments." **patching/GEN_PATCHING.md** at repo root: gen~ programming model section, canonical idioms (slide envelope follower, samplerate→ms idiom, equal-power vs linear crossfade with selector 2), latency-compensation principle, cross-references. CLAUDE.md updated to require patching/GEN_PATCHING.md before any work inside `gen~` / `gen` / `jit.gen`. **Promotion sweep**: 20 [PROMOTION-CANDIDATE] flags across `c74-forum/forum_insights.md` (10), `cookbook/cookbook_insights.md` (6), and `c74-projects/c74_projects_insights.md` (4) marked `[PROMOTED 2026-05-04]`; canonical statements written into patching/MAX_PATCHING.md (8 new bullets + new "Jitter / GL knowledge" subsection + new "Shipping a Community Max Package" section), SPEC_REFERENCE.md (4 new entries: patcherargs, copymatrixtoarray + Array.sort, ESM in node.script, JXS texture-binding by `<param>` order), and patching/GEN_PATCHING.md (the 3 transient_crossfade idioms inline). **Textbook-port cleanup** (per same-session user decision "interested in native maxmsp only"): three georgeNikmus repos (Andy Farnell *Designing Sound* port, Miller Puckette *Theory and Technique* port, Linux4Max binaries) removed from priority lists in c74_projects_insights.md / database / TASK_QUEUE — they require the `Pd4Max` shim or wrap Linux plugin binaries, not native Max idiom. Catalog rows kept for completeness. The earlier "Curtis Roads" attribution on Theory and Technique was incorrect (book is by Miller Puckette); flagged for the audit trail. **Forum pass 2** (full chunk per "fully analyze another 250"): enumerated 243 unique threads across MaxMSP pp.2-5 + Jitter pp.4-7 + RNBO pp.2-5 + Javascript pp.4-7 + Gen pp.1-4 (NEW subforum, entirely unscanned previously) + Misc pp.1-3 + Max For Live pp.1-2 = 240 NEW + 3 already-in-state. Per-thread artifact detection: 103 artifact-bearing (43% yield); downloaded 212 files (167 .maxpat, 18 .zip, 1 .js, 1 .jxs, 1 .rnbopack, 24 other) to /tmp/forum-pass2/. No Unicode-slug failures encountered in the forum corpus this pass; the same fix is already in `c74-projects/enumerate_c74_projects.py` from the prior session in case the forum enumerator hits one later. Read 25+ patches in detail (including the entire chaos-library, TB-303 filter gen~, varispeed-overdubbing looper gen~, oscbank-in-genexpr, Lorenz/Henon-Heiles attractors, transparency PBR example, mass-spring-damper jit.phys chain, more-than-one-parameter jsui, smoothed-preset-interpolation function→buffer→play~, dirigera-max layout). Wrote ~30 new entries to `c74-forum/forum_insights.md` across new "Gen / gen~" section (10 entries — slide as envelope follower, history-with-init-value, samplerate/twopi/dim() identifiers, peek @boundmode/@interp, fixnan, codebox, mc.gen~ param targeting, multi-param buffer-as-config-table, buffer copy glitch, ODE integrator template, ladder filter template) plus Audio (6: phasor→delta→<~0 edge detector, function setdomain, smoothed preset via play~ buffer, comb~ resonator, mass-spring-damper jit.phys, speedlim vs qlim), Video/Jitter (7: PBR transparency stack, jit.gl.cornerpin from pwindow, point-cloud rendering, PLY import, databending raw bytes, Windows jit.world frame drops, particle-trail feedback), JS/v8 (3: pattr+pattrstorage for jsui params, this.box.setattr at init, anim.node worldpos timing pitfall), MIDI (1: bendin → div 127 → list.sum → pak f f → rnbo~), RNBO (4: list.* primitive vocabulary, cpuclock interval timing, sample-dependency export with @file, web-export bundling), Max for Live (4: live.path → live.object LOM chain, getpath needs deferlow, live.thisdevice init+self-ref, Push 3 polyphonic pressure via Control_Surfaces). 5 new [PROMOTION-CANDIDATE] flags planted on the most actionable findings for the next promotion sweep. Updated `forum_crawl_state.json` (now 321 entries: 147 downloaded, 168 skipped-no-artifact, 6 phase-C-scraped) and `c74-forum/FORUM_CRAWL_LOG.md` Coverage row. Three structural findings reinforced from the larger sample: (a) gen~ is a fundamentally different language and warrants its own reference doc (now created); (b) RNBO list primitives don't overlap with Max's `zl` family — namespace is meaningful; (c) the M4L LOM chain is the load-bearing pattern for half a dozen distinct M4L tasks and deserves canonical treatment in patching/MAX_PATCHING.md.

- 2026-05-03c: **C74 Projects Crawl — pages 12-21 + author-site triage + 3 patches analyzed (210 total surveyed, 37 free-extractable).** Continued the crawl per user request "do 1, 2, 4". Sequence: option 2 (forward to pages 12-21) → option 4 (triage author-site bucket) → option 1 (analysis pass). **Pages 12-21**: 100 more projects fetched; older era (2021-2023) yields **30 raw free-external** (10 GitHub + 2 m4l) — ~5× the recent-tier rate. Fixed Unicode-slug bug in `enumerate_c74_projects.py` (`urllib.parse.quote(slug, safe='/-')` for em-dash / accented Latin / Spanish ñ in slugs); 9 failed slugs re-fetched. **Author-site triage** (95 projects across 62 unique domains): refined classifier (gum.co, youtu.be, bandcamp, drive.google, dropbox, mega, /download/ paths, leading-whitespace strip) caught easy cases; lexical scan of ~70 author-site URLs for buy-vs-free keywords gave verdicts on most; manual academic-domain overrides (`metacreation.net`, `cosmolab.faselunare.com`, `wkleinsasser.net`, `julienbayle.net/works/`, etc.) caught the academic-free pattern the lexical heuristic misses. **Final 210-project status**: 37 free-extractable (15 GitHub + 4 m4l + 2 fileshare + 1 download-page + 15 author-free), 118 commercial, 49 showcase, 6 still-ambiguous. **Era split confirmed**: pages 1-11 (2024-26) → ~11% free; pages 12-21 (2021-23) → ~25% free; older era 5× more open-source. Plausible explanation: gallery's recent tier is dominated by paid M4L launches (gumroad/ko-fi/vstopia/simple-machines) as those stores matured, whereas the earlier era posted more academic/hobbyist source-released work. **Database rewritten** with: (a) Observations section maintained per-session with 8 named findings (uniform Misc topic, no inline patches, era split, recent-only metadata, author concentration where Tilman Ehrhorn alone has 17 projects, topic clusters dominated by sequencers/spectral/stutter, archive-quality finds, bounded ambiguous residual); (b) Free-extractable candidates section organized by source; (c) compact 210-row catalog by year cohort. **Three patches analyzed in depth**: (i) **transient_crossfade** (jbaylies, MIT, gen~) — envelope-driven transient/tonal split using `slide` as gen~ envelope follower, equal-power vs linear crossfade selectable via `selector 2` + `param interpolate_crossfades`, per-input sample-accurate predelay via `samplerate * (predelay/1000) → delay 48000` with the latency-compensation principle that the env input must connect to the source as directly as possible (3 PROMOTION-CANDIDATEs for `patching/MAX_PATCHING.md` § gen~); (ii) **!SHAKE!** (Vinczius) — parent/child abstraction with 8 audio outlets in the child for voice multiplexing, sub-Hz `cycle~` (0.11, 0.31 Hz) as canonical slow LFO, parameterized message-box presets using `$1-$4` substitution as a pattrstorage-free preset strategy (1 PROMOTION-CANDIDATE on sub-Hz cycle~); (iii) **Dirigera Max** (maceq687, MIT) — exemplar of properly-shipped community Max package layout (`package-info.json` + `patchers/` + `javascript/` + `help/` + `docs/<obj>.maxref.xml` + `extras/` + `icon.png`); refpage XML follows the canonical `<c74object>` schema, so a properly-built community package is I/O-resolvable in Claude2Max specs without overrides (1 PROMOTION-CANDIDATE for `patching/MAX_PATCHING.md` § "Shipping a community package"). **31 candidates queued** for future analysis in `c74_projects_insights.md` "Future-analysis queue" section. Top priorities: Facet (mjcella, ★★, live-coding), sonus (★★, C++ algorithmic composition), DGMD (★★, ML training-data), SwiftRNBO (★★, Swift↔RNBO), the 4 free MaxForLive devices, and the academic author-site set. **Three georgeNikmus repos** (Andy Farnell *Designing Sound* port, Miller Puckette *Theory and Technique of Electronic Music* port, Linux4Max plugin binaries) initially flagged as ★★★ priorities were excluded per 2026-05-04 user decision — the textbook ports require the `Pd4Max` shim (Pd patches running through a translation layer, not native-Max designs) and Linux4Max wraps Linux plugin binaries rather than teaching Max idiom; native-Max only for now. The earlier "Curtis Roads" attribution on the *Theory and Technique* book was incorrect — that book is by Miller Puckette, not Roads — flagged here for the audit trail since the original 2026-05-03c entry contained the error.

- 2026-05-03b: **C74 Projects Crawl — pages 2-11 added (110 total surveyed).** Continued the crawl per user request ("look at the next 100"). Crawled pages 2-11 with `--detect-content` (~110 HTTPs across 10 listing pages + 100 project bodies). Reclassified all 110 by URL pattern: **7 free-external** (5 GitHub: `!SHAKE!`, `Dataset Generator for Musical Devices`, `Dirigera Max`, `RNBO Preset Backup`, `sonus`; 2 MaxForLive.com: `Modulus`, `PAULA 2.0`), **59 commercial** (gumroad, ko-fi, vstopia, simple-machines, structure-void, h-4.digital, ...), **34 author-site** (mixed; some clearly commercial product pages with trailing-path gumroad URLs the matcher missed, some likely free academic/personal sites like `metacreation.net`, `valeriorlandini.github.io`, `novelmusic.org`), **10 showcase** (no URL, or YouTube/Vimeo only), **0 inline patches**. Rewrote `c74-projects/c74_projects_database.md` as a compact year-cohort table (110 rows organized by 2026/2025/2024/2023; H4-per-project shape didn't scale past ~10 entries). Free-extractable candidates section calls out the 7 confirmed for next analysis pass. Updated `C74_PROJECTS_CRAWL_LOG.md` Coverage row, regenerated `c74_projects_crawl_state.json` (110 entries with `decision` ∈ {enumerated-free-external, enumerated-commercial, enumerated-author-site, enumerated-showcase}). Updated TASK_QUEUE entry with the survey state and four next-session options (analysis on the 7 free-external; continue forward; sample-deep at pages 50/100/150; triage author-site bucket). **Three structural findings reinforced**: (a) `forum_topic` is uniformly "Misc" gallery-wide; (b) inline `patcher`/`attachment` ProseMirror nodes are essentially unused (forum-style embedding doesn't apply); (c) older pages have no `project_keywords`/`website_url` because those are recent platform metadata fields that weren't backfilled. **Strategic stance**: gallery's primary value is *catalog* (answering "is there an existing M4L device that does X?") not *insight extraction*. Free-yield rate is ~6% in the recent tier (7/110); whether that holds or rises in older tiers is the open empirical question. No insights written to `c74_projects_insights.md` yet — that requires fetching the 7 free repos.

- 2026-05-03: **Queue review + C74 Projects Crawl bootstrap (first 10 projects).** Reviewed `TASK_QUEUE.md` end-to-end with the user; queue hygiene pass: moved `Borrow MaxMCP's plugin/skills surfacing pattern` and `Cross-link /c2m-explain from CLAUDE.md` to the Done section (both completed 2026-05-01); merged `Test claude2max-design skill` and `Layout engine Phase 3 — screenshot verification` into a single Opus task **Return to claude2max-design skill — extend design sense + Phase 3 screenshot verification on current patches** (the c2m-design skill is fundamentally incomplete — no generalized design sense, no principle extraction from samples, no implementation of articulated rules; merging keeps the design loop end-to-end with screenshot verification on current patches: drift-sequencer-{lcars,soviet}, jit-grab-scale, ensemble-sequencer-v5, lcars). Updated Forum Knowledge Crawl entry with the actual 2026-05-01e state (81 threads scraped / 32 insights / 13 PROMOTION-CANDIDATEs). Bumped cookbook per-session target from 12 → 20 recipes; noted state-machine `scraped` flag drift to fix inline. Updated polish-pass subtask 1 to reflect that queue hygiene was handled during this review. **C74 Projects Crawl bootstrap**: created `c74-projects/` (parallel to `c74-forum/` and `cookbook/`); built `enumerate_c74_projects.py` (Next.js JSON-driven, no HTML scraping needed since `pageProps.postData.results[]` carries clean per-project metadata); wrote `C74_PROJECTS_CRAWL_LOG.md`, `c74_projects_crawl_state.json` (10 entries from page 1), `c74_projects_database.md` (catalog with all 10), and `c74_projects_insights.md` (skeleton — 0 insights yet). Added c74-projects entry to CLAUDE.md "Knowledge Resources — Consult Before Designing". **Findings from page 1**: 1822 total projects across 183 pages of 10. (a) **`forum_topic` is uniformly "Misc" across the entire gallery** (sampled pages 1, 50, 100, 150, 180 — 100% Misc) — not a useful organizing axis; catalog must group by year cohort, inferred theme, or commercial-vs-showcase status. (b) **0 inline patches** across all 10 page-1 projects — `patcher` and `attachment` ProseMirror nodes are essentially unused on the projects gallery, very different from the forum (where MCT blocks are inline). Patches live behind external URLs: GitHub, MaxForLive.com, gumroad, vstopia, vendor product pages. (c) **Page-1 commercial skew**: 7 of 10 link to paid M4L stores (vstopia, structure-void, gumroad, simple-machines, h-4.digital), 3 are showcase-only with no external URL (Arpeggio, Synesthesia, Nostalgia Panner). **0 free-extractable patches from the first 10**. (d) **Metadata is recent-only**: `project_keywords` and `website_url` populate ~half of recent posts but are absent from older pages (50, 100, 150, 180) — the metadata fields appear to be a recent platform addition that wasn't backfilled. **Strategic implication**: the projects gallery's primary value is *catalog* (answering "is there an existing M4L device that does X?"), not *insight extraction* like the forum/cookbook crawls. Next session should either continue pages 2-3 to confirm the commercial-skew pattern or jump to pages 50/100/150 to test whether older non-commercial projects yield more inline patches.
- 2026-05-02: **Repo sync + cleanup pass.** Untracked `__pycache__/spec2maxpat.cpython-314.pyc` and added `__pycache__/` + `*.pyc` to `.gitignore` (build artifact, shouldn't be in repo). Removed leftover Plan-agent worktree `claude/jovial-wozniak-26a2d7` (clean, on same commit as main). Verified deletions match WORK_HISTORY entries: `face-capture.maxpat` + `face-capture-tutorial.js` (rebuilt as `jit-grab-scale` per 2026-04-30), `patches/drift-sequencer-soviet.js` (converted to `.maxpat` per 2026-05-01), `hooks/pre_commit_review.py` (replaced by `hooks/inject_admonitions.py` per 2026-04-29). Replaced CLAUDE.md "Flag Natural Clearing Points" rule with **"Flag Natural Commit Moments — Proactively Suggest Commit, Sync, and New Session"** — same set of preconditions, but the suggestion is now to commit + push + clear, not just clear. Default cadence: commit early, sync often, restart frequently. User-confirmed wording before edit per the propose-before-enshrining rule. TASK_QUEUE.md cookbook entry rewritten to make actual progress clear: 23 of 735 viable recipes analyzed (~3%), state-machine `scraped` flag is unused (insights extracted outside the tracked status), most of the work still ahead. Bundled the accumulated session work — hook refactor, knowledge-base folders (c74-forum, cookbook, packages), skills system (.claude/skills, c2m-explain, c2m-themes), patch updates, doc pass — into five logical commits and pushed to `origin/main`. Deleted stray `origin/temp/one-sound-a-day` transport branch (done its job; would regress codebase if merged since it diverged from a much earlier base).

- 2026-05-01l: **Plugin/skills + CLAUDE.md compression.** Built four user-invocable slash commands in `.claude/skills/`: `/c2m-design`, `/c2m-package-search`, `/c2m-tutorial`, `/c2m-sync` — each with a SKILL.md carrying frontmatter (`name`, `description`, `argument-hint`) and a step-by-step body. Compressed `CLAUDE.md` from 609 → 354 lines (42% reduction) by moving reference content to homes where it's already accessed: v8/JS objects, What the Converter Handles, Modifying Externally-Sourced Patches, and MCT encoding algorithm → `SPEC_REFERENCE.md` (already in session-start read list); package extractor/curation prose → new `packages/CURATION.md`. Added `## Plugin / Slash Commands` table listing all five c2m skills. CLAUDE.md now contains only rules, principles, and workflow — no how-to reference material that duplicates the skill bodies or other docs. TASK_QUEUE.md updated: "Borrow MaxMCP" and "Cross-link /c2m-explain" marked complete.

- 2026-05-01k: **Max Cookbook Analysis — second focused chunk: Jitter matrix manipulation (12 recipes).** Cluster picked via keyword filter on `cookbook_crawl_state.json` (`{'jit.op','jit.pack','jit.unpack','jit.spill','jit.rota','jit.alphablend','jit.xfade','jit.matrix','jit.scissors','jit.glue','jit.dimmap','jit.repos'}` — 29 candidates surfaced; 12 picked for breadth across plane manipulation, transformation, compositing, submatrix extraction, math, and matrix-as-data-buffer). Recipes: `combine-2-matrices`, `submatrices-and-masking`, `bidirectional-jitmatrix-scroll`, `rotate-zoom-and-offset-video-using-jitrota`, `alpha-masking`, `reveal-sections-image-alphablend`, `crossfade-and-blur-movie-files-using-jitxfade-and-jitmatrix`, `get-column-jitmatrix`, `split-screen-video`, `adjust-brightness-and-contrast-1-plane-char-matrix`, `using-jitter-store-numerical-data`, `stretch-1d-matrix-2d-matrix`. All 12 downloaded to `/tmp/cookbook/<slug>/` and read in full via the box+lines dump approach (559 lines for the full set). Wrote 12 insight entries to `cookbook/cookbook_insights.md` Video/Jitter section, 3 flagged `[PROMOTION-CANDIDATE]`. Notable: (a) **named `jit.matrix NAME` boxes share memory** — the matrix analog of `send`/`receive` and the pv/v variable family; (b) **`@planemap N0 N1 N2 N3`** for arbitrary plane reassignment without per-pixel logic; (c) **`@dstdim*`/`@srcdim*` on `jit.matrix`** for sub-rect extraction/placement without `jit.submatrix`/`jit.scissors`; (d) **`jit.rota @boundmode`** must be set explicitly — the default `0=ignore` produces visual smearing; (e) **cheap blur via downsample-then-upsample-with-interp** (`jit.matrix 4 char 16 12 → jit.window @interp 1`) instead of Gaussian/convolution; (f) **`jit.scissors`/`jit.glue`** auto-create R×C outlets/inlets — important for spec generation; (g) **`jit.matrix` as general-purpose multi-dim data buffer** (not video-only) — 18 xyz × 1800 frames in one box; (h) **brightness/contrast math** `jit.brcosa` does internally — `jit.op @op -/*/+` chain or `jit.expr @inputs 3 @expr in[1]+in[2]*(in[0]-in[1])` with `jit.3m` mean; (i) **`jit.window @floating 1`** and **`jit.qt.movie @unique 1`** are silent-bug attributes most patches forget. Updated `cookbook/COOKBOOK_CRAWL_LOG.md` Coverage row + Resume point (dropped Jitter-first-chunk suggestion; added Jitter GL/3D and Jitter video-sources/feedback as next options). New PROMOTION-CANDIDATEs (3) added to the consolidated promotion-review TASK_QUEUE entry: named `jit.matrix` shared-memory pattern → `patching/MAX_PATCHING.md` data-storage; cheap downsample-blur idiom → `patching/MAX_PATCHING.md` video-patterns; `jit.matrix` as general data buffer → `patching/MAX_PATCHING.md` data-storage. No rules enshrined; awaiting user confirmation.

- 2026-05-01j: Bookkeeping pass following the cookbook chunk (2026-05-01h). Resumed and completed the `cookbook/` corpus scan (was 325/741 mid-session; now 735/741 scanned + 5 skipped + 1 404 on slug `204` — possibly a bad URL on the cookbook itself). Final chapter distribution captured in `cookbook/COOKBOOK_CRAWL_LOG.md` Resume point: Max=350, MSP=263, Jitter=118, Max for Live=7, MIDI=1 (most MIDI-using recipes are tagged Max or MSP — search by keyword instead of chapter for those). Rewrote the Resume point with concrete next-chunk suggestions (Jitter as a fully-scanned adjacent set; MSP buffer/loop, MSP synthesis, Max sequencer, MIDI keyword search) and the Python-on-state-file snippet pattern for filtering. Added a new TASK_QUEUE entry **"Promotion-candidate review pass"** that consolidates all `[PROMOTION-CANDIDATE]` flags from `forum/forum_insights.md` (13 carry-over from 2026-05-01e) and `cookbook/cookbook_insights.md` (2 from 2026-05-01h: dB-mapped slider with `select 0` short-circuit + `gain~`, and the cookbook poly~-ready dual `inlet`/`out~` template that confirms the existing CLAUDE.md rule), plus the **cwd/relative-path lesson** proposed in chat as a third pending CLAUDE.md candidate. The task documents the user-confirmation gate (per CLAUDE.md "Rules from Corrected Errors") and includes the `grep` enumeration recipe so future sessions can sweep all candidates uniformly. No rules enshrined; nothing in repo-root *.md changed pending user confirmation.

- 2026-05-01i: Shipped `/c2m-explain` — a one-shot patch-walkthrough skill — and integrated it bidirectionally with the tutorial builder. **New files**: `c2m-explain/c2m_explain.py` at repo root (read-only walkthrough generator with `--summary` mode), `.claude/skills/c2m-explain/SKILL.md` (the local-discovery manifest in the location Claude Code's auto-discovery scans, parallel to the four sibling c2m skills), and `c2m-explain/UPSTREAM-SKILL.md` (the upstream-distribution copy). **Direction A integration** (shared description cascade): factored `REFPAGE_ALIAS`, `_load_package_index`, `package_lookup`, and `describe_object` from c2m_explain into `add_tutorial.py`. The cascade is now: curated `OBJ_DESCRIPTIONS` → C74 refpage digest (with `REFPAGE_ALIAS` mapping symbolic operators `+/-/*/%/==/!=/</>/<=/>=/&&/||` and their `~` MSP variants to alphabetic spellings — every alias verified against `<C74>/docs/refpages/<domain>/<spelling>.maxref.xml`) → installed-package digest from `packages/package_objects.json`. `add_tutorial.build_step_description` now uses this cascade, so tutorial step descriptions get refpage + package digests for every object instead of just the ~50 hand-curated entries. The explain skill imports the same `describe_object` from add_tutorial. Lazy-imports `spec2maxpat.REFPAGE_CACHE` to avoid circular import. **Direction B integration** (tutorial-aware overlay): explain skill detects an embedded tutorial (`v8 <name>-tutorial.js` controller box + companion JS file containing `var STEPS = [...]`), parses the JS literal via regex, and overlays the tutorial's authored step names + descriptions onto the walkthrough. The safety check is strict — `len(tutorial_steps) == len(generated_clusters)` — so a drifted tutorial (boxes added/removed since baking) silently falls back to auto-generated text rather than misattributing descriptions. Header note appears when the overlay is in effect. Tested on jit-grab-scale (6 = 6, overlay applies cleanly), drift-sequencer-soviet and drift-sequencer-lcars (10 ≠ 17, correctly falls back). Other display fixes: `panel`/`fpic` filtered out of per-stage object lists into a footnote ("plus N decoration objects"), stages with only decoration omitted entirely, presentation-view label finder uses `presentation_rect` adjacency first then falls back to `patching_rect` (catches labels positioned in presentation layout that patching-layout adjacency misses), bracketless display for non-newobj boxes (text wins for messages, maxclass for toggles/jit.fpsgui/etc.). **Caught by another instance**: the initial skill manifest was placed only at `c2m-explain/UPSTREAM-SKILL.md`, but Claude Code's auto-discovery scans `.claude/skills/` — fixed by adding the local SKILL.md at the discoverable location. The four sibling skills (`c2m-design`, `c2m-package-search`, `c2m-sync`, `c2m-tutorial`) already followed this pattern and `c2m-tutorial` even said "use /c2m-explain instead, when available" — anticipated. Updated all c2m-explain references in the repo: TASK_QUEUE.md (line 87 cross-reference, marked task complete and moved to Done, added follow-up task to cross-link from CLAUDE.md after the bigger Opus plugin task lands), c2m-tutorial/SKILL.md (dropped "when available"), c2m-explain/UPSTREAM-SKILL.md (fixed stale `c2m_explain.REFPAGE_ALIAS` reference to `add_tutorial.REFPAGE_ALIAS` after the refactor, added cross-link to local manifest, documented tutorial overlay behaviour). No new rules enshrined this session — the work was entirely tooling.

- 2026-05-01h: **Max Cookbook Analysis — first session.** Bootstrapped the `cookbook/` knowledge folder (parallel to `forum/`): built `enumerate_cookbook_recipes.py` (Drupal-markup parser for the Cookbook's recipe pages — extracts chapter, keywords, attachments, body), `cookbook_crawl_state.json` (status per recipe: pending → scanned → downloaded → scraped/skipped), `COOKBOOK_CRAWL_LOG.md` (session recipe matching `forum/`), `cookbook_insights.md` (topic-organized, same shape as `forum_insights.md`). Bootstrapped the state with all 741 alphabetical recipes; ran `--scan-meta` across the corpus (interrupted at ~325/741 mid-session for the focused chunk; can resume any time — it's idempotent). **First focused chunk**: 11 MSP envelope/amplitude recipes (`ab-audio-crossfade`, `adjusting-audio-amplitude`, `adsr-filter-envelope`, `alter-phase-sinusoid-milliseconds`, `amplitude-and-decibels`, `amplitude-inversely-proportional-distance`, `arpeggiate-harmonic-series`, `audio-amplitude-control`, `calculating-doppler-shift-moving-virtual-sound-sources`, `subpatch-suitable-use-poly`, `variation-simple-envelope-follower`) downloaded to `/tmp/cookbook/<slug>/` and read in full via the dump-every-box-and-line approach. Wrote 16 insight entries: 11 in Audio (MSP), 5 in Patching Patterns, 3 flagged `[PROMOTION-CANDIDATE]`. Notable: (a) `number~ @mode 1 @ramptime 10` is a one-object replacement for `pack value 10 → line~`; (b) `!/`, `!-`, `!*~`, `!-~`, `!/~` are the "reverse" arithmetic family for putting a constant on the LEFT of a binary op; (c) `sig~ N` is required when feeding a constant into signal-rate math (silent-failure trap otherwise); (d) `adstatus sr` reads current samplerate as int — replaces hardcoded 44100; (e) `tapin~`/`tapout~` for variable-time delay (delay length on `tapin~`, read offset on `tapout~`); (f) constant-power pan (`sqrt(az)` / `sqrt(1-az)`) preserves total power vs linear (which dips 3 dB at center); (g) `startwindow` (not `start`) on `dac~` for example patches; (h) the cookbook's poly~-ready abstraction template uses BOTH `inlet`/`outlet` AND `in N`/`out~ N` so the same patch works in either context — confirms the inlet-comment labeling rule already in CLAUDE.md is standard Max practice, not a Claude2Max stylistic preference. Ops note: the `pre-edit` and `pre-Read` hooks in `.claude/settings.json` were using relative `hooks/...` paths and broke the moment cwd shifted into `cookbook/` — required user intervention to convert to `$CLAUDE_PROJECT_DIR/...` (the documented Claude Code env var, verified via the official hooks docs). The cwd lesson is queued in chat as a proposed CLAUDE.md rule pending user confirmation.

- 2026-05-01g: Field-survey session comparing Claude2Max to other LLM-to-Max approaches. Researched five projects (MaxPyLang, MaxMCP, tiianhk MaxMSP-MCP, Patch Pal yeschat GPT, esling/maxmsp_ai) and crawled two Cycling '74 forum threads (`max-and-large-language-models-generative-ai-gpt-etc`, `claude-code-to-generate-max-patches`) via the existing forum framework. **Architecture matrix**: imperative-Python → .maxpat (MaxPyLang), live-MCP-mutation (MaxMCP, tiianhk), declarative-spec → .maxpat (Claude2Max). No project combines refpage-grounded I/O + curated package library + embedded-spec round-trip — those are Claude2Max's specific differentiators. Notable finding: Thread 1 Reply 30's pointer to `taylorbrook/MAX-MSP_CC_Framework` is the same source Claude2Max imported its original 1,694-object I/O database from in 2026-04 before replacing it with on-demand `RefpageCache` parsing — already absorbed. Added new `## LLM / AI-Assisted Patching` section to `forum/forum_insights.md` with two entries: (1) the contemporary-field architecture survey (Thread 2), (2) the documented 2023 LLM failure modes (Thread 1 — Pd/SuperCollider confusion, hallucinated object names, broken gen~ "simplifications") which justify CLAUDE.md's existing rules empirically. Queued two new tasks based on field findings: **[Opus]** "Borrow MaxMCP's Claude Code plugin/skills surfacing pattern" — codify Claude2Max's accumulated knowledge as user-invocable slash commands (`/c2m-design`, `/c2m-package-search`, `/c2m-tutorial`, `/c2m-sync`, `/c2m-explain`) instead of relying entirely on a 600+-line CLAUDE.md; lowers onboarding cost without losing the rule corpus. **[Sonnet]** "Add `/c2m-explain` — first-class 'explain this patch' skill" — borrowed from tiianhk's MaxMSP-MCP server; reuses existing infrastructure (extract, refpage cache, package query, tutorial clustering) to produce on-demand walkthroughs of any .maxpat from its embedded spec.

- 2026-05-01f: Added "Max Cookbook Analysis" task to TASK_QUEUE.md (Opus section) — crawl https://music.arts.uci.edu/dobrian/maxcookbook/, open all example patches, extract insights to new `cookbook/` folder, follow links with small/large classification protocol. Added `## Knowledge Resources — Consult Before Designing` section to CLAUDE.md listing packages, forum, and cookbook as sources to check during patch design; added folder convention for future scraped resources.

- 2026-05-01e: Forum Knowledge Crawl — first real crawl session. Chunk: 81 threads across 4 subforums (Jitter pp.1-3 + Javascript pp.1-3 + MaxMSP p.1 + RNBO p.1; M4L deliberately excluded). 44 artifact-bearing threads (119 artifact files: 88 .maxpat from MCT decode, 9 .js, 8 .zip, 6 .rnbopack, 3 .jxs, plus a few PNG/PDF/JSON). Phase A downloaded everything to `/tmp/forum/<slug>/` and marked each as `downloaded` in `forum_crawl_state.json`. Phase B read the discussions + the actual artifact code (the user's principle — insights live in the code, not the summary) and wrote 28 entries into `forum_insights.md` covering Audio/MSP, Video/Jitter, JS/v8, MIDI/DMX, UI/Presentation, Performance, Patching Patterns, RNBO. 13 entries flagged `[PROMOTION-CANDIDATE]` for future evaluation. Phase C did targeted WebFetches over the remaining 31 non-artifact-bearing threads, captured 4 more entries (M4L embed; jit.gl.syphonclient frame-rate gap; suckah Apple Silicon bug; RNBO per-voice param exposure), and marked the rest as `skipped-no-artifact` in state. State tracking now distinguishes `downloaded` (44) / `scraped` (6 Phase C) / `skipped-no-artifact` (31), totalling 81 — matches chunk size exactly. Next-session diff via `enumerate_forum_threads.py --diff` will show only NEW or UPDATED threads. **Notable insights surfaced**: (a) `jit.gl.multiple` per-instance alpha needs feature-stack-specific shader (multshade.jxs); (b) `jit.gl.buffer` requires one-buffer-per-inlet plus round-robin output cadence; (c) `copymatrixtoarray`/`copyarraytomatrix` is the canonical Jitter↔JS interop with type-switched typed arrays; (d) JXS shader textures bind by `<param>` declaration *order*, not by name; (e) PNG transparency requires `discard` in fragment shader to fix depth-test rejection; (f) `jit.gl.polymovie` (Max 8.2+) for multi-head movie playback; (g) `node.script` has a confirmed unresolved memory leak at high message rates (~135MB/hr at 44Hz × 20 universes); (h) `tc.preset` is the canonical pattrstorage UI replacement; (i) `jam.dmxusbpro` is the live DMX external (FTDI VCP driver required on M1); (j) `multislider @contdata 2` for mouse-up-only output (Max 8 inspector hides this).

- 2026-05-01: Forum Knowledge Crawl — built the crawl infrastructure end-to-end. Test crawl insights and progress have been purged; the actual crawl will start fresh. Permanent additions:

  **Tool — `enumerate_forum_threads.py`**: hits the forum directly with `urllib`, parses thread `slug` + `title` + `reply_count` from the HTML's `components_forumItem__` blocks (works around a WebFetch quirk that caches by URL path and silently returns stale page-1 content for `?page=2+`). Supports `--page N` / `--pages 1-5`, `--category MaxMSP|Jitter|Javascript|RNBO|...`, `--detect-content` (per-thread fetch of `__NEXT_DATA__`, count `patcher` MCT-blocks and `attachment` ProseMirror nodes), `--has-content` / `--has-mct` / `--has-attach` filters, `--list-attachments`, `--download <DIR>` (downloads attachments AND retrieves+decodes every MCT). Default sort is artifact-presence-first then replies — `--min-replies` deliberately *not* in the recipe because single-reply threads with MCT or attached files often beat 20-reply discussions with no artifacts.

  **MCT retrieval pipeline**: tracked the forum's React Copy button through Next.js chunks; the URL pattern is `https://cycling74.com/api/v1/patchers/<id>` (the `Gr` URL-prefix function in `_app-af36503ba7321926.js` resolves to `${origin}/api/v1${path}`). The endpoint returns `{_id, md5, patch, format: "juceb64_zip_json", __v}`. The `patch` field decodes via `spec2maxpat.py mct_decode` (after wrapping with begin/end delimiters). The decoded body is **standard Max copy-paste JSON** — see `SPEC_REFERENCE.md` "Max's Three Patch Serialization Formats" for the canonical explanation. `mct_to_maxpat()` adds the `{"patcher": {...}}` wrapper only when the body is the unwrapped (selection-style) form, so the saved `.maxpat` always opens cleanly.

  **Documentation**: added "Max's Three Patch Serialization Formats" section to `SPEC_REFERENCE.md` (`.maxpat` envelope vs copy-paste JSON vs MCT — same payload, three envelopes, only the wrapper/compression differs); rewrote `FORUM_CRAWL_LOG.md` with the canonical enumeration recipe, the artifact-prioritization principle, and the MCT pipeline explanation; reset `forum_insights.md` to a clean section skeleton. The first real crawl session will pick a focused subforum and start populating insights from scratch.

- 2026-03-30: Set up GitHub connection, added Work History tracking to CLAUDE.md and moved log to this file. (Note: a stop hook for auto-updating work history was attempted but never reliably worked — replaced by proactive in-session updates per CLAUDE.md.)
- 2026-03-30: Replaced `11clicks` with `shell` in mouse-control patch, reformatting all commands as cliclick CLI calls. Clarified in CLAUDE.md that spec embedding is required whenever the Claude2Max workflow is used, not just when running the converter binary.
- 2026-03-31: Reviewed prior session work, verified WORK_HISTORY.md structure, and configured automatic work history updates for all Claude Code instances via stop hook.
- 2026-03-31: Created one-sound-a-day repo (local only — git server proxy restricts to Claude2Max; push manually). Original patch on main; JS-refactored branch `claude/js-refactor` replaces date/time/match/blink/newday logic with `v8 onesound.js`. Documented v8 object usage, external JS files, message routing, dialog/playlist~/umenu patterns in CLAUDE.md and SPEC_REFERENCE.md.
- 2026-04-03: Added `add_tutorial.py` — analyzes a .maxpat's data-flow graph (wave-depth grouping), generates tutorial steps with object descriptions, and adds a umenu + prev/next nav + v8 JS controller + thispatcher to the patch. Highlights objects per step via bgcolor scripting; annotation comments show/hide per step. Tested on jit-grab-scale.maxpat (9 steps). Also deleted redundant external .json spec files (spec now embedded in .maxpat only). Added Tutorial System section to CLAUDE.md.
- 2026-04-03: Fixed tutorial annotation z-order (annotations now last in boxes array = on top), positioned annotations adjacent to their panels with correct bubbleside arrows. Added ALL CAPS naming convention to CLAUDE.md. Configured bypass permissions globally (`~/.claude/settings.json` + UI mode selector). Added audience note: repo is for students, CLAUDE.md must be beginner-comprehensive.
- 2026-04-03: Rewrote tutorial step grouping in `add_tutorial.py` — spatial clustering (X+Y distance), any-pair merging of connected objects, bounding box limits. Switched annotations to bubble-arrow comments (`bubble: 1` + `bubble_bgcolor`), panels to locked background (`locked_bgcolor: 1`). Fixed strip-before-generate ordering bug. Added `TUTORIAL_GUIDELINES.md` with process lessons. Configured global `~/.claude/settings.json` with `dontAsk` permission mode and broad Bash/Read/Write/Edit allow rules.
- 2026-04-03: Added AI-enhanced tutorial descriptions to `add_tutorial.py` — `--ai` flag (Claude API/Haiku), `--analyze`/`--steps-json` two-pass workflow (no API key, works from Claude Code). Fixed Max z-order (first in boxes array = on top, not last), corrected @bubbleside mapping (0=top, 1=left, 2=bottom, 3=right), restored flexible annotation placement. Added z-order and bubbleside docs to SPEC_REFERENCE.md, TUTORIAL_GUIDELINES.md, and CLAUDE.md.
- 2026-04-03: Added PreToolUse hook (hooks/sync_maxpat.py + .claude/settings.json) — any .maxpat file Claude reads is automatically synced: generates an embedded spec if none exists, or reconciles the existing spec with manual edits. Also: analyzed taylorbrook/MAX-MSP_CC_Framework; imported 1,694-object database (max_objects.json via build_objects_db.py), applied expert overrides, added 6 missing variable_io rules (spray, funnel, join, unjoin, selector~, matrix~), updated appversion to Max 9, fixed playlist~ outlet count bug (5→3), added Object Correctness Notes and Object Relationships sections to SPEC_REFERENCE.md, added upstream maintenance note to CLAUDE.md. Added `sync` command to spec2maxpat.py: reverse-engineers a spec from any .maxpat (no embedded spec) or reconciles an existing embedded spec with manual edits made in Max. Updated CLAUDE.md workflow to cover both cases.
- 2026-04-17: Created LCARS Panel Generator jsui (lcars.js + lcars.maxpat) — draws LCARS-style panels with 4 color schemes, generates random layouts, persists last panel to file, and supports JSON import/export.
- 2026-04-17: Fixed overlapping labels/controls in lcars.maxpat; added UI Layout spacing rules section to CLAUDE.md covering label-width estimation, horizontal spacing formula, routing-object widths, and the loadmess init chain pattern.
- 2026-04-17: Synced user edits to lcars.maxpat (jsui filename attr, resize to 560px, removed title comment); documented three rules in Claude2Max CLAUDE.md: use jsui filename attr, right-size UI elements, no redundant title comments.
- 2026-04-25: Built Improvised Movement+Music Ensemble Sequencer v3 and v4 (ensemble-sequencer-v4.maxpat + ensemble-v4.js + display.js). v4 features: grey names until piece starts (setactive), large GL timer, randomized performer order, max permutations with coverage-first selection, max consecutive same-role parameter, permutation list in jit.cellblock with index column and dynamic rows/cols, perm count message box, 7-outlet brain. Fixed jit.gl.text centering (align 1, int type — not anchor_x or string "center"), fixed jit.cellblock set COL ROW order (column-first). Moved all object-specific behavioral notes from CLAUDE.md to SPEC_REFERENCE.md. Added rules: never regress functionality when changing modality; always verify Max attributes against documentation before use (type, values, default, settability); object-specific knowledge goes in SPEC_REFERENCE.md not CLAUDE.md; update WORK_HISTORY proactively, not just at session end; read WORK_HISTORY at session start.
- 2026-04-26: Fixed perm-summary.js and ensemble-v5.js: (1) pass performer constraints in summary data so noSoloDance/noSoloMusic suppress expected observations; (2) track music and dance streaks separately, report all outliers not just one; (3) remove "N sections have a solo performer" (not notable); (4) add soft streak penalty in optimizeOrder (always active, grows with streak length beyond 3, weight 0.6) to reduce long same-role runs without hard-blocking.
- 2026-04-26: Added scrollbar + autoscroll:0 to perm-summary textedit in ensemble-sequencer-v5.maxpat. Mirror avoidance: non-sequential mirrors are fine and not notable; only adjacent mirrors are avoided. ensemble-v5.js: added permKey()/mirrorKey() helpers; optimizeOrder() adds +3.0 penalty when next candidate is the mirror of the current section. perm-summary.js: removed mirror-section and bookend-mirror observations (non-sequential mirrors are not notable). Added MOVING column (4th) to jit.cellblock permlist showing how many performers move in the next transition (-- for last row); widened cellblock presentation_rect from 346→432px.  MOVING column shifted to represent next transition, not previous.
- 2026-05-01b: Fixed `ezdac~` in `drift-sequencer-soviet.maxpat` — (1) `presentation_rect` corrected from 2-value position to 4-value `[x, y, w, h]` (36×36, top-aligned with gain at y=404); (2) colors set per refpage: `bgcolor`=INK (blends into output panel), `color`=RED (speaker icon when audio ON), `elementcolor`=CREAM (icon when audio OFF). On/off state visual now works; ezdac integrates with the INK panel instead of floating as a cream island.
- 2026-05-01: Built `patches/drift-sequencer-soviet.maxpat` — graphic-design-driven redesign with Soviet aesthetic. First attempt rebuilt the spec from scratch and silently dropped: comment-padding compensation (lost label/control x-alignment), the tutorial system, and other working details — also invented several attribute names on `live.gain~` and `ezdac~` that don't exist on those objects (Max accepts unknown attrs silently). Redone the right way: `extract → edit → convert` against the original spec, applying only deltas (title 22→40px, title bar 5→10px, +40 y-shift for content, 5 new section background panels, 2 new red section dividers, ink/cream styled controls, refpage-verified `live.gain~`/`ezdac~` color attrs). Original alignment, defaults, wiring, and tutorial system all preserved. Updated `c2m-themes/README.md` Soviet entry with concrete graphic patterns; wedges and paper grain marked optional. Enshrined two rules in `CLAUDE.md`: (a) **Never Write API Names From Memory** — cross-language discipline; silent-failing environments (Max attrs, CSS) are *more* dangerous than throwing ones because invented names are accepted and ignored; the live.gain~ valid color attr set per refpage is coldcolor/warmcolor/hotcolor/overloadcolor/slidercolor/textcolor/tricolor/trioncolor/tribordercolor/focusbordercolor/modulationcolor/inactivecoldcolor/inactivewarmcolor. (b) **Modify, Don't Rebuild — and Treat Observed-Good Patterns as Binding** — for new versions of existing patches the default is extract→edit→convert; rebuilding silently drops every working detail not re-derived; patterns called out as good in critique create a binding obligation to preserve them in any subsequent implementation.
- 2026-04-30: Applied Soviet Constructivist theme (c2m-themes/themes/soviet.css) to drift-sequencer.maxpat presentation view. Cream paper bg, red accent multisliders/bars, ink text, large bold title. Key fixes: (1) removed unconditional `parameter_enable`/`saved_attribute_attributes` injection for `live.*` objects from `spec2maxpat.py` — was locking gain slider on load; (2) fixed `live.gain~` arrow invisibility on cream bg using `@trioncolor`/`@tricolor` (not `bglocked`); (3) removed stale `sov_gain_bg` dark panel and extended `sov_bg` to cover full presentation height; (4) compensated for Max comment internal padding (~4px) by offsetting comment x positions to achieve precise left-alignment; (5) fixed window rect to match presentation content size. Enshrined rules: (a) `live.*` on light/dark themes — set `@tricolor`/`@trioncolor` when bgcolor changes significantly; (b) Max comment text offset — ~4px internal padding, compensate and verify visually; (c) sync-first applies to programmatic .maxpat post-processing too, not just user GUI edits; (d) converter must match Max's defaults, never exceed them (CLAUDE.md).
- 2026-04-29: Discovered c2m-themes folder — a complete CSS design system for Claude2Max presentation panels and jsui canvases, with 10 themes (5 original sci-fi + 5 new design-history themes), a token contract, and an UPSTREAM-SKILL.md manifest for a `/claude2max-design` skill. Added task to Opus queue to test the skill before integrating into the presentation UI workflow.
- 2026-04-29: Explored applying LCARS-Bold theme to drift-sequencer.maxpat. Iterations: (1) attribute-only restyling of existing controls (amber multisliders, dark number boxes); (2) added `panel`-based chrome (rail/header/pills) — deemed not convincing as LCARS; (3) generated authentic Okuda PNG background via PIL (large sweeping inner elbow, desaturated peach/salmon/lavender/mustard palette, numeric codes, multi-cell statement bar) referenced from a real screen-used Okuda console image, integrated as `fpic` at z-order 0; (4) reverted all theme styling per user request. Final patch state: tutorial panels restored, `live.gain~` `_parameter_initial` attributes stripped (now uses default settings instead of forcing to 0 dB on load — note: 0 dB is unity gain, not silence). Added `patching/MAX_PATCHING.md` rule "Themes carry graphic elements, not just colors" and `c2m-themes/README.md` per-theme graphic-elements checklist (10 themes, plus a "how to read CSS frame tokens" guide). Enshrined rules: (a) audio parameter values are meaningless without their unit — verify `@units`/`@range` before describing behavior (`SPEC_REFERENCE.md`); (b) when a multi-part clarifying question is partly answered, re-ask the unanswered parts rather than acting on a best guess (`CLAUDE.md`).
- 2026-04-29: Repaired tutorial system across drift-sequencer, face-capture, and jit-grab-scale. Root cause: `spec2maxpat.py` `_PRESERVE_ATTRS` and `reconcile_spec()` only preserved styling attrs (bgcolor/textcolor/font*), so anything affecting **identity / visibility / z-order / runtime behavior** was silently dropped on each sync→convert cycle, breaking tutorials over time. Fixes: (a) extended `_PRESERVE_ATTRS` to include `varname`, `hidden`, `background`, `bordercolor`, `border`, `rounded`, `locked_bgcolor`, `bubble`, `bubbleside`, `bubblepoint`, `bubbletextmargin`, `bubble_bgcolor`, `items`, `prefix`; (b) updated `reconcile_spec()` to re-extract `_PRESERVE_ATTRS` from live boxes for matched objects, not just `pos`/`text` (without this, varnames added in Max's Inspector were invisible to sync); (c) restored proper `tut-panel-N`/`tut-ann-N` varnames, `hidden:1`, `background:1`, `locked_bgcolor:1`, panel chrome (border/rounded/bgcolor/bordercolor), bubble formatting (`bubble:1`, `bubble_bgcolor`, `bubbleside`), 220px standard width, and umenu `items` arrays on all 3 patches. Refactored: tutorial annotations now park at a single shared "stow" position per patch (out of the way of editing), and the JS controller's `gotoStep()` moves the current step's annotation to its active position via a baked-in `ACTIVE_RECTS` array (Max box-rect format `[x1,y1,x2,y2]`) plus `STOW_RECT`. Round-trip verified: sync→convert preserves all tutorial state across all three patches. Fixed face-capture's `t b l` → `t l b` (right-to-left fire order means the operation that runs FIRST must be the rightmost arg; existing wiring was topologically correct but trigger args had bang and list swapped, causing iter to receive a no-op bang and clear to wipe the menu after population). Enshrined rules: (a) `TUTORIAL_GUIDELINES.md` — full structural contract (panel attrs, annotation attrs, umenu items, patchlines, JS structure) plus comment-pile pattern explanation and a sync/convert breakage diagnostic script; (b) `CLAUDE.md` — "Reference Instances Illustrate Principles" (enshrine the structural contract, not specific files; files are illustrative, may be renamed); (c) `patching/MAX_PATCHING.md` — "trigger for sequencing — verify both order and outlet types" (with `t l b` vs `t b l` worked example).
- 2026-04-28: Restructured docs: extracted patching/MAX_PATCHING.md from CLAUDE.md (patching principles, presentation guidelines, doc verification rules, common pitfalls); generalized patching principles to lead with intent; added model-switching rules (6 Opus trigger cases, exact switch commands, Claude Design workflow for presentation UI); added rules for verifying external state before asserting and always generalizing before enshrining; reorganized TASK_QUEUE.md into Opus/Sonnet sections; added TouchOSC mk2 Integration task.
- 2026-04-27: Fixed drift-sequencer and face-capture tutorial/patch bugs: bubbleside=1 on tutorial annotations 1–9 (were pointing up instead of left); face-capture vdevlist→umenu chain (inserted route vdevlist → t b l → clear + iter → prepend append). Reviewed drift-sequencer presentation view via screenshot; fixed layout against CLAUDE.md guidelines: content-driven multislider width (400px = 8 steps × 50px), monospace Courier font on all labels, ignoreclick+triangle=0 on display-only note number box. Enshrined rules: read presentation guidelines before visual review; multislider width = step_count × 40–50px (never window-derived); Courier/Monaco uses ~8.5px/char (not 7.5). Cleaned up youthful-austin worktree (branch was fully behind main, nothing to merge).
- 2026-04-26: Extended RefpageCache to cache digest, attributes, messages, object arguments, output descriptions, and see-also in a single XML pass (spec2maxpat.py). Added describe() convenience method. Created perm-summary.js (10 analysis dimensions, ranked plain-English output). Created ensemble-v5.js (outlet 7 → perm-summary) and ensemble-sequencer-v5.maxpat (adds perm-summary v8 + textedit in presentation view). Implemented layout engine Phase 1 (presentation_layout() — grid-based placement from {"col","row"} hints) and Phase 2 (auto_layout improvements: side-by-side grouping of inputs sharing a downstream destination). Phase 3 (screenshot verification) queued. Also fixed label-test and clamp-test patches: inlet/outlet 30×30, @comment via attrs, clip not max/min, * 1.0 for float multiply, no double title. Added rules: verify object existence + digest before use; rules from errors require user confirmation before enshrining; inlet/outlet @comment on box AND adjacent comment box.
- 2026-04-25: Replaced max_objects.json + build_objects_db.py with RefpageCache in spec2maxpat.py — on-demand XML parsing of C74 maxref.xml per object, session-cached, zero build step, always matches installed Max. Removed taylorbrook dependency entirely. Added TASK_QUEUE.md with two queued tasks (extended RefpageCache metadata; layout engine + screenshot verification). Added session-start check script (bash, /tmp timestamp) to both Claude2Max CLAUDE.md and global ~/.claude/CLAUDE.md. Established subpatcher inlet/outlet labeling rule: @comment attr on p object (outside) + comment box adjacent to inlet/outlet (inside). Created label-test.maxpat demonstrating convention. Fixed label-test.maxpat errors: inlet/outlet size now 15×15 (added to UI_SIZES), @comment attr via spec attrs field, * not *. for int×float multiply, removed redundant title comment, inputs arranged side by side.
- 2026-04-29: Fixed face-capture face-count detection. Two bugs: (a) `jit.matrix 4 char 640 360 @usedstdim 1` had `@usedstdim 1` set without `@dstdimstart`/`@dstdimend` companions — a no-op toggle; removed the attribute entirely. (b) `cv.jit.faces` outlet 1 wired directly into `> 0`; that outlet emits info messages like `nfaces N`, not raw integers, so the comparison never fired. Inserted `t b l` to sequence matrix-in then `getnfaces` query, and `route nfaces` between outlet 1 and `> 0`. Enshrined two rules in `SPEC_REFERENCE.md`: (i) attribute "enable" toggles must be set as a group with their companion bounds (e.g. `@usedstdim 1` requires `@dstdimstart`/`@dstdimend`) — set the whole group or omit the toggle; (ii) info outlets follow a query/route pattern — send `get<param>` to inlet, place `route <selector>` on the outlet, never wire info outlets directly into arithmetic. Process correction: violated the existing CLAUDE.md "Partial Answers Are Not Consent" rule by acting on the nfaces fix when only the @usedstdim question had been answered — flagged for stricter adherence going forward.
- 2026-04-30: Fixed face-capture video-never-reaches-matrixset bug. Later session: threw out face-capture.maxpat and rebuilt from scratch after full doc verification. New patch: jit.grab → jit.matrix 4 char 320 240 → (jit.rgb2luma → cv.jit.faces → gate control) → gate → jit.matrixset 300 4 char 320 240 → jit.world @floating 1 for playback. All depth-first ordering correct (resize→luma fires before resize→gate inlet 1; gate→trig_store/write_counter fires before gate→matrixset). face-capture-tutorial.js is now orphaned (tutorial not rebuilt). Root causes: (a) gate was receiving a bang instead of the video matrix — matrix from resize was never routed to gate or matrixset; (b) trig_store was `t b b`, so matrixset only received bangs; (c) counter max connected to inlet 3 (reset) instead of inlet 4 (set maximum); (d) jit.matrixset missing required positional args — needs `<matrixcount> <planecount> <type> <width> <height>` (e.g. `300 4 char 640 360`), not just `@matrixcount`; (e) jit.gl.render + jit.window replaced with jit.world @floating 1 (semi-deprecated). Fixes: resize routes to gate inlet 1; gate fires depth-first to trig_store (t b → counter → index) then directly to matrixset; counter max on inlet 4; matrixset with full format args; jit.world for display. Enshrined jit.world preference rule in patching/MAX_PATCHING.md and SPEC_REFERENCE.md.
- 2026-04-30: Started Package Objects Library task. Built `build_package_objects.py` — extracts refpage XML metadata for installed Max packages by reusing `RefpageCache._parse` from `spec2maxpat.py`. Supports `--package <name>` (single-package extract) and `--merge package_objects.json` (preserves existing `use_when` values across re-extractions). Curated HISSTools Impulse Response Toolbox (HIRT) as v1 pilot — 24 objects, every entry has a verbose `use_when` describing the patching scenarios where it beats native chains, with decision-relevant arguments and messages folded into the prose. Final per-object schema: `digest`, `numinlets`, `numoutlets`, `outlettype`, `kind` (external/abstraction/javascript, auto-detected by file presence in the package directory), `tags`, `use_when`, plus optional `deprecated_by` (set when refpage discussion text mentions "deprecated"). Attributes, arguments, messages, seealso, and per-outlet output descriptions are intentionally NOT cached — they're available from the refpage XML on demand and aren't needed for "should I reach for this?" decisions. Added "Consult Installed Packages Before Long Native Chains" section to CLAUDE.md. Survey results: 28 user packages have refpages (~1700 objects total); ~10 well-known packages (cv.jit, Vsynth, ejies, modulo, Syphon, RNBO Synth Building Blocks, Max for the Visual Arts, catart-mubu) ship help files only and are deferred to a Phase 2 extractor.

- 2026-04-30: Extended package curation to help-file-only packages (no refpages). Added `build_helpfile_objects.py` — a parallel extractor that walks `*.maxhelp` files, pulls the digest comment, the canonical-instance args/attrs/I-O counts, and a description list. Same `--package` / `--merge` CLI as the refpage extractor. Curated 5 help-file packages at verbose tier: catart-mubu (18), ejies (44), cv.jit (70), modulo (100), RTC-lib (180). 412 new entries; `package_objects.json` now holds 652 entries across 11 packages. Verbose-tier verbosity stayed uniform but average length per package varied with object density: catart-mubu 391 chars (corpus-pipeline abstractions need substantial wiring notes), cv.jit 239 chars (mostly single-purpose CV externals), ejies 260 chars, modulo 152 chars and RTC-lib 155 chars (both have many trivially-defined utility wrappers where 1-2 sentences IS the operational entry — verbosity should match object complexity, not run a fixed minimum). Help-file metadata is sometimes thin (Essl's RTC-lib often has only "Recompiled as Universal Binary…" as the most prominent comment), so I supplemented with knowledge of the package's documented architecture (Koenig's selection principles, ED-based rhythm objects, 12-tone-row tools). 10 truly help-less packages identified — neither refpages nor `.maxhelp` files (Jitter Recipes, Gen CV Tools, ABL Effect Modules, AudioMix, JitLygia, MC Movement Studies, PGS-1, Delicious Tutorials, gen~ Plugin Export, adsr221) — flagged in TASK_QUEUE for a third extractor approach.

- 2026-04-30: Rewrote 216 `package_objects.json` entries at verbose (HISSTools) tier across FFTease, zsa.descriptors, Sound Design Toolkit, CNMAT Externals, and FluidCorpusManipulation. The first-pass entries were terse "what + when" descriptions that downgraded fidelity from the HISSTools baseline; user flagged the regression after seeing a CNMAT sample. The verbose entries now fold in decision-relevant control surface — message selectors, key attributes, constructor args, output behaviour — so a reader can configure and wire the object from the entry without a refpage round-trip. Average use_when length ~370 chars (matches/exceeds HISSTools baseline of 334). Built `/tmp/dump_refpages.py` (one-off) using `RefpageCache._parse` to emit compact per-object metadata (digest, args, attrs, messages) as the source of truth for the rewrite. Enshrined the rule under "Consult Installed Packages" in CLAUDE.md: **"Curated entries are operational, not just descriptive — fold in decision-relevant control surface so a reader can configure and wire from the entry alone, only consulting the refpage for edge cases."** All future package curation follows this tier.

- 2026-04-30: Curated five more packages into `package_objects.json` — FFTease (38 objects), zsa.descriptors (22), Sound Design Toolkit (25), FluidCorpusManipulation (80), CNMAT Externals (51). 216 new `use_when` judgments written, 240 entries total across 6 packages. CNMAT was initially skipped because 48 of 51 refpages have empty `<inletlist>` tags ("do not use"); user reframed: the I/O zeros are fine if there's a documented convention to consult the refpage. Added that convention to the CLAUDE.md "Consult Installed Packages" section: when `numinlets` or `numoutlets` is `0`, treat as "unknown" and consult `<package>/docs/refpages/<package_ref>/<object>.maxref.xml`. Same convention applies to FluCoMa (refpages omit `<outletlist>` entirely). Hardened `RefpageCache._parse` against non-integer `size` attributes (CNMAT's `bpf.maxref.xml` had `size="variable"` which crashed `int()` coercion); now does `try int → fall back to original string`. Enshrined rule in CLAUDE.md: "Parsers Must Tolerate the Schema's Full Value Space, Not Just the Sample You Tested Against" — general principle, with the size="variable" case as illustration. Tagged `{!pre-edit}`. CNMAT's deprecated OSC objects (opensoundcontrol, osc-route, osc-schedule, osc-timetag) flagged in their `use_when` with pointer to the odot package as replacement.

- 2026-04-29: Refactored advisory hooks to a single source of truth. Tagged headings inline in CLAUDE.md with `{!pre-edit}` and `{!pre-commit}` markers (Rules from Corrected Errors, Verify External State, Reference Instances, Partial Answers, When Building a New Version, Never Regress, Working on existing patch — sync first, Spec files temporary, Keeping Docs in Sync, Work History). Replaced `pre_commit_review.py` and `pre_edit_review.py` with `hooks/inject_admonitions.py <event>` — one generic injector that greps tagged headings across `*.md`, extracts each tagged section (heading through next ≤-level heading), and surfaces them as `additionalContext`. Same gates as before: pre-edit skips `/tmp/`+`/var/folders/`; pre-commit only fires when Bash command contains `git commit`. Adding a new admonition is now a one-tag edit, not a Python checklist edit; rules and at-action-point reminders are guaranteed-identical text. Updated `.claude/settings.json` to call the new injector. Tested all paths (pre-edit fires with full tagged content, /tmp skips, pre-commit non-commit skips, pre-commit on `git commit` fires).

- 2026-05-01: Curated all 10 remaining helpfile-only packages — 533 new use_when entries, library now 2,795 records across 68 of 79 installed packages. Packages: Vsynth (137), jasch objects (113), Max for the Visual Arts (90), TS.Modular (84), EAMIR SDK (48), RNBO Synth Building Blocks (24), XRAY (23), XY Coordinates Generator Pack (10), Syphon (2), jk.lpx (2). 15 hand-written overrides for foundational/decision-relevant objects (Vsynth render terminal, RNBO SBB envelope variants, Syphon GPU sharing pair, EAMIR modal trio); remaining 518 use templated framing per package. Added concept sections to `package_concepts.md` for Vsynth (RGB vector signal protocol, single-render-terminal rule), RNBO SBB (gen~-domain primitives, when-to-use vs native MSP), EAMIR SDK (modal/accessibility paradigm). Fixed `build_helpfile_objects.py` to tolerate non-UTF-8 helpfiles (Max for the Visual Arts has at least one helpfile with macOS-Roman / latin-1 byte sequences) — try utf-8 then fall back to latin-1 which decodes any byte sequence. Only the 11 truly help-less packages (no refpages and no .maxhelp files) remain uncurated; they'd need a third extractor approach (inspect example abstractions / source patcher metadata).

- 2026-05-01: Refpage-extractor I/O recovery — packages whose refpages don't carry structured `<inletlist>`/`<outletlist>` tags (FrameLib documents I/O in prose, abstraction-based packages like Upshot don't have an external to query, MIAP has refpages and helpfiles in different cases) were producing zero-I/O records that the converter `PackageObjectsCache` couldn't use for fallback. Added a two-stage `_supplemental_io()` probe to `build_package_objects.py`: (1) walk the matching `.maxhelp` file for a canonical instance whose first text token equals the object name (works when help patches instantiate the object directly — handles FrameLib externals); (2) walk the matching abstraction `.maxpat` for top-level `inlet`/`outlet` boxes (handles abstraction-based packages — Upshot's panners are abstractions). Both stages match basenames and instance text case-insensitively (handles MIAP's `MIAP~.maxhelp` vs `miap~.maxref.xml` mismatch). Recovery: FrameLib 163/165, Upshot 105/110, ACToolkit 11/11, MIAP 3/4, CNMAT 42/51, av-toolbox 65/91. Library-wide zero-I/O: 394 (17%) → 269 (11%). Converter `PackageObjectsCache` fallback index: 459 → 2,051 resolvable objects (a 4.5× increase). All 2,262 records still pass `query_packages.py validate` with 0 hard issues.

- 2026-05-01: Mass curation pass — all 46 remaining refpage-XML packages curated (1,369 new entries). Library now 2,262 records across 58 of 79 installed packages; only helpfile-only (10) and no-docs (11) packages remain uncurated. Approach: hand-written overrides for foundational/decision-relevant objects (~40 entries — FrameLib schedulers/contexts/converters, PeRColate STK models, MuBu container objects, odot OSC core, etc.); digest-templated entries for the bulk (~1330 entries — math/trig wrappers, panners, utilities) where the digest IS the operational entry plus a one-sentence "use as the X analogue of Y" disambiguator. Added concept sections to `package_concepts.md` for FrameLib (frames, contexts, schedulers, NRT mode, complex frames), cage/dada (bach-extension pointer), ears (HISSTools-aligned buffer audio), odot (modern OSC, deprecation pointer), MuBu For Max (multi-track containers), PeRColate (STK port), grainflow (per-grain scheduling vs cloud-style), Digital Orchestra Toolbox (DMI mapping). Fixed parser tolerance: `RefpageCache._parse` now handles non-integer outlet `id` attributes (odot uses `id="1--"` on at least one refpage) — falls back to source order, same pattern as the size="variable" fix. Post-batch upgrade pass for thinnest entries (AOO, Nonlinear Sequencer, karma~, Auzzie, Upshot panners — 19 entries) where digest alone was uninformative. All 2,262 records pass `query_packages.py validate` with 0 hard issues. Mean entry length 144 chars across the whole library.

- 2026-05-01: Curated bach (241 objects, refpage source) into `package_objects.json` — total library now 893 records across 12 packages. Created `package_concepts.md` as the per-package reference for foundational paradigms that don't fit per-object — bach section covers lllls (Lisp-like nested lists, bach's universal data type), rationals + pitches (native data types beyond Max's int/float/sym), midicents (numeric pitch representation, 100 mc = 1 semitone), addresses (path-into-llll syntax with negative indices, multi-element, ranges), lambda inlets/outlets (synchronous callback protocol — `pipe`/`delay` break it), bach.roll vs bach.score (proportional vs measured notation), slots (per-note metadata containers), bach.reg/bach.pv/bach.shelf/bach.value (state-sharing scopes), bach set theory and constraint-satisfaction infrastructure, bell scripting language. Curation tier appropriate per object: foundational (bach.roll, bach.score, bach.iter, bach.mapelem, bach.find, bach.subs, bach.eval, bach.constraints) get 300-700 char operational entries with messages/attrs/gotchas; substantive domain objects (set theory, score conversion, matrix algebra) get 100-300 chars; utility wrappers (bach.+, bach.median, bach.gcd) get 60-150 chars where the digest already says everything important. Mean entry length 149 chars, 0 hard validation issues. Enshrined two new rules. (1) CLAUDE.md "Consult Installed Packages" — extract conceptual knowledge with every package, capture once in `package_concepts.md` rather than re-explaining in every per-object entry. The trigger: if you can't write `use_when` for the package's central object without first explaining a custom data type, paradigm, or vocabulary, that explanation belongs in concepts. (2) TASK_QUEUE entry to backfill `package_concepts.md` for already-curated packages — HISSTools, FFTease, FluCoMa, SDT, CNMAT, catart-mubu, cv.jit, RTC-lib all have concepts worth capturing; modulo and ejies likely don't.

- 2026-04-30: Schema-conformance pass for `package_objects.json`. Goal: future package scans cannot drift from the canonical shape. (1) Created `package_schema.py` — `CANONICAL_DEFAULTS` (ordered dict of required keys + defaults), `OPTIONAL_KEYS` set, `TYPE_CONSTRAINTS`, `VALID_SOURCES`, `VALID_KINDS`. Provides `normalize(record)` (auto-coerce structure: fill missing keys with defaults, coerce basic type mismatches, strip unknown and underscore-prefixed transient keys, preserve OPTIONAL keys verbatim) and `validate(record, *, strict=False)` (semantic checks: missing required, type mismatch, enum violations, unknown keys; with `strict`, also flags soft issues like empty digest, empty use_when, zero I/O). Adding a new schema key now propagates automatically — change `CANONICAL_DEFAULTS` and both extractors emit it on next run. (2) Wired `normalize()` into both `merge_into()` functions — every record landing in `package_objects.json` passes through normalization exactly once, including untouched prior records that share a bucket. Silent drift is now impossible. (3) Added `query_packages.py validate` (and `--strict`) for library health checks; verified live library passes with 0 hard issues across 652 records. (4) Round-trip tested: re-extracted HISSTools (refpage) and cv.jit (helpfile) — 24/24 and 70/70 use_when values preserved, schema validation still clean. (5) Defense-in-depth verified: a synthetic record with wrong types, unknown keys, and invalid enum values is auto-fixed by `normalize()` for structure, with remaining enum violations flagged loudly by `validate()`. (6) Documented contract in CLAUDE.md "Consult Installed Packages" — including a paragraph explaining how a future third extractor approach (e.g. inspecting example abstractions for help-less packages) must integrate with the schema module.

- 2026-04-30: Package library usability pass. (1) Unified the schema across both extractors — every record in `package_objects.json` now has the same key set (`digest`, `numinlets`, `numoutlets`, `outlettype`, `kind`, `tags`, `args`, `source`, `use_when`, optional `deprecated_by`); migrated all 652 existing entries via a one-shot in-place migration. Refpage extractor now also captures `objarg` names into `args`; helpfile extractor adds `tags: []` and stashes transient `_description`/`_attrs_seen` for the curator's first read (stripped on persist). (2) Added `query_packages.py` — read-only CLI with `list` (every installed package + curation status, distinguishing curated / uncurated-extractable / no-docs) and `search <term>` (ranked matches across `digest` + `use_when` with use_when hits scored highest). Tested against live data: `list` correctly identifies 11 curated, 57 extractable-uncurated, 11 no-docs across 79 installed packages; `search "convolve"` ranks HISSTools/FFTease entries appropriately. (3) Wired `package_objects.json` into `spec2maxpat.py` via new `PackageObjectsCache` class — final fallback in `guess_newobj_io()` after the C74 RefpageCache. Skips entries with zero I/O signatures (helpfile extraction failures), exposes 459 distinct objects resolvable without spec overrides. Verified end-to-end: a spec referencing `cv.jit.faces` and `bufconvolve~` (neither in C74) converts cleanly with correct numinlets/numoutlets/outlettype. (4) Updated CLAUDE.md "Consult Installed Packages" with the schema table, query CLI examples, and the converter-fallback note. No new rules enshrined this session — the work was entirely tooling.

- 2026-05-01: Folderized repo — moved forum files (enumerate_forum_threads.py, forum_crawl_state.json, forum_insights.md, FORUM_CRAWL_LOG.md) into forum/ and package files (package_objects.json, package_concepts.md, package_schema.py, build_package_objects.py, build_helpfile_objects.py, query_packages.py) into packages/. Updated all path references: spec2maxpat.py PackageObjectsCache DEFAULT_PATH, enumerate_forum_threads.py STATE_FILE_DEFAULT (now __file__-relative so it always finds forum_crawl_state.json regardless of CWD), and all command examples and prose references in CLAUDE.md, TASK_QUEUE.md, PRE_EDIT_CHECKLIST.md, and script docstrings. Smoke-tested: PackageObjectsCache resolves packages/package_objects.json correctly; query_packages.py list runs clean; enumerate_forum_threads.py --help parses without error.

- 2026-05-27: Flipped the student GitHub policy from "local-only is the default, GitHub is optional" to "every student gets their own fork by default, Claude runs all `git`/`gh` commands for them". CLAUDE.md changes: (1) Renamed and rewrote the top-of-file section "Local-Folder Use Is Fully Supported — GitHub Is Optional" → "Default Workflow — Your Own GitHub Fork (Claude Handles the Setup)" — recommends forking, justifies why (portability, instructor visibility, upstream pulls, community PRs back), binds the rule that students never type `git`/`gh` themselves, keeps local-only as a documented fallback with a once-or-twice re-offer policy. (2) Rewrote "New User Setup" as a five-step flow: detect git checkout → maintainer skip → `gh` install/auth (Claude walks them through `gh auth login` interactively) → fork detection (via `gh repo view --json parent,nameWithOwner`) and creation (via `gh repo fork --remote=true --remote-name=origin --clone=false`) → `insights/<name>` branch + push. Local-only fallback documented as a parallel branch. (3) README.md line 24 updated to recommend the fork workflow and point at the CLAUDE.md section. (4) Added a top-of-Opus-queue task in TASK_QUEUE.md: **Community Knowledge Pipeline — auto-gather usage data per fork, auto-PR back to upstream** — passive structured logging (`community-data/observations.jsonl`) on `insights/<name>` branches, anonymization, periodic PR back to a `community-observations/` folder on upstream, plus `/c2m-share-insights` and `/c2m-log-observation` skills. Then added "Preferred Objects for Common Tasks" — a per-task default-object table (audio I/O `ezadc~`/`ezdac~`, MIDI `notein`/`ctlin`/`bendin` direct, oscillator trio `saw~`/`tri~`/`rect~`, `groove~` for sample playback, `bp.Gigaverb`/`bp.Freeverb` for reverb, `tapin~`/`tapout~` for delay, `biquad~`+`filtergraph~` for filter, `metro`, `random` with attrs, `scale`, `v8` for comparison/routing AND list manipulation, `multislider`/`jit.cellblock`, message-right-inlet OR comment+set readout, `dialog` for text input, `dict` default for storage, `jit.matrix`+`importmovie` for image load, `jit.world`+`jit.gl.videoplane` for matrix display, `jit.grab` webcam, CNMAT `o.route` for OSC, `node.script`+multi-user-template for networking, `send`/`receive`/`value`/`pv`/`v` by scope, `v8` default JS) — student-friendly defaults that Claude reaches for first when planning a new patch. Section closes with a reminder to consult the package library before any 3+ object native chain. Reverb research note: built-in Max ships BEAP `bp.Gigaverb` / `bp.Freeverb` (no native `yafr2~` / `freeverb~` externals exist); `hirt.convolutionreverb~` from HISSTools is the higher-fidelity option when installed.

- 2026-05-28: Task queue review pass triggered by the new MCP+enforcement task being added to "Pending — Opus." Walked all 12 active tasks (3 in progress + 9 pending) and applied four hygiene edits: (1) struck subtask 5 of "Plugin/skills polish pass" (`/c2m-refpage` skill) with a supersession note pointing at MCP's `lookup_object` — same use case, but a first-class tool instead of skill-printed text; (2) added a "Supersedes…" + "Soft prerequisite: Promotion-candidate review pass" note to the MCP task's "Fits into the larger system" paragraph; (3) added a pairing note to MCP Phase (i) Step 5 cross-referencing the "Refine the student/user setup process" Sonnet task (both want a fresh-machine, fresh-project Claude session — run them in the same test window); (4) merged "Package Objects Library — quality upgrade pass" + "Backfill `packages/package_concepts.md`" into one entry "Package Library — operational quality pass" with Layer A (per-object `use_when`) and Layer B (per-package concept sections) sub-headings preserving the original structure — both write to `packages/` tree, both serve the same downstream consumers (MCP, converter, package-search skill), and a session that has a package's docs loaded for one is naturally positioned for the other. New "Queue Analysis & Hygiene Notes" section appended at end of TASK_QUEUE.md documenting cross-task relationships, soft prerequisites, supersession decisions, pairings, considered-and-rejected fold-ins (Repo-as-Total-Scope kept separate; the three crawl tasks kept separate), and the position-vs-priority note for the new MCP task. Pointer added near the top of TASK_QUEUE.md telling readers to also read the analysis section. Approach: section dated, with explicit instructions on when to redo the analysis (queue drift >~5 tasks or material shape change); prior dated reviews preserved for historical context. No tasks deleted as stale — queue was in good shape modulo the one redundancy and one merge.

- 2026-05-28: Cross-platform pass over the MCP server + global enforcement task in `TASK_QUEUE.md`. Step 1 (research & decisions) rewritten to be explicitly cross-platform: lock per-OS Claude Code MCP config paths (macOS `~/Library/Application Support/Claude/`, Linux `~/.config/Claude/`, Windows `%APPDATA%\Claude\` — all hedged with "typically" since paths historically move; confirm via official docs during Step 1, do not assert from memory), commit to Python for the installer + hook (not bash/zsh — won't run on Windows; not PowerShell — won't run on macOS/Linux), and use `tempfile.gettempdir()` + `hashlib` for portable sentinel paths instead of hard-coded `/tmp` and `md5sum`. Propagated the same cross-platform reality through the rest of the task body: skill description, hook description (filename `claude2max_max_edit_gate.py` not `.sh`), installer description (`install_global.py` not `.sh`), deliverables list. Reasoning preserved inline: shell scripts exclude Windows, batch/PowerShell excludes macOS/Linux, Python is the only language first-class on all three and is already the toolkit's runtime. The `--verify` mode of the installer becomes the mechanism for confirming Linux/Windows-specific paths on student machines where the dev machine couldn't test directly.

- 2026-05-28: Scoped + queued the next major architecture push: **Claude2Max MCP server + global enforcement layer**, merged from two earlier task drafts (Tier 1+2 "global skill + hook" and Tier 3 "MCP server") after John's challenge that Tier 3 is the real goal and Tier 1+2 was risking throwaway work. Resolution: the two tiers are *orthogonal* — Tier 3 is knowledge representation (prose → callable tools), Tier 1+2 is enforcement (Claude can't act without consulting). Even Tier 3 alone leaves the persuasion problem unfixed (Claude must still *choose* to call MCP tools), so the hook from Tier 2 is required regardless. Architecture unified into one task with MCP designed first and the skill/hook scaffolding wrapping it from day one — the skill body invokes MCP `essentials()` rather than reading prose, so no refactor later. Task added to `TASK_QUEUE.md > Pending — Opus` as the top entry, with full architecture (3 pieces — MCP server / global skill / PreToolUse hook), tool surface (`essentials`, `lookup_object`, `lookup_attribute`, `list_attributes`, `search_packages`, `verify_spec`, `search_pitfalls`, `lookup_rule`), 7 open design decisions with recommendations, full deliverables list, prerequisites, system-fit notes, and an embedded **"Phase (i) Step-By-Step"** section that names the first 5 sessions: (1) research & decisions (MCP SDK choice, Claude Code MCP config path, $CLAUDE_SESSION_ID availability, lock the design recommendations); (2) server skeleton + `essentials()` tool; (3) `lookup_object()` wrapping the existing RefpageCache + package_objects.json chain; (4) `lookup_attribute()` + `list_attributes()` by parsing `<attribute>` entries from refpage XML; (5) end-to-end real-world test in a fresh non-Claude2Max project. Each step sized to a single focused session. Timeline runway is May → September 2026 (no students until fall semester) — sufficient to ship Phase (i) cleanly and iterate before student arrival. Confirmed in-chat that the MCP server lives in the Claude2Max repo (`mcp_server/` subdirectory, same model as existing Python tooling — runs from the user's clone, no external hosting).

- 2026-05-28: Strengthened the Claude2Max consultation framing in response to John reporting that other Claude instances (working in repos that *reference* Claude2Max via the global pointer) routinely skip the actual consultation step and proceed with training-data Max knowledge. Three coordinated edits, all approved by John in intent though not in literal wording (flagged for revision): (1) Added a new top-of-file section to `Claude2Max/CLAUDE.md` titled "If You Are A Claude Instance Reading This From Another Repo — Why You're Here" — leads with "You do not know Max" and names the silent-failure mode concretely (unknown attributes accepted-and-ignored, made-up object names landing as missed red boxes, presentation conventions producing "works but unusable" patches). Lists the three required-reading files in priority order (`CLAUDE.md`, `SPEC_REFERENCE.md`, `patching/MAX_PATCHING.md`) and the domain add-ons (gen/jit.gen/jitter-js/m4l). Frames the cost: "a few minutes of context vs. silent-failure debugging the student inherits after you're gone." (2) Rewrote the global-pointer template in Step 3 of New User Setup with the same operational framing — "REQUIRED reading before any Max work" replaces the older advisory "always consult." Adds the package-library consultation requirement (`python3 packages/query_packages.py search "<term>"` before any 3+ native chain). (3) Mirrored the same pointer text into John's live `~/.claude/CLAUDE.md` so all Claude instances on this machine pick up the new framing immediately, not just future fork installs. Architecture decision logged: rejected the "create a distilled ESSENTIALS.md" approach on John's call — `SPEC_REFERENCE.md` and `MAX_PATCHING.md` are themselves the essentials; the right move was strengthening the pointer language, not adding a tier between pointer and source-of-truth. Also rejected a Claude2Max-side session-start hook for the same reason John identified: students work in *other* repos and refer back to Claude2Max from there, so Claude2Max's own session-start machinery doesn't fire in those sessions — only the global-pointer text reaches them.

- 2026-05-28: Two new binding rules enshrined in CLAUDE.md following user confirmation that c2m.inspect should be a default Claude debug tool. (1) **"Always Inspect, Never Guess — Reach for /c2m-inspect During Patch Debugging"** — when a Max-debugging question hinges on runtime contents of a named data structure (dict/buffer~/jit.matrix), default to dumping it rather than reasoning from upstream wiring. Recognition signal called out: any sentence beginning "the dict probably contains…" or "the buffer should have…" is the moment to stop and run the dump. Three-step mechanic: ping → add c2m.inspect if missing (via sync→edit→convert + user reload) → dump → read /tmp JSON. (2) **"Clearly Mark Debug Additions to a User's Patch — Binding Rule"** — any object Claude adds for diagnostic purposes (c2m.inspect, extra print boxes, scope displays, value-watch comments) must be visually unmistakable: magenta bgcolor `[1.0, 0.3, 0.8, 1.0]` + matching bordercolor (reserved repo-wide for debug additions), adjacent `🔍 DEBUG (Claude) — remove when done` comment with same magenta bg, spatially grouped in bottom-right corner of patching view, NEVER in presentation, and tracked in a top-level `debug_additions: ["<id>", …]` array in the spec for mechanical cleanup. Rule framed as symmetric with "Always Hide Plumbing Patchcords": visibility matches intent. Closes with a proactive-cleanup commitment — when debugging is complete, Claude proposes removing every object listed in `debug_additions`. Rule applies in Claude2Max repo workflow; will need follow-up to teach `spec2maxpat.py` about the `debug_additions` spec field if cleanup pass becomes automated.

- 2026-05-28: Built `c2m.inspect` — a live-introspection toolchain that lets Claude (or anyone) dump the contents of named Max data structures over OSC while the patch is running. Five new pieces: (1) `patching/abstractions/c2m_inspect.js` — v8 dumper with `@cellcap`/`@samplecap`/`@tmpdir`/`@asyncdelay` attrs; supports `ping`/`scan`/`dump <tag> <kind> <name>`; writes JSON to `/tmp/c2m_inspect_<tag>.json`. (2) `patching/abstractions/c2m.inspect.maxpat` — drop-in abstraction wiring `[udpreceive 7474] → [route /inspect/ping /inspect/dump /inspect/scan] → [prepend …] → [v8 c2m_inspect.js]` plus an inlet for direct-message bypass and an outlet for status. (3) `tools/c2m_inspect_send.py` — dependency-free OSC sender (hand-encodes OSC 1.0 packets via stdlib socket+struct; no python-osc). Subcommands: `ping`, `scan`, `dump <tag> <kind> <name>`. Polls `/tmp/c2m_inspect_*.json` mtime and prints when updated. (4) `.claude/skills/c2m-inspect/SKILL.md` — slash command. (5) `patching/abstractions/c2m_inspect_test.maxpat` — test harness with `[c2m.inspect]` plus `coll TEST_COLL`, `dict TEST_DICT`, `table TEST_TABLE @size 16`, `buffer~ TEST_BUF 250`, `jit.matrix TEST_MAT 3 char 4 4`, loadbang init, and a button-per-route presentation UI. **Verified end-to-end against a live Max session**: ping/scan/dump-dict/dump-jit.matrix/dump-buffer~ all work over OSC; coll/table return graceful structured errors. **Three rules enshrined in `patching/MAX_PATCHING.md > Common Pitfalls`** (user-confirmed): (a) `messnamed` from v8/js only delivers to `[receive]` objects, NOT to globally-named coll/table/buffer~/jit.matrix/dict — silent failure if you assume otherwise. (b) `udpreceive <port>` (default mode, no symbol arg) natively decodes simple OSC messages into Max messages — the optional symbol arg is only needed for FullPacket/bundle mode. Earlier guidance in multi-user-template's CLAUDE.md that "Max 9 has no built-in OSC-to-message parser" was misleading; corrected here. (c) Max caches abstractions for the session — editing the file and reopening the parent does NOT reload; must close every patch holding the abstraction (one cached load keeps it pinned), then reopen. **Converter fix**: `spec2maxpat.py` now actually supports the 5-element connection form `[src, outlet, dst, inlet, {"hidden": 1}]` that SPEC_REFERENCE.md has long documented — three call sites updated to unpack `conn[:4]` and apply 5th-element attrs to the patchline dict. Coll/table support deferred to v2 — workaround documented in skill/JS: use a `[receive <NAME>_INSPECT]` upstream of the target, then `messnamed("<NAME>_INSPECT", ...)`. Also discovered: dict's `set steps[0] 1` syntax silently fails; correct array-set is `set steps 1 2 3 4` (list as value).

- 2026-05-28: Added a new "Step 3 — Install the global pointer to Claude2Max" to the New User Setup flow in CLAUDE.md (renumbered the GitHub steps to 4 and 5). The step runs for every non-maintainer mode (forked, cloned-no-fork, local-only) before any GitHub work, detects an existing pointer via `grep -q "Claude2Max" ~/.claude/CLAUDE.md`, offers to append a pointer block (substituting the absolute path of the current clone) when absent, and offers a path-drift update when present with a stale path. The pointer block makes consultation unconditional ("supersedes any older Claude2Max instruction that gated consultation on the literal phrase 'use claude2max'") so that any future Claude session on the same machine — in any project — defaults to reading Claude2Max's `CLAUDE.md`, `SPEC_REFERENCE.md`, and `patching/MAX_PATCHING.md` whenever Max work appears. Rationale enshrined inline: without the pointer, Claude instances opening unrelated Max patches elsewhere fall back to training-data memory, which fails silently in Max (unknown attributes accepted-and-ignored, made-up object names land as easily-overlooked red boxes). Rule was proposed in chat in plain language and confirmed by the user via "enshrine".

- 2026-06-13: MCP server task — Phase (i) Step 1 (research & decisions, no code) complete. Created `mcp_server/DESIGN_DECISIONS.md` (locked choices + per-OS path table + corrected sentinel scheme) and `mcp_server/requirements.txt` (pins `mcp>=1.27.0`). Findings verified via claude-code-guide agent (official docs) + live local inspection, not memory. **Two corrections to the TASK_QUEUE.md task body**, both silent-failure-class: (1) the session-id env var is **`CLAUDE_CODE_SESSION_ID`**, not the guessed `$CLAUDE_SESSION_ID` — confirmed live in a Bash subprocess, matches `~/.claude/session-env/<uuid>/` dir naming; the hook also receives `session_id` on stdin as the documented-reliable fallback. (2) Claude **Code** MCP config is `~/.claude.json` (`mcpServers` key, written by `claude mcp add --scope user`), NOT the guessed `~/Library/Application Support/Claude/` which is Claude **Desktop**'s `claude_desktop_config.json` — two distinct surfaces the installer must handle separately. Other locks: SDK = official `mcp`/FastMCP, Python ≥3.10; sentinel key resolution = env `CLAUDE_CODE_SESSION_ID` → stdin `session_id` → cwd-hash+1h-TTL, stored at `tempfile.gettempdir()` (this machine's TMPDIR is `/tmp/claude-501`, not `/tmp` — both subprocs inherit it); installer/hook/glue all Python (cross-platform parity); narrow tools (descriptions are prompts); read-once + mtime cache. Hook mechanics captured for Phase iii: matcher is tool-name-level (`Edit|Write`), file-type filtering happens in-script via `tool_input.file_path`; deny via stdout `hookSpecificOutput.permissionDecision:"deny"` (exit-2+stderr fallback); the global gate registers in **user-level** `~/.claude/settings.json` with an **absolute** hook path (not `$CLAUDE_PROJECT_DIR`, which points at the wrong repo from elsewhere) and merges idempotently. Confirmed-vs-uncertain table drives what `install_global.py --verify` exercises on Linux/Windows. Next: Step 2 — server skeleton + `essentials()` tool.

- 2026-06-20: MCP server Phase (ii) — end-to-end test of the `assess()`/`load()` module system, plus an architecture change to `assess()`. End-to-end test confirmed `load()` assembly and `assess()` routing; surfaced a real gap (keyword matcher keyed on Max jargon, not beginner plain-English — e.g. "granulate a recorded sound" routed to core-only). Per maintainer direction, **changed `assess()` to route by Claude reading task intent** (Anthropic API call, `claude-haiku-4-5`, parameterized via `CLAUDE2MAX_ASSESS_MODEL`) rather than substring matching. MCP sampling (no-key path) is unsupported by Claude Code ([#1785](https://github.com/anthropics/claude-code/issues/1785)), so the server makes its own API call — needs `ANTHROPIC_API_KEY` via `claude mcp add --env`; without it `assess()` degrades to the (now-broadened) keyword matcher and reports `method: "keyword-fallback"`. Added `anthropic>=0.40.0` to requirements (installed 0.111.0 in `.venv`). Verified: fallback path routes correctly; LLM parse/validate logic tested with a mocked client (clean multi-domain, empty→core, markdown-fence tolerance, invalid-domain dropping). **Live Haiku routing test passed** (key installed into `~/.claude.json` `mcpServers.claude2max.env`): all 8 cases routed correctly, all `method:"llm"`, with intent inference beyond keyword reach (e.g. "Max for Live device with a dial and a filter" → m4l+msp). Also confirmed the `anthropic` client logs to stderr, so `assess()` API calls don't corrupt the MCP stdio JSON-RPC stream. Docs updated: README (install/register + key + env var + fallback), PROJECT_CLAUDE_SNIPPET maintainer notes, SMOKE_TEST_RESULTS Phase (ii) section.

- 2026-06-20: MCP server — extended the "Claude reads intent" pattern from `assess()` to two more tools (per maintainer request to find other useful sites). (1) **`lookup_object()` did-you-mean**: on the not-found path, an LLM suggests the closest REAL objects (typos matched via difflib over a 3,969-name pool; concept-errors from model knowledge), every suggestion validated against the index and dropped if it doesn't resolve. New `did_you_mean` return key. Live: `oscparse`→`o.route`/`osc.packet`/`udpreceive`, `metroo`→`metro`, `jit.gl.videplane`→`jit.gl.videoplane`, `reverb~`→`cverb~`/freeverb externals. (2+3) **`search_packages()` semantic**: LLM query-expansion → substring-gather (literal full-query matches seeded first to survive the 40-cap) → LLM rerank by intent; new `method` field (`llm-semantic`/`substring`) + optional `why`. Live: "convolution reverb"→`hirt.convolutionreverb~`, "detect pitch"→`fluid.pitch~`/`sdt.pitch~`/`pipo.yin`, "granular synthesis"→`az.granulate~` etc. Caught+fixed a cap bug that dropped the obvious literal match before rerank. Shared plumbing: `_llm_json()` + `_LLM_MODEL` (env `CLAUDE2MAX_LLM_MODEL`, back-comp `CLAUDE2MAX_ASSESS_MODEL`, default haiku). Guardrail throughout: LLM only expands/reorders/suggests, never invents names — `lookup_object` found-branch + `lookup_attribute`/`list_attributes` deliberately left deterministic (authoritative lookups; LLM there would reintroduce hallucination). All paths verified: fallback (no key) degrades honestly; live tests pass; `py_compile`+import clean. Docs: README, SMOKE_TEST_RESULTS (new extension section).

- 2026-06-21: MCP Phase (iv+) complete — global skill (`skills/max-patching/SKILL.md`), PreToolUse enforcement hook (`hooks/claude2max_max_edit_gate.py`), one-command installer (`install_global.py` / `uninstall_global.py`) all shipped. Sentinel uses `tempfile.gettempdir()` for cross-platform portability; session key priority: `CLAUDE_CODE_SESSION_ID` env → stdin `session_id` → cwd-hash+1h-TTL. Hook gates `.maxpat`/`.maxhelp`/`.amxd` edits + adjacent `.js`/`.mjs`; deny via JSON stdout (exit 0). CLAUDE.md updated: global skill docs after Plugin/Slash Commands table; `.c2m-current-project` pointer file convention in State-File Location section; `install_global.py` offer in New User Setup Step 3. Also shipped same session: `maxhelp_observed_attrs.json` wired into `_GateResolver._load_observed_attrs()` with alias fix in `attrs_for()`, ≥3-box floor, `^rnbo`/`^frozen` filter — attribute validity is now `own refpage ∪ jbox ∪ observed-in-help`. 38/38 verify tests pass; 4 production patches convert clean with 0 violations; `patches/4step-sequencer.maxpat` committed.
- 2026-06-21: c2m.inspect coll/table support finished (code) + v2 planned. **coll/table**: wired the previously-stubbed async dumpers into `c2m_inspect.js`'s `dump()` dispatch (they returned a "v1 gap" error before). New `reachNamedWrite()` tries path (a) `pat.getnamed(name).message("write", file)` (zero-wiring, needs Scripting Name == name, same patcher) then falls back to path (b) `messnamed(name+"_INSPECT", "write", file)` (needs `[receive NAME_INSPECT] → [coll NAME]` wire). API names verified, not guessed: `Maxobj.message` against bundled v8 examples, `getnamed` against the scripting userguide, `messnamed`→receive-only is the repo's 2026-05-28 pitfall. A `SENTINEL` pre-write + `notReached()` distinguishes a no-op write from real data (returns `collTableSetupError` naming both setup paths instead of reading stale data); result carries `reach_method`. `node --check` clean. Docs synced: JS header, `scan()` kinds, `/c2m-inspect` SKILL.md, CLAUDE.md (§ Debugging Data Structures Live + cross-ref), MAX_PATCHING.md messnamed pitfall. **Live verification still owed** (queued as `[in progress]`): round-trip in `c2m_inspect_test.maxpat`, and specifically validating `parseTableText` against real `table write` output (the one untested parser). **v2 planned** (queued, Sonnet): two-way live debugging — `/inspect/send <dest> <msg…>` (inject via getnamed/messnamed) + `/inspect/watch <tag> <dest> [attr]` (targeted readback via attr getters / pattr tap / wired `_PROBE` convention), gated behind `@allowinject 1`, additive over v1.5, keeping the disk return-path. Also answered John's framing Qs: inspect stays opt-in debug scaffolding (not in every patch); jit.desktop rejected for UI inspection (pixels-as-JSON is useless; screenshot wins).

- 2026-06-21: c2m.inspect coll/table — **live-verified** + two bugs fixed. Added `[receive TEST_COLL_INSPECT]→[coll TEST_COLL]` + table-equiv relay wires to `c2m_inspect_test.maxpat` (sync→edit-spec→convert, IDs preserved). Round-trip confirmed in live Max: coll→`{1:alpha,2:beta,3:gamma,4:delta}`, table→`[100,…,800,0×8]` (all 16 cells), error path→structured setup error. Two bugs surfaced+fixed during verification: (a) `parseTableText` was written for an assumed `data N …;` block — real `table write` emits a flat `table v0 v1 …` line; rewrote the parser. (b) JS `File` "write" mode does NOT truncate — a shorter write left a tail of the previous longer dump (success JSON + stale error-JSON fragment); added `f.eof = f.position` in a new shared `writefile()` (empirically confirmed the truncate works, so `eof`/`position` File props verified). Both enshrined as Common Pitfalls in `patching/MAX_PATCHING.md` (user-approved). coll/table task → Done; v2 (two-way send/watch) still queued.

- 2026-06-21: Enshrined John's c2m note as a Common Pitfall in `patching/MAX_PATCHING.md` — `pack`/`unpack`/`pak` need per-slot type specificity (unspecified slots silently default to int, truncating floats / mishandling symbols), e.g. `unpack 0 0.` or `unpack i f`; `unjoin` is type-agnostic (configured only by number of outlets, groups the list untouched). Verified against `pack.maxref.xml` (no-arg = two int-0 slots) and `unjoin.maxref.xml` (configured by "Number of outlets").

- 2026-06-27: maxhelp prose-insight extraction pass 1 — read 20+ core `help/max/` help patches and wrote structured per-object notes to `maxhelp/maxhelp_insights.md` covering timing (metro/qmetro/clocker/delay/pipe/defer/deferlow), fan-out (trigger/bangbang/gate/switch), routing (route/select), list ops (pack/unpack/iter/uzi/append/prepend/sprintf/tosymbol/fromsymbol), data storage (coll/dict/table/value/pv), initialization (loadbang/loadmess/change/onebang), and debug (print/number/send/receive). Promoted four PROMOTION-CANDIDATEs to reference docs (user-confirmed): (1) `gate` vs `switch` naming clarification in MAX_PATCHING.md + new rows in CLAUDE.md preferred-objects table; (2) `select @fuzzy` for float values from UI in MAX_PATCHING.md; (3) `sprintf symout` for single-symbol output in MAX_PATCHING.md; (4) `qmetro = metro @defer 1` clarification in CLAUDE.md metro row.

- 2026-08-20: Rule-enforcement task item 1 — built `verify_patch()`. Added `verify_patch_file(path, resolver=None, use_resolver=True)` to `spec2maxpat.py` (mirrors `gate_maxpat_file`'s reference shape, but runs the FULL rule library, not just resolver-only checks): prefers the embedded Claude2Max spec via `extract_spec` when present, falls back to per-nested-subpatcher native-scope conversion (`_iter_patcher_scopes`) otherwise — the fallback is what lets it check C74's own corpus, which never carried a Claude2Max spec. Wired three ways: `verify_patch(path, sweep, use_resolver)` MCP tool in `server.py`; `python3 spec2maxpat.py verify <path...> [--summary] [--json] [--no-resolver] [--limit N] [--kinds ...]` CLI with directory-sweep support and a resolver built once and reused; direct import for scripting. Verified against `patches/drift-sequencer.maxpat` (clean), `patches/jit-grab-scale.maxpat` (3 warnings, matches prior documented result via both CLI and direct tool call), a `patches/` sweep (matches the prior "1 warning total" figure), and a 25-file slice of the real C74 help corpus (surfaced the expected false-positive shape for item 2 — `presentation-required` firing on help patches never authored with Claude2Max conventions — plus genuine `object-unresolved`/`attribute-invalid` hits). One bug caught during verification: embedded-spec branch omitted `report`, crashing the CLI's report print; fixed. All 38 existing golden tests still pass. Item 2 (11,866-file corpus sweep + per-rule false-positive table, Opus reads) is next.

- 2026-08-20: Rule-enforcement task item 2 (Sonnet half) — full corpus sweep. Ran `verify_patch_file()` over all 11,873 files in `maxhelp/maxhelp_crawl_state.json` (core help/, bundled packages/, user Max 9/Packages) — 10.84s wall time, 11,824 checked, 49 unchecked (`not-a-patcher-file`). Results in `mcp_server/CORPUS_SWEEP_RESULTS.md` + `mcp_server/corpus_sweep_summary.json` (both committed; the 3.6MB per-file JSON was left in scratch, regenerable in ~11s). Headline: ~44% of checked files (5,224/11,824) carry an `error`-severity violation — much higher than the task write-up anticipated for the principle tier, and notable because those three resolver rules already block `convert` in production today, not just a future promotion candidate. `hidden-box-cord-visible` is the single biggest hitter by volume (44,854 hits/4,236 files). Flagged both for Opus's item-3 read; did not make any promotion decisions myself.

- 2026-08-20: Rule-enforcement item 3 (Opus read) — the corpus sweep inverted the task's premise. Decomposed every error-tier violation by root cause instead of trusting the 44% aggregate: the ERROR tier is ~98%+ false positive and is a **live production defect**, not a future promotion risk. `attribute-invalid` is 98.6% RNBO-stamped attrs (`rnbo_serial`/`rnbo_uniqueid`/`rnbo_classname`/`rnbo_extra_attributes`) + `frozen_*_attributes` — tool-written structural metadata, not user attributes. `object-unresolved` is ~41% symbolic-operator aliases missing from the hand-maintained table (`/`, `&`, `|`, `>>`, `<<`, `!-`, `!/`, `/~`, all `mc.*`), ~15% gen-domain vocabulary (the scope walker descends into `gen~`/`jit.gen`/`rnbo~` and checks their operators against the Max object set), ~12% local abstractions. Verified end-to-end that a spec using `[/ 2]` and `[i]` is BLOCKED from converting ("'/' is not a known Max object", exit 1) — division. Sensitivity confirmed intact (`oscparse`/`jit.pwindo`/`metroo` still caught), so this is purely a specificity failure. Discovered C74 declares operators' real names in the refpage XML root's `name` attribute (`div.maxref.xml` → `name="/"`), so the alias table can be harvested mechanically: 56 aliases, zero collisions, vs 23 hand-maintained across two files. Simulated the fixes (no repo edits): object-unresolved 8,291→1,101 (−87%), attribute-invalid 21,487→162 (−99.2%); residual still mostly real objects, with no confirmed genuine invented-name detection anywhere in 11,873 files. Also concluded the corpus **cannot** authorize principle-tier promotion at all — C74 patches are a different population, so `presentation-required`/`hidden-box-cord-visible` volume measures preference not defect, exactly as the task predicted; the 5-patch evidence blocker stands. Analysis + recommendation in `mcp_server/CORPUS_SWEEP_RESULTS.md` § "Item 3 — Opus read"; evidence in `corpus_sweep_causes.json` + `corpus_sweep_simulated_fix.json`. Holding all warn→block promotion; fixes to the block tier await John's sign-off (one open question: corpus-harvested object names would clear the residual but risk whitelisting gen operators as valid Max objects).

- 2026-08-20: Rule-enforcement item 3b — fixed the misfiring block tier (John approved all four causes + corpus-harvest-with-scope-tracking). **The open question resolved better than either option offered**: the Max install ships its own authoritative object database — `Contents/Resources/C74/interfaces/obj-qlookup.json` (1,323 core objects, each with an optional `alias` field: `v`→value, `i`→int, `f`→float, `t`→trigger) plus per-package `obj-qlookup.json` and `max.db.json`'s `maxdb.aliases` registry (`jit.mo.sin`→jit.mo.func, `jit.gl.layer`→jit.gl.videoplane); 1,342 names / 361 aliases. It resolves every real object in the residual and rejects every negative control — `oscparse`, `metroo`, `jit.pwindo` and the gen operators `clamp`/`history`/`swiz` are all absent because they aren't Max objects — so the gen/Max boundary comes for free with no scope tracking and no corpus harvesting. Also discovered C74 declares operators' real names in the refpage XML root's `name` attribute (`div.maxref.xml` → `name="/"`), making the alias table harvestable instead of hand-maintained (56 vs 23, zero collisions). Seven changes: object-db resolution source; harvested alias map replacing both hand-maintained tables; `_iter_patcher_scopes` no longer descends into gen/`rnbo~` (foreign languages); `rnbo*`/`frozen*` skipped as tool-stamped not authored; bundled abstractions under `Max.app/.../patchers/**` resolve case-insensitively; package refpages nested a level deeper found (RNBO); bare-numeric object text treated as int/float constant + `linecount` added to universal box attrs. **Measured A/B over an identical 3,009-file set with the "before" arm materialized from `git HEAD` (not a simulation)**: `attribute-invalid` 21,487→95 (−99.6%), `object-unresolved` 8,291→203 (−97.6%), files-with-error 1,590→169 (−89.4%). Sensitivity re-verified after every fix: invented names still unresolved and still block convert (exit 1), gen operators in a Max scope still flagged, `bgcolor`-on-`live.gain~` still invalid while `coldcolor` valid, 38/38 golden tests pass, five in-repo patches verify identically. The reported defect is gone — the `[/ 2]` + `[i]` spec now builds. Note: the re-measurement excludes the `~/Documents/Max 9/Packages` tier because macOS began denying reads there mid-session (`Operation not permitted`), so absolute totals aren't comparable to the earlier 11,873-file figures; both A/B arms ran the same 3,009 files.

- 2026-08-20: Caught and fixed a tool-vs-gate divergence I had just created. While documenting the object-registry rule in CLAUDE.md I asserted that `mcp__claude2max__lookup_object` reads the registry; verifying instead of trusting that claim showed it did not — the tool reported `/`, `i`, `v`, `jit.gl.layer`, `jit.mo.*` as NOT FOUND while the convert gate resolved and built them. That is precisely the bug class the repo was bitten by before (`lookup_attribute`/`list_attributes` refpage-only vs a gate validating `refpage ∪ jbox ∪ observed`), recreated in a new place by my own change. Fixed by routing `lookup_object` through the same cached `_GateResolver`: alias hits return `source="c74-refpage-alias"` with `alias_of` and the target's documented I/O; registry-only hits return `source="max-object-db"` with an explicit note that no refpage exists so inlet/outlet counts must be declared in the spec. Verified 0 tool-vs-gate disagreements across 17 positive and negative cases; 38/38 tests still pass; `[/ 2]` still builds and `oscparse` still blocks. Lesson worth keeping: a claim about the repo's own tooling is external state — verify it by running the tool, exactly as CLAUDE.md's "this repo's own tooling counts as state to verify" says.

- 2026-08-21: Added a "Write Responses in Plain Language" convention after John's feedback that Opus responses were often hard to parse. Global copy in `~/.claude/CLAUDE.md` § Conversation Conventions (applies in every project); repo copy in `CLAUDE.md`, adapted for the student audience. Both state the same six practices (lead with the plain fact; one idea per sentence; spell out or drop jargon; say what it means for the reader; end decisions as a clear question with named options; keep structure light) and both carry the guard that this governs prose, not rigor — do not simplify the work, drop caveats, or soften bad news to shorten a sentence.

- 2026-08-21: Swept the rule-enforcement task's early items for work buried in prose rather than tracked as numbered items — prompted by John noticing the two missing rule families were described inside item 3's body and would have been lost when item 3 completed. Promoted four things to real queue entries: **item 8** (embedded-spec presence/integrity + spec-vs-patch object-count drift, moved out of item 3, with the design question named — the presence check must not fire on the 11,873 native C74 patches that legitimately have no embed, so it needs an "is this ours?" discriminator); **item 9** (unmissable convert summary line when only warnings fire — matters more now that item 3 held every principle rule at WARNING, since a warning nobody reads is the same as no rule); **item 10** (creation-arg arity against the refpage `<objarglist>`, whose data `RefpageCache._parse()` already extracts and no rule consumes); **item 11** (promote `message-unverified` to ERROR, blocked on the corpus below). Added **deliverable (e)** to the `.maxhelp` Corpus Crawl: an observed-MESSAGES corpus, the exact counterpart of the observed-attrs map that unblocked the attribute rule — `message-unverified` is the largest warning family in the sweep (4,717 hits / 964 files) and cannot be promoted without it. All four came from `mcp_server/DESIGN_DECISIONS.md` § (j) and § (i).1, which item 1 pointed at but whose suggestions had never been promoted to the queue — the same burial pattern. Also recorded a reproducibility caveat on item 2: macOS TCC began denying `~/Documents/Max 9/Packages` mid-session, so a full-corpus re-run needs Full Disk Access or the 8,815-file user-packages tier silently drops out and totals won't match. Fixed the stale cross-references in the `extract_spec` task and item 3 to point at item 8.

- 2026-08-21: Added the **Creative proposal tool (`/c2m-propose`)** task to the queue at John's request — a skill that generates rather than verifies: creative solutions, creative responses to prompts, starting points for new explorations (with or without a seed), and oblique angles on an existing patch. Wrote it up with the central tension named explicitly (Claude2Max exists to stop Claude inventing; this tool's job is to invent) and resolved as **generative about ideas, strict about names** — propose freely at the concept level, but every Max object named must still verify or a student will try to build a trap. Flagged that the hard part is *diversity, not generation* (five ideas is easy, five different ones is the engineering), so each proposal must come from a named lens and be able to say how it differs. Every proposal needs a smallest-possible-first-step. John's three decisions recorded as binding: log the proposals (feeds the community pipeline, and is the only way to learn which lenses pay off); be opinionated when invoked, but the tool is **optional, not standard** — it fires only when deliberately invoked, never proactively, and is not part of the default workflow; `oblique` may break the piece's stated concept but never proposes something technically unbuildable. John also caught a real language error in my example — "put [playlist~] **behind** [cv.jit.faces]" — see the next entry.

- 2026-08-21: Enshrined the directional-vocabulary rule John caught, and wired it into the MCP. He flagged that "put [playlist~] **behind** [cv.jit.faces]" is subtly wrong — Max flows top→bottom, left→right, so the Max-native phrasings are "under", "downstream of", "connect A to B", "A feeds/drives B". Generalized before enshrining, per the repo convention: the underlying concern is **borrowing directional vocabulary from an adjacent domain**, which is worse than clumsy when the borrowed word already means something else in the target. In Max it does — "behind" means **z-order** (the `background` attribute the tutorial system depends on), so the phrasing reads as a layering instruction rather than a wiring one. Added as a `## ` binding rule in `CLAUDE.md` (which makes it automatically indexed by `lookup_rule` — verified: `lookup_rule("behind")` returns it as top hit) AND added to `_ESSENTIALS_MD` so it front-loads via `load(["core"])` — verified present in the 33K core module, since `lookup_rule` only finds it on demand while core is always loaded. 38/38 tests still pass. Queued **item 12** for the mechanical half John asked for, with the scope stated honestly: chat prose CANNOT be hook-enforced (MCP can't block; hooks fire on tool calls, not sentences), so the checker targets the durable surfaces that outlive the conversation — comment boxes, `attrs.comment`, tutorial step text. Named the false-positive trap that makes it non-trivial: "behind" is CORRECT for z-order, and this repo's own tutorial system uses it that way, so a naive substring check would flag the repo's own correct output; must calibrate against the corpus and ship a negative test before choosing severity.
- 2026-08-21: Dependency-ordered priority pass over TASK_QUEUE.md — first dated hygiene note since 2026-06-21. Ordering re-derived from dependency edges rather than value/position; six edges corrected (bypass-trace before repair, detection before repair, item 11 added as B1's downstream, prose-extraction before package pass, student-setup before community pipeline). Verified Full Disk Access to `~/Documents/Max 9/Packages` is currently working (3,162 files readable), so the 2026-08-21 TCC caveat in CORPUS_SWEEP_RESULTS.md is not blocking — kept as a pre-sweep check.

- 2026-08-21: Amended the plain-language rule in both `CLAUDE.md` and `~/.claude/CLAUDE.md` after John caught an impenetrable sentence of mine. Two things were true: the rule was not followed (three ideas in one sentence, a compound noun coined on the spot, a rule referenced by description instead of by name), AND the rule's recognition signal genuinely could not have caught it — the existing signal only fires on sprawl (second read to find the subject, stacked dash-clauses), and the offending sentence was short. Added a second signal for the opposite failure, compression: if a sentence assumes the reader can fill in a name, a definition, or a step you did not state, rewrite it. Generalized before enshrining per repo convention — the principle under both signals is that the reader should not have to supply anything you did not give them; sprawl buries it, compression omits it. No MCP sync needed: `lookup_rule` reads `CLAUDE.md` directly, and the plain-language rule is not one of the nine sections duplicated in `_ESSENTIALS_MD`.
- 2026-09-08: Built `patches/kslider-restrike.maxpat` + `patches/kslider-restrike.js` — a polyphonic `kslider` driving `vst~` (kslider → `join 2` → `midiformat` outlet 1 → `vst~` → `live.gain~` → `ezdac~`, mirroring C74's own vst~ help wiring), a `flush` message into the kslider, and a v8 that stores note-ons, drops them on note-off, and on `restrike` flushes the kslider then re-sends velocity + pitch for every stored note (outlets 0/1 feed back into the kslider's two inlets; outlet 2 → `prepend set` → readout message). Three-panel presentation view (plug-in / keyboard / output). Every object and attribute was checked through the MCP server's `lookup_object` / `lookup_attribute`, and `verify_spec` passed with 0 findings. Tooling finding: the MCP server is not registered in Claude Code (`claude mcp list` empty) and cannot run here as-is — `mcp_server/.venv` was a broken stub, the only interpreter is `/usr/bin/python3` 3.9.6, and `mcp>=1.27` needs Python ≥3.10 (`brew` is available). Worked around by importing `server.py` with a stubbed `mcp` SDK and `from __future__ import annotations` (server.py uses `dict | None` at def time). Visual check of the presentation view was not possible: Max exposed no accessibility windows and System Events timed out.
- 2026-09-08: MCP server brought up for real. John installed `python@3.12` via Homebrew (the only prior interpreter was Apple's 3.9.6, below the SDK's 3.10 floor). Rebuilt `mcp_server/.venv` on `/opt/homebrew/bin/python3.12`, installed `mcp` + `anthropic`, and registered the server at user scope with `claude mcp add --scope user claude2max -- <venv python> mcp_server/server.py`; `claude mcp list` reports it connected. No `ANTHROPIC_API_KEY` passed, so `assess` / did-you-mean / semantic package search run on their deterministic fallbacks until one is added. Gotcha: `rm -rf .venv` under Dropbox failed once with "Directory not empty" because Finder/Dropbox dropped `.DS_Store` files into it mid-delete (a stale `lib/python3.14` remnant from another machine's sync was also present) — a second `rm -rf` cleared it.
- 2026-09-08: Completed the global enforcement layer at John's request: `install_global.py` installed the `max-patching` skill to `~/.claude/skills/` and merged the PreToolUse edit gate plus the PostToolUse content gate into `~/.claude/settings.json` (MCP registration was already in place from the earlier step). Takes effect in the next Claude Code session.
- 2026-09-08: Found and fixed a blocking bug in `install_global.py` the moment the hooks went live: hook commands were written as `python3 <path>` with the repo path unquoted, and this clone lives under `john jannone/`, so the shell split the path, Python exited 2, and Claude Code treated exit 2 from a PreToolUse hook as a hard block on EVERY Edit/Write (my own edits were refused mid-fix). Scrubbed the two broken entries from `~/.claude/settings.json`, changed both hook builders to `shlex.quote()` the path, reinstalled, and confirmed each installed command runs through `sh -c` with exit 0. Also made `install_mcp` register the venv interpreter (`mcp_server/.venv/bin/python3`, falling back to `sys.executable`) instead of a bare `python3`, which on this machine resolves to Apple's 3.9 and would have produced a registration that never connects. Rule proposed to John (not yet enshrined): any path handed to a shell — hook commands, launch configs, MCP registrations — must be shell-quoted, because a path with spaces is a silent failure until it runs.
- 2026-09-08: Reworked `kslider-restrike.js` after John's Max test: restrike now sends exactly two messages to the kslider — `flush`, then one `chord p1 v1 p2 v2 …` built from the stored notes — instead of per-note velocity/pitch pairs into both inlets (which produced a spurious `0`-prefixed message). v8 dropped to 2 outlets; the v8→kslider right-inlet cord is gone; readout moved to outlet 1. Sync → extract → edit → convert; `verify_spec` clean. Doc discrepancy found, not yet fixed: `SPEC_REFERENCE.md` (kslider section) says `chord` "does not echo back out the outlets", but the refpage digest is "Display and output chords" and `kslider.maxhelp` wires `chord` into a kslider whose outlets feed `pack` downstream — chord DOES output.
- 2026-09-08: Two presentation defects John spotted in `kslider-restrike.maxpat`, fixed and enshrined. (a) The patching-only v8 note was dark text on the dark label background — the `lbl()` helper stamped the panel bgcolor on every comment and the caller only overrode textcolor; now a plain default comment. (b) The keyboard hint's box (y 176, h 20) ran under the flush row at y 190; re-flowed the keyboard panel with computed row spacing and grew it and the output panel. Added a mechanical rect-overlap test, a panel-containment test, and a bg/text luminance-diff test to the spec check; all clean. Notes written to `patching/MAX_PATCHING.md` (two new presentation subsections: per-comment contrast; per-row vertical budget) and two matching lines in `PRE_EDIT_CHECKLIST.md`.
- 2026-09-08: John approved all three doc texts: the two presentation subsections in `patching/MAX_PATCHING.md` stay as written; enshrined *Any Path Handed to a Shell Must Be Shell-Quoted — Binding Rule* in `CLAUDE.md` (placed just before Workflow); corrected the kslider `chord` note in `SPEC_REFERENCE.md` — it DOES output through the outlets (refpage digest "Display and output chords"; `kslider.maxhelp` wires chord → kslider → pack), replacing the earlier claim that it does not echo.
- 2026-09-08: Two more notes from John's review of `kslider-restrike`, enshrined in `patching/MAX_PATCHING.md > Common Pitfalls` (patch deliberately NOT rebuilt): (1) a readout `message` box takes its value on the right inlet — `[prepend set]` into the left inlet is a redundant adapter; (2) never add an outlet/message to an object just for monitoring — use `[print NAME]` on an existing outlet or `post()` in JS. The patch still carries both patterns as a worked example of what the notes correct.
- 2026-09-08: Enshrined John's cord-clutter rule of thumb in `patching/MAX_PATCHING.md > Patching Layout` (new subsection just above the `s~`/`r~` one, which becomes its signal-rate case): a cord whose vertical span crosses boxes it does not connect becomes `[s NAME]` / `[r NAME]`; short local cords stay cords; the check is geometric (endpoint rectangle vs. third boxes). For-instance is the `plug`/`open` → `vst~` cords in `kslider-restrike`, which run through the keyboard/join/midiformat region. Matching line added to `PRE_EDIT_CHECKLIST.md`. Patch not rebuilt, per John.
- 2026-09-08: Fixed `install_global.py --env`: the positional server name now precedes the variadic `--env` option (which was swallowing it), and an existing registration is removed and re-added when `--env` is passed, since `claude mcp add` refuses to update an existing entry. Verified MCP server healthy via smoke tests; assess runs keyword-fallback until ANTHROPIC_API_KEY is registered.
- 2026-09-08: Fixed `search_packages` semantic path in `mcp_server/server.py`: candidates are now ordered by how many expansion terms hit (then score, then name), so one short expansion term ("IR", 339 substring matches) can no longer fill the 40-candidate cap with alphabetical noise before the reranker sees the real answer. The semantic-path `except: pass` now reports why it fell back in the result message. Verified end-to-end with the registered ANTHROPIC_API_KEY: assess, did-you-mean, and semantic search all report their LLM method.
- 2026-09-08: Audited `CLAUDE.md`, `SPEC_REFERENCE.md`, `patching/MAX_PATCHING.md`, `PRE_EDIT_CHECKLIST.md` against the verifier's 15 rule functions; queued 22 enshrined-but-uncoded rules as item 13 of the rule-enforcement task in `TASK_QUEUE.md` (presentation overlap first, prompted by the kslider-restrike resave). Two proposed rules (orphan label, dead outlet) discarded by John.
- 2026-09-08: Found `patches/kslider-restrike.maxpat`'s embedded spec is stale — it matches the boxes of `29266b6` exactly, while the boxes carry the later s/r rework, `print v8`, the loaded vst~ plug-in, and two Max-resized presentation rects. A `convert` would revert all of it. Not repaired yet; queued as a Sonnet task (repair + a content-based `spec_matches_patch` check surfaced in `sync --check` and `verify_patch`, superseding queue item 8b). Lesson: `verify_patch` in embedded-spec mode verifies the spec, so on a file another session edited it verifies the wrong thing.
- 2026-09-08: Repaired `patches/kslider-restrike.maxpat`'s stale embedded spec with `sync` (28/28, no orphans, zero drift, verifier clean). Enshrined the rule in `CLAUDE.md > Workflow`: a patch from another session/person/source is stale until the spec-vs-boxes check passes, and that covers analyzing, verifying, and committing, not only editing.
- 2026-09-08: Labeled `kslider-restrike.js`'s inlet and outlets with `setinletassist` / `setoutletassist` (confirmed in C74's shipped `jitgltextureset.js` and Max's code-editor declarations). Extended the inlet/outlet labeling rule in `CLAUDE.md > What You Must Handle` to `v8` / `js`, mirrored in `SPEC_REFERENCE.md > JavaScript Objects`, `PRE_EDIT_CHECKLIST.md`, and queue item 13(u)'s JS scanner.
- 2026-09-08: Fixed the vocabulary: *inside* = viewing an encapsulated object's contents (p, poly~, gen~, jit.gen, rnbo~ …), *outside* = seeing the box in the parent; a label follows where it shows. Written into `CLAUDE.md > Describe Flow in the Target Environment's Own Directional Vocabulary` and into the MCP core module (`_ESSENTIALS_MD` in `mcp_server/server.py`) so `load("core")` carries it.
- 2026-09-08: Audited the MCP core against the docs: `load("core")` is a hand-written 11k digest plus Common Pitfalls; 13 patch-relevant `CLAUDE.md` rules and four whole `MAX_PATCHING.md` sections (Patching Layout, Patching Principles, Presentation Design Principles, UI Layout) reach no module. Queued a Sonnet task to replace the digest with a `{!core}` heading tag and a mechanical extractor, with the full tagging list.
