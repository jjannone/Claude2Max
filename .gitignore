# macOS
.DS_Store

# Python build artifacts
__pycache__/
*.pyc
mcp_server/.venv/

# Claude Code — local personal overrides (not for sharing)
.claude/settings.local.json
.claude/worktrees/

# jjannone-max — John's personal corpus crawl. Only the insights file is
# tracked: it carries generalized Max technique. The state file, the catalog,
# the log and the enumerator all name his private project folders and carry
# absolute paths with his account name, and this repo is public.
scans/jjannone-max/*
!scans/jjannone-max/jjannone_max_insights.md

# What sync learns about port counts from patches Max saved on this machine.
# Those patches can be private; the tracked registry is built from Max's own
# files and public packages only.
scans/maxhelp/maxhelp_port_counts_local.json
